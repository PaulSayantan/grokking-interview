export const meta = {
  name: 'hibernate-jpa-deepen',
  description: 'Pass 2 for Hibernate & JPA: exhaustive research gap-analysis, then deepen concepts + append advanced/expert MCQs to 70-90 per topic, then verify — additively',
  phases: [
    { title: 'Research', detail: 'exhaustive web gap-analysis per topic: what Hibernate/JPA depth are we missing?' },
    { title: 'Deepen', detail: 'fill gaps in concepts + append advanced/expert MCQs' },
    { title: 'Verify', detail: 'fact/version-check, dedupe, schema-check each topic in place' },
  ],
}

// Repo root. Pass `args.root` when invoking this workflow, or edit the
// fallback for your clone. The fallback is deliberately not a real path so a
// misconfigured run fails loudly instead of reading the wrong tree.
const REPO = (typeof args !== 'undefined' && args && args.root)
  || '/path/to/interview-prep'
const DIR = `${REPO}/topics/hibernate-jpa`

const SCOPE = `
DOMAIN SCOPE — "Hibernate & JPA" for BACKEND + SENIOR Java interviews. The ORM layer: how JPA (the
spec) and Hibernate (the reference implementation) map objects to relational tables, manage the
persistence context, and generate SQL. This is a HIGH-FREQUENCY interview domain — the Pass-2 goal is
to reach the depth that separates a senior/staff candidate from someone who has just used @Entity.
Ground everything in CURRENT standards: Jakarta Persistence 3.1/3.2 (jakarta.persistence.* — NOT the
legacy javax.persistence.*), Hibernate ORM 6.x/7.x (SQM query engine, the new JavaType/JdbcType type
system, @JdbcTypeCode), and Spring Data JPA. Be precise about versions and show REAL annotations +
REAL generated SQL.
- BOUNDARY vs already-authored domains (teach the Hibernate/JPA angle, CROSS-REFERENCE, do NOT re-teach):
  * messaging-databases OWNS SQL itself, B-tree/LSM internals, ACID isolation levels & anomalies,
    indexing, transactions AT THE DB LEVEL. HERE, teach how Hibernate/JPA USES the DB (dirty checking,
    flush, persistence context, JPA lock modes) and reference messaging-databases for DB mechanics.
  * spring-boot / spring-core OWN the container, @Transactional propagation/proxying, auto-config.
    HERE, 'spring-data-jpa-repositories' teaches the JPA repository abstraction and references spring-*.
  * system-design owns high-level data architecture. HERE stay at the ORM mechanism altitude.
- Ground EVERY claim (annotation names/defaults, fetch defaults, generation strategies, cascade types,
  lock modes, cache concurrency strategies, HB6/7 changes) in the Jakarta Persistence spec + Hibernate
  User Guide and verify anything uncertain via web research. A wrong default/version claim is the worst defect.
`

// slug -> next question seq (new questions start here); + research focus per topic.
const TOPICS = [
  { slug: 'orm-fundamentals-jpa-vs-hibernate', name: 'ORM Fundamentals & JPA vs Hibernate', start: 51,
    dig: "the object-relational impedance mismatch in depth (granularity, subtype/inheritance, identity — Java == vs equals vs DB PK, associations/directionality & FK-vs-reference, data-navigation/N+1); JPA spec provenance (JSR 220/317/338, Jakarta Persistence 3.0 rename + 3.1/3.2), the persistence-provider SPI (PersistenceProvider, how Hibernate is discovered); Hibernate architecture layers (SessionFactory/EntityManagerFactory, Session/EntityManager, Transaction, the ServiceRegistry/Metadata bootstrap in HB6); StatelessSession (no L1/dirty-checking/cascade/cache — bulk); Hibernate-native features BEYOND the spec (@Formula, @Where, @Filter, @NaturalId, multi-tenancy, Envers, @DynamicUpdate/@DynamicInsert, @Immutable); when NOT to use an ORM (bulk/ETL/reporting -> jOOQ/native/StatelessSession); JPA vs jOOQ vs MyBatis vs Spring JDBC trade-offs; provider portability vs Hibernate lock-in." },
  { slug: 'session-entitymanager-persistence-context', name: 'Session, EntityManager & Persistence Context', start: 49,
    dig: "persistence-context as unit-of-work + L1 cache in depth (the entity entries/EntityEntry, snapshot for dirty checking, the action queue); transaction-scoped vs EXTENDED persistence context (stateful session beans, Spring @PersistenceContext(type=EXTENDED), conversations); the OSIV pattern & its L1 lifetime; getReference/load lazy proxy vs find eager + EntityNotFoundException on deref; contains/detach/clear/flush/merge/refresh precise semantics; propagation of the persistence context across @Transactional methods (join vs new context); thread-safety (EMF/SessionFactory thread-safe & singleton vs EM/Session per-request not-thread-safe); the persistence context size / memory issue in batch loops (clear() every N); repeatable-read guarantee within a context; how flush interacts with query execution (auto-flush before a query touching dirty state); first-level cache is NOT disable-able." },
  { slug: 'entity-lifecycle-states', name: 'Entity Lifecycle States', start: 54,
    dig: "the 4 states + EVERY transition edge case: persist on detached (throws), persist on removed (re-manages), merge semantics in depth (returns a managed COPY, cascade MERGE, merging a detached graph, merging with a stale @Version -> OptimisticLock), remove on detached (IllegalArgumentException) vs on transient (no-op) vs on managed; refresh (overwrites in-memory changes from DB) & refresh on detached; the Hibernate-native save/update/saveOrUpdate/lock/replicate & why they're DEPRECATED in HB6 in favor of JPA persist/merge; re-attachment (Session.update vs merge, NonUniqueObjectException when two instances with same id in a context); detached-entity-passed-to-Spring-Data-save; identity across states (proxy vs actual, LazyInitializationException as a state issue); cascade on lifecycle ops; the interaction of flush with each transition." },
  { slug: 'primary-keys-and-id-generation', name: 'Primary Keys & ID Generation Strategies', start: 49,
    dig: "SEQUENCE allocation optimizers in depth (hi-lo legacy & its holes, pooled vs pooled-lo, allocationSize=50 default & the multi-node/gap trade-off, SequenceStyleGenerator in HB6); IDENTITY disabling JDBC batch inserts (why — id known only after INSERT) & performance implication; TABLE generator contention/row-lock; AUTO behavior change across HB versions (HB5 shared hibernate_sequence vs HB6 per-entity <entity>_seq); UUID: JPA 3.1 GenerationType.UUID + Hibernate @UuidGenerator (RANDOM v4 vs TIME v7-ish) + the index-locality/page-split problem of random UUIDs as clustered PKs (cross-ref messaging-databases indexing) + storage (BINARY(16) vs CHAR(36)); natural vs surrogate & business keys; composite-key id gen (can't @GeneratedValue); @MapsId shared PK; the equals/hashCode-with-generated-id problem (id null before flush, HashSet membership changes — use business key or app-assigned UUID); custom IdentifierGenerator." },
  { slug: 'entity-mappings-associations', name: 'Entity Mappings & Associations', start: 49,
    dig: "owning vs inverse side deep (only the owning side's changes are persisted — updating only mappedBy side is a silent no-op; syncing both sides with helper methods); @JoinColumn vs @JoinTable trade-offs; unidirectional @OneToMany pitfalls (with @JoinColumn generates extra UPDATE statements or a join table; the notorious performance issue -> prefer @ManyToOne owning or bidirectional); @ManyToMany problems (can't add join-table columns, delete-all-then-reinsert on modify, List vs Set) & the modeling fix (explicit join entity with two @ManyToOne + @EmbeddedId); List vs Set vs Map collection semantics (bag semantics, the @OneToMany List + @OrderColumn, duplicate handling); default fetch (ToOne EAGER / ToMany LAZY) & making ToOne LAZY (bytecode enhancement or @ManyToOne(fetch=LAZY) + why optional=false helps); @OneToOne lazy problem (nullable inverse side can't be proxied without bytecode enhancement); target-entity/generics; orphan/cascade interplay." },
  { slug: 'inheritance-embeddables-composite-keys', name: 'Inheritance, Embeddables & Composite Keys', start: 53,
    dig: "the 3 inheritance strategies with generated SQL & polymorphic-query cost (SINGLE_TABLE: nullable subclass cols, no NOT NULL, discriminator, fastest, no join; JOINED: normalized, joins on read, insert into 2 tables; TABLE_PER_CLASS: UNION-ALL polymorphic queries, no shared sequence, id-gen issues); @MappedSuperclass vs @Inheritance (no polymorphic queries / not an entity); mixing strategies; @Embeddable/@Embedded value objects + @AttributeOverride(s) + nested embeddables + @Embedded uniqueness/null semantics; @ElementCollection (collection table, no entity identity, ordering, eager-by-default gotcha & N+1); composite keys @IdClass vs @EmbeddedId (equals/hashCode REQUIRED on the key class, serializable, when each is idiomatic) + @MapsId derived identity for @OneToOne/@ManyToOne shared PK; discriminator formula; the equals/hashCode burden of composite keys." },
  { slug: 'fetching-lazy-eager-n-plus-one', name: 'Fetching Strategies: Lazy vs Eager & the N+1 Problem', start: 51,
    dig: "the N+1 problem exhaustively with generated SQL + the full FIX MENU & when each applies: JOIN FETCH (JPQL, cartesian product with multiple collections -> MultipleBagFetchException; the List->Set or two-query workaround), @EntityGraph (JPA named + ad-hoc, fetch vs load graph semantics, Spring Data @EntityGraph), @BatchSize / hibernate.default_batch_fetch_size (IN-clause batching, the in-clause padding in HB6), @Fetch(SUBSELECT) (one subselect for all collections), FetchMode JOIN vs SELECT vs SUBSELECT; LazyInitializationException root cause (proxy accessed after context close) & the RIGHT vs WRONG fixes (fetch-in-query/DTO vs EAGER/OSIV/hibernate.enable_lazy_load_no_trans anti-patterns); bytecode enhancement (LazyToOne, @LazyGroup, lazy basic properties); pagination + collection JOIN FETCH in-memory paging (HHH000104) & the fix (paginate ids then fetch); read-only/DTO projections to avoid entity load; why EAGER is a code smell (can't un-eager per query)." },
  { slug: 'cascade-types-orphan-removal', name: 'Cascade Types & Orphan Removal', start: 47,
    dig: "cascade semantics precisely (PERSIST/MERGE/REMOVE/REFRESH/DETACH/ALL + Hibernate SAVE_UPDATE/REPLICATE/LOCK; cascade is transitive per-op, set on the association); orphanRemoval=true vs CascadeType.REMOVE — the exact distinction (orphanRemoval fires on DE-ASSOCIATION / removing from collection / setting to null = 'private ownership'; REMOVE only cascades an explicit parent em.remove) + what happens on collection.clear()/reassign the whole collection with orphanRemoval; the DELETE-then-INSERT behavior; cascading to shared references danger (deleting shared data); aggregate roots (DDD — a child should only be reachable through its parent; cross-ref system-design DDD); merge cascade & detached graphs; cascade + bidirectional sync; flush order of cascaded ops; performance (cascade REMOVE issues N deletes vs a bulk delete); the @OnDelete(action=CASCADE) DB-level FK cascade vs JPA cascade (different layers!)." },
  { slug: 'transactions-dirty-checking-flushing', name: 'Transactions, Dirty Checking & Flushing', start: 46,
    dig: "dirty checking internals (state snapshot at load, field-by-field compare at flush; bytecode-enhanced dirty tracking to avoid the snapshot cost; @DynamicUpdate to update only changed columns vs default all-columns UPDATE; why huge entities are expensive to dirty-check); flush MODES (AUTO before affecting query + at commit, COMMIT only at commit, Hibernate MANUAL/ALWAYS) & auto-flush-before-query semantics (which queries trigger it, HQL vs native); flush ORDER (the ActionQueue order: inserts, updates, collection removals, collection updates/inserts, entity deletes — and the unique-constraint remove+insert-same-key gotcha, orphan removal timing); flush != commit; write-behind batching; the versioned-vs-unversioned optimistic interplay; @Transactional(readOnly=true) -> FlushMode.MANUAL optimization + Hibernate skips dirty checking / flush; transaction propagation effect on the persistence context (cross-ref spring); explicit flush()/clear() in batch loops; the connection-per-transaction & lazy connection acquisition (HB6)." },
  { slug: 'caching-first-second-level', name: 'Caching: First & Second Level Cache', start: 47,
    dig: "L2 concurrency strategies in DEPTH & when each is correct (READ_ONLY: immutable ref data; NONSTRICT_READ_WRITE: stale-tolerant, invalidate-then-timeout, no lock; READ_WRITE: soft locks, uses a timestamp/version, near-transactional; TRANSACTIONAL: JTA/XA cache participates in tx); shared-cache-mode (ALL/ENABLE_SELECTIVE/DISABLE_SELECTIVE) + @Cacheable + @Cache(usage, region); what L2 stores (DEHYDRATED state keyed by id, NOT object graphs; associations need @Cache on the collection which caches only IDs; you must cache the target entities too); the QUERY CACHE (separate region, caches query -> list of IDs, needs entity L2 or re-fetches, invalidated on ANY table update -> often a net negative, UpdateTimestampsCache); natural-id cache; cache providers (Ehcache, Infinispan clustered invalidation vs replication, Caffeine, Redis via Hibernate); staleness/invalidation across a cluster; cache-miss put/get flow; when L2 hurts (write-heavy, low re-read); cross-ref messaging-databases Redis caching patterns (cache-aside/write-through)." },
  { slug: 'querying-jpql-hql-criteria-native', name: 'Querying: JPQL, HQL, Criteria API & Native SQL', start: 49,
    dig: "JPQL depth (constructor expression SELECT new DTO, JOIN vs JOIN FETCH, implicit vs explicit joins, ON clause, subqueries, aggregate/GROUP BY/HAVING, TREAT for polymorphic, CASE, functions, positional ?1 vs named :p binding + injection); HQL extras beyond JPQL (HB6 SQM: additional functions, tuple/array, limit/offset syntax, set operations); Criteria API (CriteriaBuilder/CriteriaQuery/Root, the static metamodel Entity_ classes generated by the annotation processor, type safety, dynamic predicate building, why verbose; Criteria vs QueryDSL vs Specifications); native SQL (createNativeQuery + @SqlResultSetMapping / @NamedNativeQuery, entity vs scalar vs constructor result, addEntity); @NamedQuery precompiled/validated at startup; PAGINATION (setFirstResult/setMaxResults) + the collection-fetch-join in-memory paging warning (HHH000104) + keyset/seek pagination vs offset (cross-ref); BULK update/delete (executeUpdate BYPASSES L1 + dirty checking + cascade + @Version -> must clear() the context & version not bumped); scroll/stream for large results; stored procedures (@NamedStoredProcedureQuery); projections (interface/class/tuple)." },
  { slug: 'concurrency-optimistic-pessimistic-locking', name: 'Concurrency Control: Optimistic vs Pessimistic Locking', start: 49,
    dig: "optimistic @Version internals (WHERE version=? in UPDATE, 0-rows-affected -> OptimisticLockException/StaleObjectStateException; int/long/short/Timestamp/Instant version types & clock-skew risk of timestamps; version bump only on owning-entity change, NOT on collection/association change -> OPTIMISTIC_FORCE_INCREMENT for aggregate consistency); LockModeType matrix (OPTIMISTIC=read-check-at-commit, OPTIMISTIC_FORCE_INCREMENT, PESSIMISTIC_READ=shared, PESSIMISTIC_WRITE=SELECT..FOR UPDATE exclusive, PESSIMISTIC_FORCE_INCREMENT, READ/WRITE aliases, NONE); pessimistic lock timeout (jakarta.persistence.lock.timeout, NOWAIT/SKIP LOCKED dialect support) & deadlock; where locks apply (em.find with lock, em.lock, Query.setLockMode, @Lock in Spring Data); optimistic vs pessimistic decision (contention, retry-ability, financial); the retry-on-OptimisticLockException pattern (@Retryable, re-load); interaction with DB isolation levels (cross-ref messaging-databases isolation/anomalies — repeatable read vs the lost update the @Version prevents); first-commit-wins; detached-entity optimistic checks on merge." },
  { slug: 'value-mapping-converters-enums-types', name: 'Value Mapping: Converters, Enums, Temporal & Custom Types', start: 53,
    dig: "@Enumerated ORDINAL fragility (stores position, reorder/insert breaks data — a classic prod bug) vs STRING (safe, but rename breaks; a custom converter for a stable code is best); AttributeConverter<X,Y> deep (autoApply, converting enums to a DB code, JSON, encrypted fields, money, comma-joined lists; converters CANNOT apply to @Id or @Version pre-HB6, and don't compose with @Enumerated); java.time native support (LocalDate/LocalDateTime/Instant/OffsetDateTime — no @Temporal needed in JPA 2.2+; time-zone handling hibernate.timezone.default_storage NORMALIZE vs NATIVE, storing Instant vs zoned); @Temporal only for legacy Date/Calendar; Hibernate 6 TYPE SYSTEM overhaul (JavaType + JdbcType + @JdbcTypeCode(SqlTypes.JSON/...) replacing the removed legacy UserType/@Type/@TypeDef/BasicType — a big HB5->6 migration point) + writing a custom UserType in HB6; @Lob (CLOB/BLOB, streaming, the LOB-outside-tx problem); @Convert vs @JdbcTypeCode for JSON; BigDecimal precision/scale for money; @Basic(optional/fetch) lazy basic (needs bytecode enhancement)." },
  { slug: 'lifecycle-callbacks-auditing-interceptors', name: 'Entity Lifecycle Callbacks, Auditing & Interceptors', start: 48,
    dig: "JPA callback rules & PITFALLS (@PrePersist/@PostPersist/@PreUpdate/@PostUpdate/@PreRemove/@PostRemove/@PostLoad; @PreUpdate only fires when a dirty UPDATE actually happens — NOT on every flush; no EntityManager operations / no other-entity access inside callbacks = undefined; @EntityListeners + a default listener; ordering); Spring Data JPA auditing (@CreatedDate/@LastModifiedDate/@CreatedBy/@LastModifiedBy + AuditingEntityListener + @EnableJpaAuditing + AuditorAware); Hibernate ENVERS deep (@Audited, REVINFO + _AUD tables, revision types ADD/MOD/DEL, querying historical/AuditReader, ValidityAuditStrategy vs DefaultAuditStrategy performance, tracking deletions/relation changes) vs simple auditing (when each); soft delete (@SoftDelete new in HB6.4, vs the older @SQLDelete + @Where combo, and the @Where/@FilterDef partial-visibility gotchas with unique constraints & caching); Hibernate Interceptor / EventListener SPI (onSave/onFlushDirty/preInsert; multi-tenancy, encryption, cross-cutting); @DynamicUpdate interplay with @PreUpdate." },
  { slug: 'spring-data-jpa-repositories', name: 'Spring Data JPA & Repository Abstraction', start: 47,
    dig: "derived-query grammar depth (keywords: And/Or/Between/LessThan/Like/In/IgnoreCase/OrderBy/Top/First/Distinct/Exists; limits & when to switch to @Query); @Query JPQL vs nativeQuery + SpEL (?#{}, :#{}) + #{#entityName}; @Modifying(clearAutomatically, flushAutomatically) for bulk + @Transactional requirement; Page vs Slice vs List (Page fires a COUNT query — expensive; Slice fetches size+1, no count; countQuery override); projections deep (interface CLOSED vs OPEN @Value/SpEL — open disables the projection optimization & fetches the entity; class DTO constructor; dynamic <T> projections; nested projections + N+1); @EntityGraph on repo methods (fix N+1 declaratively); Specifications (JpaSpecificationExecutor, CriteriaBuilder wrapper, composing and/or) & QueryDSL; how save() decides new-vs-existing (isNew: id null, @Version null, or Persistable/@CreatedDate; the ASSIGNED-id problem -> forces a SELECT on every save, fix with Persistable/version); saveAll batching; the OSIV default in Spring Boot (spring.jpa.open-in-view=true) — what it hides, the warning it logs, why to disable & the consequence (must fetch eagerly/DTO); custom repository fragment impls; @Lock/@QueryHints on methods." },
  { slug: 'configuration-bootstrapping-schema-generation', name: 'Configuration, Bootstrapping & Schema Generation', start: 46,
    dig: "bootstrap deep (JPA persistence.xml persistence-unit + PersistenceUnitInfo vs Spring's LocalContainerEntityManagerFactoryBean + JpaVendorAdapter; Hibernate native Metadata/StandardServiceRegistryBuilder/MetadataSources bootstrap in HB6); dialect auto-detection in HB6+ (no need to set hibernate.dialect); hbm2ddl.auto / jakarta.persistence.schema-generation.database.action (none/validate/update/create/create-drop/drop-and-create) & why update is UNSAFE in prod (never drops/alters safely, can't rename) -> validate + Flyway/Liquibase (cross-ref devops/messaging-databases zero-downtime-migrations); import.sql / jakarta.persistence.sql-load-script-source seeding; JDBC batching config (hibernate.jdbc.batch_size, order_inserts, order_updates, batch_versioned_data) + why IDENTITY breaks it; default_batch_fetch_size; connection handling (HikariCP defaults in Boot, connection acquisition/release modes, lazy acquisition in HB6); SQL logging done right (show_sql/format_sql are crude; org.hibernate.SQL + org.hibernate.orm.jdbc.bind logger, or p6spy/datasource-proxy for bound params & counts to catch N+1); naming strategies (PhysicalNamingStrategy/ImplicitNamingStrategy, Spring Boot's default snake_case); Hibernate properties reference (in_clause_parameter_padding, use_sql_comments)." },
  { slug: 'performance-tuning-pitfalls', name: 'Performance Tuning & Common Pitfalls', start: 49,
    dig: "synthesize the whole domain's senior perf material: N+1 detection (datasource-proxy/p6spy query counts, Hypersistence statistics, assertions in tests) + the full fix menu; JDBC BATCHING done right (batch_size + order_inserts/updates + batch_versioned_data; IDENTITY disables it; the getGeneratedKeys issue); the entity equals/hashCode disaster (generated @Id in a HashSet before persist; use business key or app-assigned UUID/UUIDv7; Hibernate proxy + equals with instanceof vs getClass + Hibernate.getClass()); LazyInitializationException & the OSIV debate (why Boot enables it, what it masks, disabling consequences); over-fetching (EAGER, SELECT huge entity, use projections/DTO/@NamedEntityGraph); MultipleBagFetchException + cartesian products; pagination + collection fetch in-memory (HHH000104); large persistence context in batch loops (flush+clear every N, or StatelessSession); read-only queries (setHint org.hibernate.readOnly, readOnly tx skips dirty check & snapshot -> big memory/CPU win); immutable entities @Immutable; @DynamicUpdate/@DynamicInsert trade-offs; second-level/query cache misuse; DTO projections vs entities for read models; bulk ops (HQL update/delete, StatelessSession, JDBC) & their L1/cache bypass; connection-pool sizing (cross-ref reliability-ops/messaging-databases); a senior anti-pattern checklist." },
  { slug: 'hibernate-6-7-and-jakarta-migration', name: 'Modern Hibernate: 6/7 Changes & Jakarta Migration', start: 49,
    dig: "the JAKARTA migration in depth (javax.persistence.* -> jakarta.persistence.* is a Jakarta EE 9 PACKAGE RENAME not a version bump — trademark/Oracle->Eclipse Foundation; Hibernate 5.6 bridge/dual vs HB6 jakarta-only; Spring Boot 2->3 forces it; the Eclipse Transformer/OpenRewrite migration tooling; what breaks: imports, XML namespaces, persistence.xml version, third-party libs); HIBERNATE 6 major changes (the SQM Semantic Query Model — full query-engine rewrite, read-by-position/JDBC-array reads for perf; the revamped TYPE SYSTEM JavaType/JdbcType/@JdbcTypeCode replacing UserType/@Type/@TypeDef/@TypeDef removal — the biggest migration pain; better JPA 3.1 compliance: GenerationType.UUID, EXTRACT, ceiling/floor/round/etc math functions, LOCAL DATE/TIME; @TenantId; boolean/uuid/enum/array mapping changes; improved bytecode enhancement); HIBERNATE 6.2/6.3/6.4 (@SoftDelete, JSON/embeddable aggregate @Struct, improved stateless session) and HIBERNATE 7 (Jakarta Persistence 3.2 — program-level entity-graph API, JPQL enhancements, the new Jakarta Data @Repository/StatelessSession-based repositories, Java 17 baseline, Hibernate Reactive/Mutiny); migration gotchas (dialect auto, removed legacy Criteria/hbm.xml deprecations, LEGACY vs new bootstrap); Jakarta Data (the new spec) preview. Cross-ref apache-tomcat jakarta migration." },
]

const RULES = (t) => `
GOAL: make this topic's MCQ bank DEEPER and HARDER and fill any concept gaps — WITHOUT
removing or altering existing content. This is Pass 2; a research brief of likely-missing
concepts is provided.

${SCOPE}

STEP 1 — READ both existing files first:
  ${DIR}/${t.slug}/concepts.md
  ${DIR}/${t.slug}/questions.yaml
Understand what's already covered so you do NOT duplicate existing questions or concepts.

STEP 2 — ENRICH concepts.md (edit in place, ADDITIVE):
  - Use the RESEARCH BRIEF (provided separately) plus your own web research to add MISSING
    high-value concepts and deepen thin subtopics (advanced mechanism internals, REAL generated SQL,
    concrete annotation examples, edge cases, HB6/7 specifics, version differences, real gotchas/CVEs
    of behavior). You MAY add new "## " subsections for genuinely missing areas. Keep EXISTING "## "
    headings stable (questions ref them). Keep "## Common Interview Follow-ups" and "## References" last.
  - Vendor-accurate (Jakarta Persistence spec + Hibernate User Guide), current (jakarta.* namespace,
    HB6/7). Where a new diagram clarifies, add a \`\`\`mermaid block (no semicolons in sequenceDiagram
    messages; QUOTE flowchart node labels that contain () or ? e.g. Q{"isNew()?"}).

STEP 3 — APPEND new questions to questions.yaml (do NOT rewrite existing ones):
  - Add AT LEAST 25 new questions (target total 70-90). New ids are "${t.slug}-NNN" starting
    at ${String(t.start).padStart(3, '0')}, incrementing, zero-padded 3-digit, unique.
  - Difficulty of NEW questions: heavily ADVANCED and EXPERT (~50% expert, 40% advanced,
    10% intermediate). Use 'expert' for deep mechanism/version detail, subtle behavior distinctions,
    and senior/staff scenario questions.
  - DEEP-DIVE every subtopic + the newly added concepts. Prioritize SCENARIO/JUDGMENT ("given this
    entity graph + query, which avoids N+1 / which SQL runs?", "why does this throw
    LazyInitializationException and what's the correct fix?", "what does merge return and what's the
    state of the argument?"), CODE-READING (read an entity/annotation and predict generated SQL or
    behavior), and subtle-distinction (cascade REMOVE vs orphanRemoval; EnumType ORDINAL vs STRING;
    @Version increment rules; Page vs Slice). Distractors = real misconceptions. NEVER let a
    wrong-in-practice option (EAGER-everything, @Id in hashCode, hbm2ddl=update in prod, EnumType.ORDINAL
    as 'safe', string-concatenated JPQL, retrying non-idempotent writes) be the correct key.
  - No "all/none of the above". Do NOT duplicate an existing question's meaning.

SCHEMA (must hold for every new question): keys id, difficulty
(beginner|intermediate|advanced|expert), tags[], question, options[3-5], answer (0-BASED,
in range), explanation, ref ("concepts.md#anchor" resolving to a real "## " heading via
GitHub slug rules — lowercase, spaces->-, punctuation stripped, "A & B" -> "#a--b" double
dash). VARY correct-option position (no clustering; roughly even spread). Keep top-level
topic/domain(hibernate-jpa)/topic_slug(${t.slug})/version intact.

Use Edit/Write. Return one line: "${t.slug}: +<newCount> questions (now <total>), concepts enriched: <yes/no>, gaps filled: <short list>".
`

phase('Research')
const results = await pipeline(
  TOPICS,
  (t) => agent(
    `You are researching the topic "${t.name}" for a "Hibernate & JPA" senior-Java interview library, to make sure Pass-2 deepening misses NOTHING.\n\n${SCOPE}\n\n` +
    `FIRST read the current material at ${DIR}/${t.slug}/concepts.md (its "## " headings show what's already covered).\n` +
    `THEN do EXHAUSTIVE web research (the Jakarta Persistence 3.1/3.2 spec, the Hibernate 6/7 User Guide & migration guides, Vlad Mihalcea's blog/High-Performance Java Persistence, Thorben Janssen, Baeldung, "senior/staff Hibernate JPA interview questions 2024/2025", real Stack Overflow gotchas) and produce a GAP BRIEF for this topic:\n` +
    `- MISSING concepts/mechanisms/annotations not in the current concepts.md that a strong 2025 senior Java interview expects (with a one-line why each matters).\n` +
    `- THIN areas needing deeper treatment (mechanism internals / real generated SQL / version-specific behavior HB5-vs-6-vs-7 / edge cases / classic prod bugs).\n` +
    `- Specific hard/senior question angles worth adding (scenario / "predict the SQL" / "why does this throw" / subtle-distinction framings).\n` +
    `Focus hints to make sure you cover: ${t.dig}\n\n` +
    `Return a concise but COMPLETE brief (bullet lists). This brief is handed to the deepening author, so be concrete and specific (name the annotation, the property, the exception, the HB version, the generated SQL shape).`,
    { label: `research:${t.slug}`, phase: 'Research', effort: 'high' }
  ),
  (brief, t) => agent(
    `You are a staff-level Java/Hibernate engineer and senior interviewer deepening the interview-prep material for "${t.name}" (slug: ${t.slug}). Add the hard, real-world mechanism/behavior questions and missing concepts that separate senior candidates from juniors.\n\n` +
    `RESEARCH BRIEF (gaps + angles found for this topic — incorporate these):\n${brief}\n\n${RULES(t)}`,
    { label: `deepen:${t.slug}`, phase: 'Deepen', effort: 'high' }
  ),
  (deepenSummary, t) => agent(
    `Verify the deepened Hibernate & JPA topic "${t.name}" (slug: ${t.slug}). Read ${DIR}/${t.slug}/concepts.md and ${DIR}/${t.slug}/questions.yaml and FIX IN PLACE:\n` +
    `1) FACTUAL/VERSION errors in any concept text, MCQ answer index, or explanation — web-research anything uncertain (default fetch types @ManyToOne/@OneToOne EAGER & @OneToMany/@ManyToMany LAZY; IDENTITY disables JDBC batch inserts; SEQUENCE allocationSize=50; cascade vs orphanRemoval distinction; @Enumerated ORDINAL fragility; @Version 0-rows->OptimisticLockException & no-bump-on-association-change; L1 mandatory/L2 optional; L2 concurrency strategies; JOIN FETCH MultipleBagFetchException; HHH000104 in-memory paging; bulk executeUpdate bypasses L1/cache/version; Page fires COUNT vs Slice; Spring save() new-vs-existing; OSIV default; jakarta.* namespace HB6+/Boot3; HB6 SQM + @JdbcTypeCode replacing @Type; HB6.4 @SoftDelete; HB7 Jakarta Persistence 3.2 + Jakarta Data). A wrong 'answer' index or wrong default/version claim is the WORST defect — fix it.\n` +
    `2) NO WRONG-IN-PRACTICE KEYS: ensure no correct answer endorses a bad practice (EAGER-everything, generated @Id in hashCode, hbm2ddl=update in prod, EnumType.ORDINAL as safe, string-concatenated JPQL, unbounded persistence context in batch loops). A plausible-but-wrong option must be a DISTRACTOR, never the key.\n` +
    `3) SCOPE DRIFT: refocus content that belongs to messaging-databases (raw SQL/ACID/isolation/indexing internals), spring-* (@Transactional propagation/proxy mechanics), or system-design onto the Hibernate/JPA angle, cross-referencing not duplicating.\n` +
    `4) DUPLICATES: if a newly added question is a semantic duplicate of an existing one, rewrite it to cover something new.\n` +
    `5) SCHEMA: valid YAML; unique ids all prefixed '${t.slug}-' 3-digit seq, no collisions; difficulty in {beginner,intermediate,advanced,expert}; 3-5 options; 0-based in-range 'answer'; every 'ref' resolves to a real '## ' heading. Fix violations. Confirm correct-option positions are VARIED — REBALANCE by reordering options if any index exceeds ~35% share. Verify Mermaid parses (quote flowchart labels with ()/?; no semicolons in sequenceDiagram messages).\n` +
    `6) DIFFICULTY/COVERAGE: confirm the bank now has a strong block of advanced+expert questions and reaches 70-90 total; expert-tagged ones must be genuinely hard. Re-tag/ADD if short.\n\n` +
    `Return one line: "${t.slug}: <total> questions (<nBeg>/<nInt>/<nAdv>/<nExp>), <fixed|clean>, notes: ...".`,
    { label: `verify:${t.slug}`, phase: 'Verify', effort: 'high' }
  )
)

return results.filter(Boolean)
