topic: "Specialized Architectures (Blackboard, Primary-Replica, Broker)"
domain: system-design
topic_slug: arch-specialized
version: 1
questions:
  - id: arch-specialized-001
    difficulty: beginner
    tags: [altitude, architecture-vs-design-pattern, fundamentals]
    question: |
      In this reference, how do "architectural styles" (arch-*) differ in altitude from
      GoF "design patterns" (dp-*)?
    options:
      - "Architectural styles describe the whole-system structure/deployment; design patterns describe object collaboration inside one component"
      - "They are the same thing at different scales of the same class diagram"
      - "Design patterns describe deployment topology; architectural styles describe class relationships"
      - "Architectural styles only apply to databases; design patterns only apply to UIs"
    answer: 0
    explanation: |
      arch-* is the system altitude — top-level components, how they are distributed, how
      they communicate — which dominates the -ilities. dp-* (GoF) is the object altitude:
      class/object collaboration within a single component.
    ref: "concepts.md#architectural-styles-vs-object-level-design-patterns"

  - id: arch-specialized-002
    difficulty: beginner
    tags: [blackboard, problem-fit, ai]
    question: |
      What core problem characteristic makes the Blackboard style appropriate?
    options:
      - "The workload is embarrassingly parallel and fully deterministic"
      - "Reads vastly outnumber writes on a single database"
      - "There is no known deterministic algorithm; a solution must be assembled opportunistically from many specialists"
      - "Clients must call servers without knowing their location"
    answer: 2
    explanation: |
      Blackboard (origin: Hearsay-II speech recognition) fits ill-defined domains where no
      step-by-step algorithm exists and partial contributions from diverse Knowledge Sources
      must be combined opportunistically.
    ref: "concepts.md#blackboard"

  - id: arch-specialized-003
    difficulty: intermediate
    tags: [blackboard, components, control]
    question: |
      Which component distinguishes a Blackboard architecture from a passive shared data store?
    options:
      - "The marshalling proxy"
      - "The active control/scheduler component that opportunistically selects which Knowledge Source runs next"
      - "The read replica"
      - "The client-side stub"
    answer: 1
    explanation: |
      A Blackboard adds an active control component (scheduler) that inspects state and
      heuristically decides which Knowledge Source fires next. Without it you just have a
      shared repository.
    ref: "concepts.md#blackboard"

  - id: arch-specialized-004
    difficulty: intermediate
    tags: [blackboard, communication, knowledge-sources]
    question: |
      How do Knowledge Sources in a Blackboard system communicate with each other?
    options:
      - "By direct RPC calls between Knowledge Sources"
      - "Through a central message broker with request/reply"
      - "They do not share any state; each produces a final answer independently"
      - "Only indirectly, by reading from and writing to the shared blackboard"
    answer: 3
    explanation: |
      Knowledge Sources never call each other. They observe the blackboard, fire when their
      trigger condition matches, and write improved hypotheses back — communication is only
      through the shared blackboard.
    ref: "concepts.md#blackboard"

  - id: arch-specialized-005
    difficulty: advanced
    tags: [blackboard, trade-offs, ilities]
    question: |
      Which -ility does Blackboard optimize, and which does it most clearly sacrifice?
    options:
      - "Optimizes performance; sacrifices extensibility"
      - "Optimizes extensibility/flexibility; sacrifices testability and predictability"
      - "Optimizes strong consistency; sacrifices availability"
      - "Optimizes deployability; sacrifices interoperability"
    answer: 1
    explanation: |
      Adding/removing specialist Knowledge Sources is easy (extensibility), but the heuristic,
      non-deterministic control loop is hard to test, debug, and prove convergent —
      predictability and testability suffer.
    ref: "concepts.md#blackboard"

  - id: arch-specialized-006
    difficulty: advanced
    tags: [blackboard, when-to-avoid]
    question: |
      When is Blackboard a poor architectural choice?
    options:
      - "A problem with a known deterministic algorithm and hard real-time deadlines requiring reproducibility"
      - "Sensor-fusion systems combining noisy inputs"
      - "Speech and handwriting recognition"
      - "Symbolic AI planning with many heuristic specialists"
    answer: 0
    explanation: |
      Blackboard's non-deterministic control loop and lack of convergence guarantees make it
      wrong for deterministic problems, hard real-time deadlines, or anything needing
      reproducibility/auditability.
    ref: "concepts.md#blackboard"

  - id: arch-specialized-007
    difficulty: beginner
    tags: [primary-replica, problem-fit, replication]
    question: |
      What problem does the Primary-Replica style primarily address?
    options:
      - "Scaling write throughput across many independent write authorities"
      - "Running behavior defined as a DSL without recompiling"
      - "Combining opportunistic partial results into a solution"
      - "Scaling read load and adding redundancy while keeping one authoritative write source"
    answer: 3
    explanation: |
      Primary-Replica scales reads (fan-out to replicas) and adds failover redundancy while
      one primary remains the single authoritative source of writes — avoiding write
      conflicts.
    ref: "concepts.md#primary-replica"

  - id: arch-specialized-008
    difficulty: intermediate
    tags: [primary-replica, consistency, replication-lag]
    question: |
      A read served by an asynchronous replica returns data slightly older than the latest
      write to the primary. What is this called and why does it happen?
    options:
      - "Split-brain, because two primaries accepted writes"
      - "Cold start, because the replica just booted"
      - "Replication lag, because the replica has not yet applied the primary's latest changes"
      - "Straggler latency, because a worker was slow"
    answer: 2
    explanation: |
      Under asynchronous replication the primary does not wait for replicas to apply changes,
      so replicas trail the primary — replication lag — producing stale/eventually-consistent
      reads.
    ref: "concepts.md#primary-replica"

  - id: arch-specialized-009
    difficulty: intermediate
    tags: [primary-replica, terminology, inclusive-language]
    question: |
      "Leader-follower" and the deprecated "master-slave" both refer to which style?
    options:
      - "Broker"
      - "Peer-to-Peer"
      - "Blackboard"
      - "Primary-Replica"
    answer: 3
    explanation: |
      Primary-Replica (aka leader-follower) is the style historically and now-deprecatedly
      called master-slave. Use Primary-Replica.
    ref: "concepts.md#primary-replica"

  - id: arch-specialized-010
    difficulty: advanced
    tags: [primary-replica, sync-async, durability]
    question: |
      Choosing synchronous over asynchronous replication for Primary-Replica primarily trades
      what for what?
    options:
      - "Lower read latency for weaker consistency"
      - "Higher write latency for stronger durability / fresher replicas"
      - "More write authorities for conflict resolution"
      - "Lower operational cost for higher memory usage"
    answer: 1
    explanation: |
      Synchronous replication makes the primary wait for replica acknowledgement — stronger
      durability and up-to-date replicas at the cost of higher write latency. Async is faster
      but risks losing recent writes on failover.
    ref: "concepts.md#primary-replica"

  - id: arch-specialized-011
    difficulty: advanced
    tags: [primary-replica, write-scaling, sharding]
    question: |
      Primary-Replica does not scale writes. What is the standard way to scale write
      throughput while keeping single-writer semantics per partition?
    options:
      - "Shard the dataset across multiple primaries, each optionally with its own replicas"
      - "Add more read replicas"
      - "Switch every read to the primary"
      - "Convert all replicas into blackboards"
    answer: 0
    explanation: |
      Replication copies the same data for read scale/HA; to scale writes you shard —
      partition the dataset across multiple primaries, often each with its own replica set.
    ref: "concepts.md#primary-replica"

  - id: arch-specialized-012
    difficulty: advanced
    tags: [primary-replica, failover, split-brain]
    question: |
      During Primary-Replica failover, why must the old primary be "fenced" before promoting a
      replica?
    options:
      - "To reduce replication lag on reads"
      - "To warm the cache on the new primary"
      - "To prevent split-brain, where two nodes both accept writes and diverge"
      - "To rebalance the DHT key ranges"
    answer: 2
    explanation: |
      If the old primary is not fenced and comes back thinking it is still authoritative, both
      it and the promoted replica accept writes — split-brain — causing divergent, conflicting
      data.
    ref: "concepts.md#primary-replica"

  - id: arch-specialized-013
    difficulty: intermediate
    tags: [primary-replica, multi-primary, differs-from]
    question: |
      How does Multi-Primary (leaderless) differ from Primary-Replica?
    options:
      - "Multi-Primary has exactly one write authority and no conflicts"
      - "Multi-Primary cannot serve reads at all"
      - "Multi-Primary accepts writes on many nodes for write availability but must resolve write conflicts"
      - "They are identical; the names are interchangeable"
    answer: 2
    explanation: |
      Primary-Replica has one write authority (no conflicts). Multi-Primary/leaderless accepts
      writes on many nodes for availability but must reconcile conflicts (LWW, CRDTs, quorums).
    ref: "concepts.md#primary-replica"

  - id: arch-specialized-014
    difficulty: intermediate
    tags: [primary-replica, deep-dive, cross-reference]
    question: |
      For replication internals — sync/async/semi-sync, quorum reads/writes, sharding, and
      failover mechanics — which deep-dive topic does arch-specialized point to?
    options:
      - "message-queues-and-async"
      - "event-driven-cqrs-saga-cdc"
      - "caching-and-cdn"
      - "databases-sql-nosql-sharding-replication"
    answer: 3
    explanation: |
      The style overview points to databases-sql-nosql-sharding-replication for the
      replication/sharding internals rather than duplicating them.
    ref: "concepts.md#primary-replica"

  - id: arch-specialized-015
    difficulty: beginner
    tags: [broker, problem-fit, location-transparency]
    question: |
      What problem does the Broker style solve?
    options:
      - "Combining partial hypotheses opportunistically"
      - "Letting distributed components invoke each other without hard-coding location, transport, or platform"
      - "Splitting a deterministic workload across identical workers"
      - "Serving stale reads from replicas"
    answer: 1
    explanation: |
      Broker provides location transparency and decoupling: clients call servers through a
      mediating broker without knowing their address, transport, or platform, so services can
      move/scale at runtime.
    ref: "concepts.md#broker"

  - id: arch-specialized-016
    difficulty: intermediate
    tags: [broker, components, proxies]
    question: |
      In the classic (POSA) Broker style, what is the role of the client-side proxy (stub) and
      server-side proxy (skeleton)?
    options:
      - "They marshal/unmarshal requests and hide the remote call so neither side knows the other's physical location"
      - "They elect a new primary during failover"
      - "They store the shared blackboard hypotheses"
      - "They partition the workload into shards"
    answer: 0
    explanation: |
      The client proxy marshals the local call into a transport-neutral message; the broker
      routes it; the server proxy unmarshals and invokes the server. Proxies provide the
      location transparency.
    ref: "concepts.md#broker"

  - id: arch-specialized-017
    difficulty: intermediate
    tags: [broker, lineage, middleware]
    question: |
      Which technologies are in the lineage of the RPC-broker style?
    options:
      - "CORBA, Java RMI, DCOM, and modern gRPC/service registries"
      - "HTML, CSS, and JavaScript"
      - "MapReduce and Hadoop"
      - "TCP, UDP, and IP"
    answer: 0
    explanation: |
      The request/response location-transparent Broker lineage runs through CORBA, RMI, DCOM
      to modern gRPC and service-registry-driven RPC.
    ref: "concepts.md#broker"

  - id: arch-specialized-018
    difficulty: advanced
    tags: [broker, trade-offs, spof]
    question: |
      What is the primary structural risk of a Broker architecture?
    options:
      - "It cannot support heterogeneous platforms"
      - "It requires all components to run in one process"
      - "It makes services impossible to relocate"
      - "The broker is a central bottleneck and single point of failure that adds latency"
    answer: 3
    explanation: |
      Centralizing mediation means the broker can bottleneck throughput and is a SPOF (usually
      mitigated by clustering); it also adds latency and operational complexity.
    ref: "concepts.md#broker"

  - id: arch-specialized-019
    difficulty: advanced
    tags: [broker, pub-sub, differs-from]
    question: |
      How does the POSA request/response Broker differ from a Publish-Subscribe event bus?
    options:
      - "Broker is fire-and-forget with no reply; pub-sub always has a reply"
      - "They are identical mechanisms"
      - "Broker is request/response 'many-to-one-to-many' routing with a reply path; pub-sub is fire-and-forget 'many-to-many' event fan-out with no reply"
      - "Pub-sub requires proxies/stubs; Broker never does"
    answer: 2
    explanation: |
      The RPC Broker routes a request to a server and routes the reply/exception back.
      Pub-sub fans events out to many subscribers with no reply expectation.
    ref: "concepts.md#broker"

  - id: arch-specialized-020
    difficulty: advanced
    tags: [broker, api-gateway, service-mesh, differs-from]
    question: |
      Which statement correctly separates Broker, API Gateway, and Service Mesh?
    options:
      - "All three are the same central hub"
      - "Broker mediates internal service-to-service calls; API Gateway routes north-south edge traffic; Service Mesh pushes cross-cutting concerns into per-instance sidecars (no central hub)"
      - "API Gateway mediates internal RPC; Broker only runs at the edge; Service Mesh replicates data"
      - "Service Mesh is a central broker; Broker is a sidecar"
    answer: 1
    explanation: |
      Broker = internal location-transparent mediation; API Gateway = edge/north-south client
      routing; Service Mesh = the same concerns distributed into sidecars with no central hub.
    ref: "concepts.md#broker"

  - id: arch-specialized-021
    difficulty: intermediate
    tags: [broker, message-broker, deep-dive]
    question: |
      The message-broker form (RabbitMQ, MQTT, Kafka — queues and pub-sub) is which deep-dive
      topic in this library?
    options:
      - "databases-sql-nosql-sharding-replication"
      - "message-queues-and-async"
      - "aws-serverless-lambda-stepfunctions"
      - "dp-enterprise-application"
    answer: 1
    explanation: |
      arch-specialized treats the RPC-broker form and points to message-queues-and-async for
      the async message-broker/queue/pub-sub form.
    ref: "concepts.md#broker"

  - id: arch-specialized-022
    difficulty: expert
    tags: [broker, gof, altitude]
    question: |
      A candidate answers a "Broker architecture" question by describing the GoF Mediator
      pattern's class collaboration. Why is that an altitude mistake?
    options:
      - "Mediator is a database replication strategy, not a pattern"
      - "There is no difference; the answer is fully correct"
      - "The Broker architectural style is a whole-system distribution/communication style; the GoF Mediator/Proxy are object-level collaborations inside one process"
      - "Broker is object-level and Mediator is system-level"
    answer: 2
    explanation: |
      Broker (POSA) is a system-altitude style for distributed, location-transparent
      communication. GoF Mediator/Proxy operate at the object altitude within a single
      process — a different question.
    ref: "concepts.md#broker"

  - id: arch-specialized-023
    difficulty: beginner
    tags: [peer-to-peer, problem-fit, spof]
    question: |
      What is the defining goal of a Peer-to-Peer architecture?
    options:
      - "Eliminate the central-server bottleneck/SPOF by making every node both client and server"
      - "Keep one authoritative write node with read replicas"
      - "Interpret a DSL at runtime"
      - "Route all calls through a central broker"
    answer: 0
    explanation: |
      P2P removes the privileged central node — every symmetric peer acts as both client and
      server, so the system scales and self-heals as nodes join/leave.
    ref: "concepts.md#peer-to-peer"

  - id: arch-specialized-024
    difficulty: intermediate
    tags: [peer-to-peer, discovery, dht-gossip]
    question: |
      How do peers in a Peer-to-Peer system typically locate resources and track membership?
    options:
      - "A central registry owned by the primary node"
      - "The client-side stub queries the broker"
      - "The scheduler assigns lookups to Knowledge Sources"
      - "Distributed Hash Tables / consistent hashing for lookup and gossip for membership"
    answer: 3
    explanation: |
      Structured P2P uses DHTs/consistent hashing for resource lookup and gossip protocols for
      membership/state dissemination; consensus is used where global agreement is needed.
    ref: "concepts.md#peer-to-peer"

  - id: arch-specialized-025
    difficulty: advanced
    tags: [peer-to-peer, trade-offs, ilities]
    question: |
      Which pair correctly states what P2P optimizes and what it sacrifices?
    options:
      - "Optimizes strong consistency; sacrifices availability"
      - "Optimizes testability; sacrifices extensibility"
      - "Optimizes write latency; sacrifices read latency"
      - "Optimizes scalability + availability (no SPOF); sacrifices consistency + manageability"
    answer: 3
    explanation: |
      P2P gains elastic scale and no-SPOF availability, but strong consistency, discovery,
      trust/security, and operability become hard.
    ref: "concepts.md#peer-to-peer"

  - id: arch-specialized-026
    difficulty: intermediate
    tags: [peer-to-peer, differs-from, primary-replica]
    question: |
      What most clearly distinguishes Peer-to-Peer from Primary-Replica and Client-Server?
    options:
      - "P2P has no privileged authoritative node — every peer is equal"
      - "P2P has a privileged authoritative node; the others do not"
      - "P2P cannot serve reads"
      - "P2P always uses a central coordinator to hand out work"
    answer: 0
    explanation: |
      Primary-Replica and Client-Server have a privileged authoritative node. In P2P every
      peer is functionally identical with no privileged node.
    ref: "concepts.md#peer-to-peer"

  - id: arch-specialized-027
    difficulty: advanced
    tags: [peer-to-peer, deep-dive, cross-reference]
    question: |
      For consensus internals and for DHT/consistent-hashing mechanics, which topics does
      arch-specialized cross-reference?
    options:
      - "caching-and-cdn and aws-caching-elasticache-dax"
      - "dp-behavioral and dp-enterprise-application"
      - "consensus-clocks-and-time and rate-limiting-and-consistent-hashing"
      - "event-driven-cqrs-saga-cdc and message-queues-and-async"
    answer: 2
    explanation: |
      Consensus lives in consensus-clocks-and-time; DHT/consistent-hashing mechanics live in
      rate-limiting-and-consistent-hashing.
    ref: "concepts.md#peer-to-peer"

  - id: arch-specialized-028
    difficulty: beginner
    tags: [master-worker, problem-fit, parallelism]
    question: |
      What workload shape is Master-Worker designed for?
    options:
      - "A non-algorithmic perception problem with no deterministic solution"
      - "A large, deterministic workload that can be partitioned and computed in parallel with fault isolation"
      - "A read-heavy database needing failover"
      - "Location-transparent RPC between heterogeneous platforms"
    answer: 1
    explanation: |
      Master-Worker splits a large deterministic workload into independent sub-tasks run by
      identical workers, then aggregates — MapReduce, render farms, parallel search.
    ref: "concepts.md#master-worker"

  - id: arch-specialized-029
    difficulty: intermediate
    tags: [master-worker, coordinator, fault-isolation]
    question: |
      A single sub-task in a Master-Worker job fails. What is the natural recovery?
    options:
      - "Restart the entire job from scratch"
      - "Promote a replica to primary"
      - "Re-dispatch just that failed sub-task to another worker (fault isolation)"
      - "Fire a new Knowledge Source"
    answer: 2
    explanation: |
      Because sub-tasks are independent and workers are stateless, the coordinator re-dispatches
      only the failed shard — fault isolation without redoing the whole job.
    ref: "concepts.md#master-worker"

  - id: arch-specialized-030
    difficulty: advanced
    tags: [master-worker, stragglers, tail-latency]
    question: |
      What most commonly dominates the tail latency of a Master-Worker job?
    options:
      - "Cold starts of the interpreter engine"
      - "Stragglers — the slowest worker/sub-task holding up aggregation"
      - "Replication lag on read replicas"
      - "Broker marshalling overhead"
    answer: 1
    explanation: |
      The job cannot finish until the slowest sub-task returns, so stragglers dominate tail
      latency (often mitigated with speculative/backup task execution).
    ref: "concepts.md#master-worker"

  - id: arch-specialized-031
    difficulty: advanced
    tags: [master-worker, correctness, constraints]
    question: |
      Master-Worker's correctness depends on which property of its sub-tasks?
    options:
      - "They must communicate frequently with each other"
      - "They must all run on the same physical host"
      - "They must share mutable global state"
      - "They must be independent and deterministic"
    answer: 3
    explanation: |
      Workers do not talk to each other; correctness and safe retries require independent,
      deterministic sub-tasks. Tightly-coupled computations do not fit.
    ref: "concepts.md#master-worker"

  - id: arch-specialized-032
    difficulty: intermediate
    tags: [master-worker, primary-replica, differs-from]
    question: |
      How does Master-Worker differ from Primary-Replica?
    options:
      - "Master-Worker partitions compute; Primary-Replica replicates the same data for reads/HA"
      - "Master-Worker replicates data for reads; Primary-Replica partitions compute"
      - "They are the same style with different names"
      - "Master-Worker has no coordinator; Primary-Replica has many primaries"
    answer: 0
    explanation: |
      Master-Worker is about parallel compute partitioning; Primary-Replica is about copying
      data for read scaling and availability — different problems.
    ref: "concepts.md#master-worker"

  - id: arch-specialized-033
    difficulty: intermediate
    tags: [master-worker, deep-dive, cross-reference]
    question: |
      For concrete coordinator/worker implementations (queues, work stealing, dedup), which
      deep-dive topics does arch-specialized point to?
    options:
      - "design-job-scheduler-task-queue and design-web-crawler-data-processing"
      - "caching-and-cdn and aws-caching-elasticache-dax"
      - "dp-behavioral and dp-structural"
      - "consensus-clocks-and-time only"
    answer: 0
    explanation: |
      The overview points to design-job-scheduler-task-queue and
      design-web-crawler-data-processing for concrete implementations.
    ref: "concepts.md#master-worker"

  - id: arch-specialized-034
    difficulty: beginner
    tags: [interpreter, problem-fit, dsl]
    question: |
      What problem does the Interpreter/Virtual-Machine architectural style solve?
    options:
      - "Serving read load from replicas"
      - "Defining and changing system behavior without recompiling/redeploying the host, by shipping an engine that interprets a language/DSL/bytecode"
      - "Removing the central broker bottleneck"
      - "Assembling opportunistic partial results"
    answer: 1
    explanation: |
      The style encodes logic as a language/DSL/bytecode and ships an engine that interprets
      it at runtime — rules engines, scripting, workflow engines, language VMs — so behavior
      changes without recompiling the host.
    ref: "concepts.md#interpreter-virtual-machine"

  - id: arch-specialized-035
    difficulty: intermediate
    tags: [interpreter, components, pipeline]
    question: |
      What is the typical component flow in an Interpreter/VM architecture?
    options:
      - "Client proxy → broker → server proxy → server"
      - "Primary → replication stream → replicas"
      - "Source/DSL → parser → AST/bytecode → interpreter/VM engine → runtime effects"
      - "Coordinator → split → workers → aggregate"
    answer: 2
    explanation: |
      Source program text is parsed to an AST/bytecode, which the interpreter/VM engine walks
      and executes against the host runtime to produce effects.
    ref: "concepts.md#interpreter-virtual-machine"

  - id: arch-specialized-036
    difficulty: advanced
    tags: [interpreter, trade-offs, ilities]
    question: |
      Which -ilities does the Interpreter/VM style optimize and sacrifice?
    options:
      - "Optimizes performance; sacrifices flexibility"
      - "Optimizes read scalability; sacrifices write scalability"
      - "Optimizes consistency; sacrifices availability"
      - "Optimizes flexibility/portability; sacrifices performance and toolability"
    answer: 3
    explanation: |
      Behavior-as-data gives runtime flexibility and cross-platform portability, at the cost of
      interpretation overhead (performance) and harder static analysis/debugging (toolability).
    ref: "concepts.md#interpreter-virtual-machine"

  - id: arch-specialized-037
    difficulty: advanced
    tags: [interpreter, microkernel, differs-from]
    question: |
      How does Interpreter/VM differ from a Microkernel/Plugin architecture?
    options:
      - "Plugins are interpreted DSLs; interpreters load compiled modules"
      - "They are identical"
      - "Interpreter has no engine; plugins have no core"
      - "Plugins are compiled modules loaded by a core; an interpreter executes a language defined as data"
    answer: 3
    explanation: |
      Microkernel/Plugin extends behavior by loading compiled modules into a core. The
      Interpreter style makes the system language-driven, executing a program expressed as data.
    ref: "concepts.md#interpreter-virtual-machine"

  - id: arch-specialized-038
    difficulty: expert
    tags: [interpreter, gof, altitude]
    question: |
      Why is "Interpreter" a same-name altitude trap between arch-* and dp-*?
    options:
      - "It is only a database term"
      - "It exists only in dp-* and never as an architecture"
      - "It is both a system-level style (ship a language + engine) and a GoF behavioral pattern (a class per grammar rule inside one component)"
      - "It is a deployment topology with no object-level meaning"
    answer: 2
    explanation: |
      As an architectural style, Interpreter makes the whole system language-driven. As a GoF
      pattern it is a class-per-grammar-rule object collaboration inside one component — same
      name, different altitude.
    ref: "concepts.md#interpreter-virtual-machine"

  - id: arch-specialized-039
    difficulty: intermediate
    tags: [overlap, pipe-and-filter, deep-dive]
    question: |
      Which style processes a data stream through independent, recomposable transformation
      stages, and which deep-dive topic covers it?
    options:
      - "Pipe-and-Filter; dp-distributed-cloud"
      - "Blackboard; consensus-clocks-and-time"
      - "Broker; message-queues-and-async"
      - "Space-Based; caching-and-cdn"
    answer: 0
    explanation: |
      Pipe-and-Filter chains independent transformation stages (composability/reuse vs.
      end-to-end latency and no shared global state); it is cross-referenced to
      dp-distributed-cloud.
    ref: "concepts.md#overlap-styles--style-overview-with-deep-dive-pointers"

  - id: arch-specialized-040
    difficulty: intermediate
    tags: [overlap, event-driven, deep-dive]
    question: |
      Event-Driven / Pub-Sub / CQRS / Event Sourcing / Saga are covered in depth by which
      topic?
    options:
      - "microservices-monolith-api-design"
      - "event-driven-cqrs-saga-cdc"
      - "aws-serverless-lambda-stepfunctions"
      - "dp-enterprise-application"
    answer: 1
    explanation: |
      arch-specialized gives only the one-line framing (loose coupling + scalability vs.
      eventual consistency) and points to event-driven-cqrs-saga-cdc for the deep dive.
    ref: "concepts.md#overlap-styles--style-overview-with-deep-dive-pointers"

  - id: arch-specialized-041
    difficulty: advanced
    tags: [overlap, space-based, blackboard, differs-from]
    question: |
      Space-Based (in-memory data grid) is sometimes confused with Blackboard because both use
      a shared space. What is the real difference?
    options:
      - "Space-Based adds an opportunistic reasoning scheduler; Blackboard is a passive cache"
      - "Space-Based removes the database bottleneck under load using a shared in-memory grid; Blackboard adds an active control component doing opportunistic reasoning over hypotheses"
      - "They are the same style"
      - "Blackboard scales writes; Space-Based only scales reads"
    answer: 1
    explanation: |
      Space-Based is about elastic throughput by keeping data in an in-memory grid (deep dive:
      caching-and-cdn / aws-caching-elasticache-dax). Blackboard's shared space is driven by a
      reasoning scheduler solving a non-algorithmic problem.
    ref: "concepts.md#overlap-styles--style-overview-with-deep-dive-pointers"

  - id: arch-specialized-042
    difficulty: intermediate
    tags: [overlap, serverless, deep-dive]
    question: |
      Which overlap style is characterized by "zero-ops autoscale + pay-per-use vs. cold
      starts, vendor lock-in, statelessness," and where is its deep dive?
    options:
      - "Space-Based; caching-and-cdn"
      - "Microservices; microservices-monolith-api-design"
      - "Pipe-and-Filter; dp-distributed-cloud"
      - "Serverless / FaaS; aws-serverless-lambda-stepfunctions"
    answer: 3
    explanation: |
      Serverless/FaaS runs event-triggered functions with no capacity management; the trade-off
      is cold starts/lock-in/statelessness. Deep dive: aws-serverless-lambda-stepfunctions.
    ref: "concepts.md#overlap-styles--style-overview-with-deep-dive-pointers"

  - id: arch-specialized-043
    difficulty: intermediate
    tags: [overlap, mvc, presentation, deep-dive]
    question: |
      MVC / MVP / MVVM, Repository, and DTO — presentation/enterprise patterns — are deep-dived
      in which topic?
    options:
      - "dp-enterprise-application"
      - "event-driven-cqrs-saga-cdc"
      - "databases-sql-nosql-sharding-replication"
      - "message-queues-and-async"
    answer: 0
    explanation: |
      The presentation family (MVC/MVP/MVVM) plus Repository/DTO is catalogued as enterprise
      application patterns: dp-enterprise-application.
    ref: "concepts.md#overlap-styles--style-overview-with-deep-dive-pointers"

  - id: arch-specialized-044
    difficulty: advanced
    tags: [overlap, sidecar, service-mesh, broker]
    question: |
      Sidecar / Ambassador / Service Mesh are "Broker-adjacent." What do they do and where is
      the deep dive?
    options:
      - "They replicate data for reads; databases-sql-nosql-sharding-replication"
      - "They interpret DSLs; dp-behavioral"
      - "They offload cross-cutting concerns (routing, mTLS, retries) from app code into per-instance infra; dp-distributed-cloud"
      - "They partition compute; design-job-scheduler-task-queue"
    answer: 2
    explanation: |
      Sidecar/Ambassador/Service Mesh push cross-cutting concerns out of app code into
      per-instance infra (uniform, language-agnostic vs. per-instance overhead). Deep dive:
      dp-distributed-cloud.
    ref: "concepts.md#overlap-styles--style-overview-with-deep-dive-pointers"

  - id: arch-specialized-045
    difficulty: expert
    tags: [scenario, blackboard, master-worker, differs-from]
    question: |
      You must interpret noisy multi-sensor input where no single algorithm produces the answer
      and specialists must iteratively refine shared hypotheses. Which style fits, and why not
      Master-Worker?
    options:
      - "Master-Worker, because sensor fusion is embarrassingly parallel"
      - "Primary-Replica, because sensors produce read load"
      - "Blackboard, because the problem is non-deterministic and specialists contribute opportunistically — unlike Master-Worker's deterministic, independent partition"
      - "Broker, because sensors need location transparency"
    answer: 2
    explanation: |
      Iterative, opportunistic refinement of shared hypotheses with no deterministic algorithm
      is Blackboard. Master-Worker requires independent, deterministic sub-tasks, which this
      problem lacks.
    ref: "concepts.md#blackboard"

  - id: arch-specialized-046
    difficulty: expert
    tags: [scenario, primary-replica, peer-to-peer, ilities]
    question: |
      A globally distributed system must accept low-latency writes in every region and tolerate
      regional partitions, accepting conflict resolution. Which style best fits, and why not
      Primary-Replica?
    options:
      - "Multi-Primary / leaderless Peer-to-Peer, because it accepts writes on many nodes for availability; Primary-Replica has a single write authority and cannot offer local low-latency writes everywhere"
      - "Primary-Replica, because it scales writes globally"
      - "Broker, because it hides node locations"
      - "Blackboard, because regions are specialists"
    answer: 0
    explanation: |
      Local writes in every region + partition tolerance points to multi-primary/leaderless
      (accepting conflict resolution). Primary-Replica funnels all writes through one authority,
      so it cannot provide low-latency local writes everywhere.
    ref: "concepts.md#peer-to-peer"

  - id: arch-specialized-047
    difficulty: expert
    tags: [scenario, interpreter, ilities]
    question: |
      Business analysts must change pricing rules daily without a code deploy, and rule
      evaluation is not on a hot latency path. Which style fits and which -ility drives the
      choice?
    options:
      - "Broker; interoperability"
      - "Primary-Replica; read scalability"
      - "Master-Worker; throughput"
      - "Interpreter/VM (rules engine); flexibility/evolvability drives it, and acceptable interpretation overhead is tolerated because it is not a hot path"
    answer: 3
    explanation: |
      Changing behavior without redeploying, by non-developers, is exactly the Interpreter/VM
      (rules engine) sweet spot; flexibility/evolvability is the driving -ility, and the
      performance cost is acceptable off the hot path.
    ref: "concepts.md#interpreter-virtual-machine"

  - id: arch-specialized-048
    difficulty: advanced
    tags: [scenario, broker, message-queues, differs-from]
    question: |
      A team needs heterogeneous services (different languages) to call each other by logical
      name with runtime relocation and request/reply semantics. Which style, and how does it
      differ from just using a message queue?
    options:
      - "Message queue; it provides synchronous request/reply by default"
      - "RPC-Broker; it provides location-transparent request/response with a reply path, whereas a message queue is fire-and-forget async pub-sub (deep dive: message-queues-and-async)"
      - "Blackboard; services are Knowledge Sources"
      - "Primary-Replica; services are replicas"
    answer: 1
    explanation: |
      Logical-name calls with runtime relocation and request/reply is the RPC-Broker form. A
      message queue is the async, fire-and-forget message-broker form (message-queues-and-async)
      — no built-in synchronous reply path.
    ref: "concepts.md#broker"
