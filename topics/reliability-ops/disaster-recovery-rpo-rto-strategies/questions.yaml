topic: "Disaster Recovery: RPO, RTO & Strategies"
domain: reliability-ops
topic_slug: disaster-recovery-rpo-rto-strategies
version: 1
questions:
  - id: disaster-recovery-rpo-rto-strategies-001
    difficulty: beginner
    tags: [rpo, definitions]
    question: |
      What does the Recovery Point Objective (RPO) measure?
    options:
      - "The maximum acceptable amount of data loss, expressed as a window of time"
      - "The maximum acceptable downtime before the system must be serving again"
      - "The average time between failures of a component"
      - "The percentage of requests that must succeed each month"
    answer: 0
    explanation: |
      RPO is the maximum acceptable data loss, measured in time. RPO = 1 hour means
      you can lose up to the last hour of writes, so it drives how frequently you
      back up or replicate data. Downtime is RTO, not RPO.
    ref: "concepts.md#rpo--recovery-point-objective-acceptable-data-loss"

  - id: disaster-recovery-rpo-rto-strategies-002
    difficulty: beginner
    tags: [rto, definitions]
    question: |
      What does the Recovery Time Objective (RTO) measure?
    options:
      - "How much committed data you can afford to lose in a failure"
      - "How often backups or snapshots are captured to storage"
      - "How long the system may be down before service must be restored"
      - "How many synchronous replicas a database cluster keeps"
    answer: 2
    explanation: |
      RTO is the maximum acceptable downtime — the clock from disaster to
      service-restored. It drives the failover architecture (how much standby
      infrastructure you keep ready). Data loss is RPO.
    ref: "concepts.md#rto--recovery-time-objective-acceptable-downtime"

  - id: disaster-recovery-rpo-rto-strategies-003
    difficulty: intermediate
    tags: [rpo, drivers]
    question: |
      RPO primarily drives which design decision?
    options:
      - "The number of application server instances kept warm for failover"
      - "The backup/replication frequency for your data"
      - "The DNS TTL used to redirect clients during cutover"
      - "The choice of programming language and framework for the service"
    answer: 1
    explanation: |
      Because RPO caps how much data you can lose, it dictates how often you must
      capture data: nightly backups → ~24h RPO, hourly → ~1h, continuous
      replication/PITR → seconds. RTO, by contrast, drives failover architecture.
    ref: "concepts.md#rpo--recovery-point-objective-acceptable-data-loss"

  - id: disaster-recovery-rpo-rto-strategies-004
    difficulty: intermediate
    tags: [rto, drivers]
    question: |
      RTO primarily drives which design decision?
    options:
      - "How frequently transaction logs are shipped to the standby"
      - "The retention period configured for your data backups"
      - "Whether cross-region replication is synchronous or asynchronous"
      - "How much recovery infrastructure you keep ready (failover architecture)"
    answer: 3
    explanation: |
      RTO is about downtime, so it drives how much you pre-provision: keeping compute
      warm/live removes provisioning and boot time. Replication frequency and sync-vs-async
      relate to RPO (data loss), not RTO.
    ref: "concepts.md#rto--recovery-time-objective-acceptable-downtime"

  - id: disaster-recovery-rpo-rto-strategies-005
    difficulty: intermediate
    tags: [rpo, rto, independence]
    question: |
      A team says "RPO and RTO are basically the same knob — tightening one tightens the
      other." Why is this wrong?
    options:
      - "They are the same underlying number, just converted between minutes and seconds"
      - "RTO is always strictly larger than RPO because recovery includes the data window"
      - "They are independent: one bounds data loss and the other bounds downtime, and they cost money in different places"
      - "RPO applies only to stateful databases while RTO applies only to the compute tier"
    answer: 2
    explanation: |
      RPO (data loss) and RTO (downtime) are orthogonal. You might need RPO≈0 but tolerate
      hours of RTO (a ledger), or tolerate an hour of stale data but need near-zero RTO
      (an ad-funded site). One is bought with replication spend, the other with standby
      compute spend.
    ref: "concepts.md#rpo-and-rto-are-independent-and-drive-cost-separately"

  - id: disaster-recovery-rpo-rto-strategies-006
    difficulty: advanced
    tags: [rpo-zero, synchronous-replication]
    question: |
      Achieving a true RPO of 0 (zero data loss) fundamentally requires what?
    options:
      - "Synchronous replication — a write is acknowledged only after a remote replica has it"
      - "Hourly incremental snapshots stored in an off-site region"
      - "Asynchronous replication with a small in-memory write buffer"
      - "A faster nightly backup job that finishes before peak traffic"
    answer: 0
    explanation: |
      RPO = 0 means no committed write may be lost, which requires the commit to wait until
      a remote replica durably has the data (synchronous replication). Async replication
      always has some lag, so it can lose the un-replicated tail on failure.
    ref: "concepts.md#rpo--recovery-point-objective-acceptable-data-loss"

  - id: disaster-recovery-rpo-rto-strategies-007
    difficulty: advanced
    tags: [rpo-zero, cross-region, cap]
    question: |
      Why is true zero-RPO synchronous replication rarely used across regions?
    options:
      - "Cross-region networks lack the bandwidth to carry any replication traffic"
      - "Object storage and block volumes are unavailable outside the primary region"
      - "It automatically violates every major data-residency compliance regulation"
      - "It couples write latency to the inter-region round-trip and blocks writes if the remote is unreachable"
    answer: 3
    explanation: |
      Synchronous replication makes every commit wait for a remote ack. Across regions
      (e.g. 60–90ms RTT) that latency is often unacceptable for write-heavy workloads, and
      if the remote is unreachable, writes must block — a CAP availability trade. So true
      zero-RPO cross-region is rare; most "near-zero" designs accept seconds of async lag.
    ref: "concepts.md#multi-site-activeactive"

  - id: disaster-recovery-rpo-rto-strategies-008
    difficulty: beginner
    tags: [ha-vs-dr, concepts]
    question: |
      What best distinguishes disaster recovery (DR) from high availability (HA)?
    options:
      - "HA handles rare region-wide catastrophes while DR handles routine single-node failures"
      - "HA absorbs routine failures within the normal footprint (node/disk/AZ); DR handles rare correlated catastrophes (region/dataset loss)"
      - "They are two interchangeable names for the same reliability practice"
      - "DR is fully automatic and invisible while HA always requires a manual declaration"
    answer: 1
    explanation: |
      HA keeps a service up through expected failures inside its normal footprint (usually
      automatic, seconds, no data loss). DR is the plan for the rare, correlated catastrophe
      that exceeds the HA envelope — a region loss, dataset corruption, ransomware.
    ref: "concepts.md#disaster-recovery-vs-high-availability"

  - id: disaster-recovery-rpo-rto-strategies-009
    difficulty: intermediate
    tags: [ha-vs-dr, multi-az]
    question: |
      An engineer claims "we're deployed across three Availability Zones, so we have a
      disaster recovery plan." What's the flaw?
    options:
      - "Three AZs is too few; genuine HA requires at least five separate zones"
      - "Availability Zones can host stateless compute but never stateful databases"
      - "There is no flaw at all; a multi-AZ deployment is a complete DR strategy"
      - "Multi-AZ is HA within one region; a region-wide event or a bad delete replicated to every AZ defeats it — DR needs a separate failure domain plus a restore path"
    answer: 3
    explanation: |
      Multi-AZ gives HA within a single region. A region-wide control-plane event, a bad
      global config push, or a DROP TABLE replicated to all AZs still takes you down. DR
      needs a separate failure domain (another region) and a restore path independent of
      the live data.
    ref: "concepts.md#disaster-recovery-vs-high-availability"

  - id: disaster-recovery-rpo-rto-strategies-010
    difficulty: beginner
    tags: [dr-spectrum, ordering]
    question: |
      Order the four AWS DR strategies from lowest cost / highest RTO to highest cost /
      lowest RTO.
    options:
      - "Backup & Restore → Pilot Light → Warm Standby → Multi-Site Active/Active"
      - "Multi-Site Active/Active → Warm Standby → Pilot Light → Backup & Restore"
      - "Pilot Light → Backup & Restore → Multi-Site Active/Active → Warm Standby"
      - "Warm Standby → Backup & Restore → Pilot Light → Multi-Site Active/Active"
    answer: 0
    explanation: |
      The spectrum runs Backup & Restore (cheapest, hours RTO) → Pilot Light → Warm Standby
      → Multi-Site Active/Active (most expensive, near-zero RTO). Each step pre-pays more
      recovery work to buy back recovery time.
    ref: "concepts.md#the-dr-strategy-spectrum-cost-vs-rtorpo"

  - id: disaster-recovery-rpo-rto-strategies-011
    difficulty: intermediate
    tags: [backup-restore, rto]
    question: |
      In a Backup & Restore DR strategy, what typically dominates the RTO?
    options:
      - "The time to update the DNS records pointing at the recovered site"
      - "The time for an already-warm synchronous replica to catch up on lag"
      - "Provisioning infrastructure plus downloading and restoring the (possibly multi-TB) backup"
      - "The time to deregister a failed region from the DNS rotation pool"
    answer: 2
    explanation: |
      With Backup & Restore, nothing runs in the DR site until disaster. You must provision
      everything and then restore data from backups — restoring multiple TB over the network
      can take hours, which is why this strategy's RTO is measured in hours.
    ref: "concepts.md#backup--restore"

  - id: disaster-recovery-rpo-rto-strategies-012
    difficulty: intermediate
    tags: [pilot-light, mechanism]
    question: |
      What characterizes a Pilot Light DR strategy?
    options:
      - "A full-size production stack runs live and fully scaled in the DR region"
      - "Core data is continuously replicated to the DR region, but the application/compute tier is provisioned but switched off until disaster"
      - "Only nightly backups exist in the DR region, with nothing continuously replicated"
      - "Live traffic is served simultaneously from both regions during normal operation"
    answer: 1
    explanation: |
      Pilot Light keeps the data layer replicated and running while the app/compute tier is
      off (the pilot light is lit but the furnace is off). On disaster you turn on and scale
      up the compute tier — skipping data restore gives an RTO of tens of minutes.
    ref: "concepts.md#pilot-light"

  - id: disaster-recovery-rpo-rto-strategies-013
    difficulty: intermediate
    tags: [warm-standby, pilot-light, comparison]
    question: |
      What is the key difference between Warm Standby and Pilot Light?
    options:
      - "In Warm Standby the full stack is always running but scaled down; in Pilot Light the app/compute tier is off"
      - "Warm Standby relies on nightly backups while Pilot Light relies on continuous replication"
      - "Pilot Light costs more to run at steady state than Warm Standby does"
      - "Warm Standby keeps no replicated data in the DR region at all"
    answer: 0
    explanation: |
      Both replicate data. The difference is the compute tier: Pilot Light keeps it OFF
      (scale up on DR), Warm Standby keeps it ON but small (scale up on DR). Because Warm
      Standby is always running, it can serve traffic immediately and be continuously
      validated — at higher steady-state cost.
    ref: "concepts.md#warm-standby"

  - id: disaster-recovery-rpo-rto-strategies-014
    difficulty: advanced
    tags: [active-active, rpo, misconception]
    question: |
      Which statement about Multi-Site Active/Active is most accurate?
    options:
      - "It guarantees RPO = 0 automatically because both sites are actively serving live traffic"
      - "It is the cheapest DR strategy because both sites reuse one shared infrastructure footprint"
      - "It cannot survive the loss of a single region without a manual restore from backups"
      - "It gives near-zero RTO and near-zero RPO, but true RPO=0 still requires synchronous cross-region writes, which is usually latency-prohibitive"
    answer: 3
    explanation: |
      Active/Active serves live traffic from multiple regions, so failover is just removing
      the failed site (near-zero RTO). RPO is near-zero via async replication, but true zero
      needs synchronous cross-region writes — impractical due to RTT — so candidates should
      say near-zero, not zero.
    ref: "concepts.md#multi-site-activeactive"

  - id: disaster-recovery-rpo-rto-strategies-015
    difficulty: advanced
    tags: [active-active, complexity, tradeoffs]
    question: |
      Beyond cost, what is the primary added difficulty of Multi-Site Active/Active
      compared with Warm Standby?
    options:
      - "It cannot use DNS-based routing and must rely solely on anycast BGP"
      - "Handling multi-master write conflicts, cross-region consistency, and split-brain because multiple sites accept writes"
      - "It requires taking the designated primary offline during normal operation"
      - "It eliminates the need for any point-in-time backups underneath it"
    answer: 1
    explanation: |
      Active/Active has multiple sites accepting live writes, so you must solve write-conflict
      resolution, cross-region consistency, and split-brain. That's engineering complexity, not
      just money — and it still needs backups underneath for logical corruption.
    ref: "concepts.md#multi-site-activeactive"

  - id: disaster-recovery-rpo-rto-strategies-016
    difficulty: intermediate
    tags: [scenario, strategy-selection]
    question: |
      A financial ledger must never lose a committed transaction (RPO≈0), but being down for
      a few hours during recovery is acceptable (RTO = hours). Where should the money go?
    options:
      - "Buy a large always-on standby compute fleet to make failover as fast as possible"
      - "Switch to nightly-only backups to save cost, accepting a full day of potential loss"
      - "Invest in continuous/synchronous replication for the data, not in expensive standby compute"
      - "Adopt Multi-Site Active/Active everywhere regardless of the loose RTO tolerance"
    answer: 2
    explanation: |
      Tight RPO with loose RTO means spend on replication (protecting data), not on standby
      capacity (which buys fast recovery you don't need). RPO and RTO cost money in different
      places, so match the spend to the tight constraint.
    ref: "concepts.md#rpo-and-rto-are-independent-and-drive-cost-separately"

  - id: disaster-recovery-rpo-rto-strategies-017
    difficulty: intermediate
    tags: [scenario, strategy-selection]
    question: |
      A stateless, read-mostly marketing site is fronted by a CDN; an hour of stale content
      is fine (RPO = 1h) but any downtime loses ad revenue (RTO ≈ 0). What's the right
      emphasis?
    options:
      - "Warm/active standby compute so traffic keeps serving, without paying for synchronous replication"
      - "Synchronous cross-region replication of every single write to the content store"
      - "Backup & Restore only, to minimize steady-state cost as much as possible"
      - "Increase the DNS TTL so clients cache the record far longer during outages"
    answer: 0
    explanation: |
      Loose RPO with tight RTO means spend on warm/active standby compute (fast, no-downtime
      cutover), not on synchronous replication. Because stale data is tolerable, cheap async
      or periodic sync of content is sufficient.
    ref: "concepts.md#rpo-and-rto-are-independent-and-drive-cost-separately"

  - id: disaster-recovery-rpo-rto-strategies-018
    difficulty: beginner
    tags: [3-2-1, backups]
    question: |
      The 3-2-1 backup rule states you should keep:
    options:
      - "3 backups per day, 2 per week, 1 per month"
      - "3 regions, 2 accounts, 1 provider"
      - "3 copies, on 2 media types, with 1 copy off-site"
      - "3 replicas, 2 read-only, 1 writable"
    answer: 2
    explanation: |
      3-2-1 = three copies of data, on two different media/storage types, with one copy
      off-site (a different location/region/provider). The point is decorrelating failure
      domains so a single site loss can't destroy all copies.
    ref: "concepts.md#backups-the-3-2-1-rule"

  - id: disaster-recovery-rpo-rto-strategies-019
    difficulty: intermediate
    tags: [3-2-1, off-site, failure-domain]
    question: |
      Why is the "1 copy off-site" part of 3-2-1 the piece that actually makes it a DR
      capability?
    options:
      - "Off-site copies restore faster than local ones because remote object stores are tuned for high-throughput sequential reads"
      - "A backup in the same region/account shares the primary's failure domain, so an off-site copy is what survives a site loss or account compromise"
      - "Off-site storage is always cheaper per gigabyte, so the rule exists mainly to cut long-term backup retention costs"
      - "It is mandated verbatim by every major compliance regime (SOC 2, PCI-DSS, HIPAA), so it is purely a checkbox"
    answer: 1
    explanation: |
      Three copies on one array (or in one region/account) all die together when that domain
      dies. The off-site copy in a separate failure domain is what survives a region outage,
      account compromise, or mass delete — turning backups into real DR.
    ref: "concepts.md#backups-the-3-2-1-rule"

  - id: disaster-recovery-rpo-rto-strategies-020
    difficulty: intermediate
    tags: [3-2-1-1-0, ransomware]
    question: |
      In the extended "3-2-1-1-0" rule, what do the extra "1" and the "0" add?
    options:
      - "1 additional standby region, and 0 seconds of downtime during any failover event"
      - "1 extra synchronous replica, and 0 milliseconds of replication lag to that replica"
      - "1 dedicated cloud provider for the copies, and 0 legacy backups kept on physical tape"
      - "1 offline/immutable/air-gapped copy, and 0 errors verified by regularly testing restores"
    answer: 3
    explanation: |
      3-2-1-1-0 adds one offline/immutable/air-gapped copy (ransomware defense) and requires
      zero errors — verified by regularly performing and validating test restores.
    ref: "concepts.md#immutable--offsite-backups-ransomware-defense"

  - id: disaster-recovery-rpo-rto-strategies-021
    difficulty: beginner
    tags: [test-restores, maxim]
    question: |
      What is meant by "an untested backup is not a backup"?
    options:
      - "Backups automatically expire and become permanently invalid after exactly 30 days, so any copy older than a month is unusable"
      - "You must encrypt every backup with a rotating key or it is useless, and encryption is the only thing a restore actually verifies"
      - "Backups can fail silently (corrupt archives, missing keys, un-runnable scripts) in ways only a real restore reveals, so recoverability is unproven until you restore"
      - "Only full backups count as real backups; incremental and differential backups are just caches and can never be restored alone"
    answer: 2
    explanation: |
      Backups fail silently — incomplete snapshots, corrupt archives, missing encryption keys,
      broken restore scripts, schema mismatches. Until you actually restore and verify, you
      only have a hope, not a backup. SRE puts it as "no one wants backups, they want restores."
    ref: "concepts.md#test-your-restores-untested-backup--no-backup"

  - id: disaster-recovery-rpo-rto-strategies-022
    difficulty: intermediate
    tags: [test-restores, rto, measurement]
    question: |
      Why does regularly performing a test restore matter specifically for validating your
      Backup & Restore RTO?
    options:
      - "The measured restore time IS your real RTO, and for large datasets it is often far longer than the SLA number"
      - "It compacts and deduplicates the archive, so each test restore measurably reduces the stored backup size"
      - "It warms the storage cache so that every subsequent restore of that dataset automatically runs faster"
      - "It quietly upgrades your asynchronous replication link into a synchronous one for the duration of the test"
    answer: 0
    explanation: |
      For Backup & Restore, restoring the data is the dominant recovery step. The only way to
      know your true RTO is to run the restore and time it — for multi-TB datasets it is often
      shockingly longer than the aspirational number on a slide.
    ref: "concepts.md#test-your-restores-untested-backup--no-backup"

  - id: disaster-recovery-rpo-rto-strategies-023
    difficulty: intermediate
    tags: [dr-drills, game-days]
    question: |
      What is the main purpose of a periodic DR drill (game day) that actually invokes the
      failover runbook?
    options:
      - "To reduce ongoing cloud costs by shutting down the primary region for the drill and running only on the standby"
      - "To deliberately increase replication lag on purpose so the standby database has time to compact its write-ahead log"
      - "To satisfy external auditors with a signed attestation form, without actually invoking or changing any part of the live system"
      - "To validate the runbook, the real RTO/RPO numbers, and the human process — surfacing AMI drift, IAM gaps, and quota limits before a real disaster"
    answer: 3
    explanation: |
      Drills exercise the whole recovery path so you discover the broken bits — stale AMIs,
      missing IAM permissions, insufficient quotas, wrong RTO estimates, unclear decision
      authority — under controlled conditions rather than during an actual disaster.
    ref: "concepts.md#test-your-restores-untested-backup--no-backup"

  - id: disaster-recovery-rpo-rto-strategies-024
    difficulty: advanced
    tags: [immutability, ransomware, object-lock]
    question: |
      How does an immutable (WORM) backup, such as S3 Object Lock in Compliance mode, defend
      against ransomware?
    options:
      - "It transparently encrypts the backup with a per-object key so ransomware that reaches the bucket cannot read or exfiltrate it"
      - "It prevents the backup from being modified or deleted until its retention period expires — even by an attacker with root/account credentials"
      - "It continuously replicates the backup across regions faster than ransomware can spread, so a clean copy always stays ahead"
      - "It runs an agent that automatically detects and quarantines any ransomware process the moment it begins encrypting files"
    answer: 1
    explanation: |
      Ransomware/insider threats target backups too. Immutability makes backups write-once
      and undeletable during the lock window — even the account owner cannot override
      Compliance-mode locks — so a clean recovery copy survives an attacker with production
      access.
    ref: "concepts.md#immutable--offsite-backups-ransomware-defense"

  - id: disaster-recovery-rpo-rto-strategies-025
    difficulty: advanced
    tags: [backup-isolation, blast-radius]
    question: |
      Why store backups in a separate cloud account with distinct, tightly-scoped credentials?
    options:
      - "To qualify for a cheaper archival storage tier that is only offered in dedicated backup accounts"
      - "Because the platform technically forbids backups from ever residing in the same account as the production workload"
      - "To avoid paying cross-region data-transfer fees, since intra-account replication between accounts is always billed as free"
      - "So a compromise or mass-delete using production credentials cannot reach or destroy the backups (blast-radius isolation)"
    answer: 3
    explanation: |
      If backups live in the same account as production, a credential compromise or a
      catastrophic mistake can delete them along with everything else. A separate account with
      its own credentials isolates the blast radius so the last line of recovery survives.
    ref: "concepts.md#immutable--offsite-backups-ransomware-defense"

  - id: disaster-recovery-rpo-rto-strategies-026
    difficulty: intermediate
    tags: [pitr, mechanism]
    question: |
      How does point-in-time recovery (PITR) let you restore to any moment within a window?
    options:
      - "It combines a base snapshot with a continuous stream of transaction logs (WAL/binlog/redo) and replays the log forward to the chosen instant"
      - "It captures and stores a complete full snapshot of the entire dataset once every second within the window"
      - "It keeps every version of every row forever in an in-memory multi-version store that is never flushed to disk"
      - "It reverses the primary's writes on demand using undo triggers"
    answer: 0
    explanation: |
      PITR = base snapshot + continuous transaction-log capture. To restore to time T, you
      load the base snapshot and replay logs up to T. This gives second-level RPO for the data
      layer and lets you land just before a destructive event.
    ref: "concepts.md#point-in-time-recovery-pitr"

  - id: disaster-recovery-rpo-rto-strategies-027
    difficulty: advanced
    tags: [pitr, logical-failure, scenario]
    question: |
      A bad deploy runs a destructive query at 14:33 that deletes production rows, and the
      delete has already replicated to every replica. What recovers the data with minimal loss?
    options:
      - "Promote a healthy read replica to primary and cut all client traffic over to it"
      - "Add more read replicas so the bad delete is diluted across a larger replica set and averages out"
      - "Use PITR to restore the database to 14:32:59, just before the destructive query"
      - "Increase the DNS TTL and wait for client resolver caches to expire so traffic drains off the affected rows"
    answer: 2
    explanation: |
      Replicas all faithfully applied the delete, so promoting one doesn't help. PITR lets you
      restore to one second before the destructive query, recovering the data with almost no
      loss of good writes — the classic antidote to a logical failure.
    ref: "concepts.md#point-in-time-recovery-pitr"

  - id: disaster-recovery-rpo-rto-strategies-028
    difficulty: advanced
    tags: [pitr, tradeoff, rto]
    question: |
      What is a trade-off of PITR based on log replay?
    options:
      - "It cannot achieve an RPO better than 24 hours because log replay is capped at one base snapshot per day"
      - "Replaying a long transaction-log stream can make the restore TIME (RTO) longer than a plain snapshot restore, and retention costs storage"
      - "It only works for stateless services, since transaction logs cannot be captured for a stateful datastore"
      - "It requires an always-on synchronous replication link to the log archive in order to function at all"
    answer: 1
    explanation: |
      PITR gives tight RPO (seconds) but restoring means replaying logs forward from the base
      snapshot, which for a long window can take longer than restoring a single recent
      snapshot. Retention of logs and snapshots also costs storage.
    ref: "concepts.md#point-in-time-recovery-pitr"

  - id: disaster-recovery-rpo-rto-strategies-029
    difficulty: advanced
    tags: [replication-not-backup, corruption]
    question: |
      "We keep three synchronous replicas, so our data is safe." From what threat does this
      NOT protect you?
    options:
      - "An accidental DROP TABLE or a data-corruption bug — the bad write is replicated to all replicas"
      - "Loss of a single database instance or an underlying disk volume in the cluster"
      - "An Availability Zone power failure that takes one replica's data center fully offline"
      - "A single-node network partition that isolates one replica from the rest of the quorum"
    answer: 0
    explanation: |
      Replicas protect against hardware/infrastructure loss but faithfully copy every write —
      including a bad DELETE, a corruption bug, or ransomware encryption — to all replicas in
      milliseconds. Only historical/immutable backups (snapshots, PITR) can go back in time.
    ref: "concepts.md#replication-is-not-a-backup-data-corruption--logical-failures"

  - id: disaster-recovery-rpo-rto-strategies-030
    difficulty: intermediate
    tags: [replication-not-backup, concept]
    question: |
      What is the core reason "replication is not a backup"?
    options:
      - "Replicas are stored on slower spinning disks, whereas backups always sit on faster flash storage tuned for durability"
      - "Replication is always asynchronous and lossy, so a replica can never hold a fully committed copy of a write"
      - "Backups are always encrypted at rest while replicas are stored in plaintext, so only backups are trustworthy"
      - "A replica's job is to hold a current copy, so it immediately propagates every write including corrupt or deleted data; only historical copies let you recover from logical errors"
    answer: 3
    explanation: |
      Replication keeps data current, which means bad writes propagate instantly. Recovery from
      logical failures (bad deploys, deletes, corruption, ransomware) requires going back in
      time, which only historical/immutable backups and PITR provide.
    ref: "concepts.md#replication-is-not-a-backup-data-corruption--logical-failures"

  - id: disaster-recovery-rpo-rto-strategies-031
    difficulty: expert
    tags: [replication-not-backup, active-active]
    question: |
      Why do even Multi-Site Active/Active architectures still maintain point-in-time backups?
    options:
      - "To reduce the cost of cross-region replication by letting backups absorb part of the inter-site data-transfer volume"
      - "Because live replication between sites propagates logical corruption/deletes everywhere, so only historical backups can recover from those"
      - "Because an active/active topology cannot actually survive the loss of a single region unless a backup restore runs first"
      - "Because a point-in-time backup is the only supported way to seed and bootstrap each new live region's initial replica"
    answer: 1
    explanation: |
      Active/Active protects against infrastructure and region loss, but its replication also
      spreads logical corruption to every live site. Point-in-time backups are the only defense
      against bad deploys, mass deletes, and ransomware — so they sit under every strategy.
    ref: "concepts.md#replication-is-not-a-backup-data-corruption--logical-failures"

  - id: disaster-recovery-rpo-rto-strategies-032
    difficulty: intermediate
    tags: [rto, dns-ttl, cutover]
    question: |
      During DR cutover, why can a high DNS TTL blow your RTO even after the DR site is fully
      up and serving?
    options:
      - "A high DNS TTL increases database replication lag between the primary and the DR site during cutover"
      - "DNS records are frozen and cannot be changed at all during a declared disaster, so cutover must wait for the freeze to lift"
      - "Clients keep using the cached old record until the TTL expires, so traffic isn't actually redirected until then"
      - "The DNS TTL value also controls how long point-in-time backups are retained before automatic expiry"
    answer: 2
    explanation: |
      RTO is only met when traffic actually serves from the recovered site. If clients cache the
      DNS record for, say, 300s, cutover isn't complete until that TTL expires — so keep
      failover-record TTLs low (e.g. 60s) or use health-checked DNS failover/anycast.
    ref: "concepts.md#rto--recovery-time-objective-acceptable-downtime"

  - id: disaster-recovery-rpo-rto-strategies-033
    difficulty: advanced
    tags: [rto, hidden-steps]
    question: |
      Which recovery step is most often UNDERESTIMATED when teams estimate their RTO?
    options:
      - "Detection plus the human decision/declaration to invoke DR"
      - "The few seconds it takes a provisioned CPU core to complete its power-on self-test and boot"
      - "The sub-millisecond time it takes the application to flush and write a single line to its log file"
      - "The minute or two it takes an engineer to open a cloud-provider support ticket for the incident"
    answer: 0
    explanation: |
      RTO covers the whole path: detection, human decision to declare a disaster, provisioning,
      data recovery, and cutover. Detection and the declaration decision are often the biggest
      hidden delays because they involve people and judgment, not just machines.
    ref: "concepts.md#rto--recovery-time-objective-acceptable-downtime"

  - id: disaster-recovery-rpo-rto-strategies-034
    difficulty: intermediate
    tags: [failover, failback, definitions]
    question: |
      What is "failback" in a DR context?
    options:
      - "Promoting the DR site to become the new primary at the moment disaster strikes the original region"
      - "Rolling back a failed software deployment to the previous known-good application release"
      - "Returning operations to the original site after it recovers, including re-replicating accumulated writes back and reconciling conflicts"
      - "Reducing the number of standby replicas back down once a temporary traffic spike has subsided"
    answer: 2
    explanation: |
      Failover promotes the DR site; failback is the return trip once the original site
      recovers — reversing replication to sync back the writes that accumulated in the DR site,
      reconciling conflicts, and cutting back over. It's often harder than failover and
      frequently undrilled.
    ref: "concepts.md#failover-and-failback"

  - id: disaster-recovery-rpo-rto-strategies-035
    difficulty: advanced
    tags: [failback, tradeoffs]
    question: |
      Why is failback frequently more difficult and risky than the initial failover?
    options:
      - "The original primary site can never be brought back online once a DR failover has occurred"
      - "You must reverse replication to sync writes accumulated in the DR site back to the primary, reconcile any conflicts, and cut back over safely"
      - "Failback requires deleting all existing backups first so the recovered primary starts from a clean slate"
      - "Failback always causes permanent, unavoidable data loss of the writes taken during the DR window"
    answer: 1
    explanation: |
      During DR the DR site accumulated writes. Failback must replicate those back to the
      recovered primary (reversing replication direction), reconcile conflicts, and cut over
      again — ideally in a quiet window. Many outages are extended by a botched failback; some
      teams instead make the DR site the new permanent primary.
    ref: "concepts.md#failover-and-failback"

  - id: disaster-recovery-rpo-rto-strategies-036
    difficulty: advanced
    tags: [failover, automatic-vs-manual, cross-region]
    question: |
      Why is cross-region DR failover more often deliberate/human-declared rather than fully
      automatic?
    options:
      - "Automatic failover is technically impossible across regions because health checks cannot traverse a region boundary"
      - "Humans can restore data faster than automation, so manual failover always beats automated cutover on RTO"
      - "The cloud provider contractually forbids automated cross-region failover outside of a manual support request"
      - "The blast radius of a mistaken failover is huge, and false positives could split-brain or move all traffic on a transient blip"
    answer: 3
    explanation: |
      Automatic failover minimizes RTO but risks acting on false positives; for a whole-region
      switch the cost of being wrong (or of split-brain) is enormous. Many teams gate
      cross-region failover behind a human declaration for safety, accepting higher RTO.
    ref: "concepts.md#failover-and-failback"

  - id: disaster-recovery-rpo-rto-strategies-037
    difficulty: intermediate
    tags: [backup-restore, best-fit]
    question: |
      For which workload is Backup & Restore the most appropriate DR strategy?
    options:
      - "A real-time payments API where any downtime is catastrophic and every committed transaction must survive a failure"
      - "A global e-commerce checkout during peak sales, where seconds of outage translate directly into lost orders and revenue"
      - "A non-critical or dev/test workload where hours of RTO and hours of RPO are acceptable and cost matters most"
      - "A stock-trading matching engine where even a brief pause corrupts order sequencing and drops in-flight trades"
    answer: 2
    explanation: |
      Backup & Restore is cheapest but has hours of RTO (provision + restore) and RPO tied to
      backup interval. It fits cost-sensitive, non-critical, or dev/test systems. Latency- and
      revenue-critical systems need warmer strategies. Note: even they keep backups as the floor.
    ref: "concepts.md#backup--restore"

  - id: disaster-recovery-rpo-rto-strategies-038
    difficulty: intermediate
    tags: [backup-restore, corruption-defense]
    question: |
      Which is uniquely TRUE of Backup & Restore compared with Pilot Light, Warm Standby, and
      Active/Active?
    options:
      - "It is the strategy that inherently protects against logical corruption and deletion, because it keeps historical copies rather than only fresh replicated data"
      - "It has the lowest RTO of the four because no data restore step is ever required at cutover"
      - "It requires the most steady-state infrastructure because a full-size fleet runs live in the DR region"
      - "It provides near-zero RPO thanks to its continuous synchronous replication to the backup store"
    answer: 0
    explanation: |
      The other three keep data "fresh" via replication, which propagates a bad delete. Backup
      & Restore keeps historical copies, so it's the one that protects against corruption/deletion
      — which is why every DR plan keeps point-in-time backups regardless of primary strategy.
    ref: "concepts.md#backup--restore"

  - id: disaster-recovery-rpo-rto-strategies-039
    difficulty: intermediate
    tags: [pilot-light, drill-risk]
    question: |
      Why do DR drills matter especially for a Pilot Light strategy?
    options:
      - "Because the continuous data replication in Pilot Light is unreliable and silently drops writes between drills"
      - "Because Pilot Light keeps no backups at all, so a drill is the only copy of the data that ever exists"
      - "Because Pilot Light already serves live production traffic and any drill would disrupt real users"
      - "Because the cold app tier is unexercised, so capacity limits, AMI drift, or scaling problems surface exactly when you invoke DR"
    answer: 3
    explanation: |
      In Pilot Light the compute tier is off, so it's never validated under load. Drills reveal
      whether it can actually be turned on and scaled up (quota, AMI drift, scaling config)
      before a real disaster depends on it.
    ref: "concepts.md#pilot-light"

  - id: disaster-recovery-rpo-rto-strategies-040
    difficulty: expert
    tags: [scenario, strategy-selection, tradeoffs]
    question: |
      A global product needs near-zero RTO and near-zero RPO and the team can invest heavily
      and solve write-conflict handling. Which strategy fits, and what's the main catch?
    options:
      - "Backup & Restore; the catch is that multi-TB restore over the network stretches recovery to many hours"
      - "Multi-Site Active/Active; the catch is multi-master conflict resolution, consistency, and split-brain complexity plus 2x+ infrastructure cost"
      - "Pilot Light; the catch is the cold compute tier that must be scaled up before it can serve any traffic"
      - "Warm Standby; the catch is that its scaled-down fleet still needs a control-plane scale-up during failover"
    answer: 1
    explanation: |
      Near-zero RTO and RPO globally points to Multi-Site Active/Active. The catch is that
      multiple live write sites force you to solve conflict resolution, cross-region consistency,
      and split-brain — engineering complexity on top of running full infrastructure everywhere.
    ref: "concepts.md#multi-site-activeactive"

  - id: disaster-recovery-rpo-rto-strategies-041
    difficulty: beginner
    tags: [rpo, calculation]
    question: |
      If your only data protection is a backup job that runs once every 24 hours, what is your
      effective RPO in the worst case?
    options:
      - "Zero, because the nightly job flushes every committed write to durable storage as it happens"
      - "Exactly the RTO, since data loss and downtime windows always coincide for backup-based recovery"
      - "One hour, matching the transaction-log checkpoint interval the database uses internally"
      - "Up to about 24 hours of data"
    answer: 3
    explanation: |
      A disaster can strike just before the next backup, so you could lose everything written
      since the last backup — up to ~24 hours. Your real RPO is bounded by the backup interval
      (or replication lag), regardless of the SLA number.
    ref: "concepts.md#rpo--recovery-point-objective-acceptable-data-loss"

  - id: disaster-recovery-rpo-rto-strategies-042
    difficulty: intermediate
    tags: [scenario, cross-region-backup]
    question: |
      A team stores nightly database backups in the SAME region and SAME account as the primary
      database. What DR risk does this create?
    options:
      - "The backups share the primary's failure domain, so a region outage or account compromise can destroy the primary AND its backups together"
      - "None; co-locating backups in the same region and account is the recommended best practice for restore speed"
      - "The backups will restore too quickly and overwhelm the primary database with a sudden write burst"
      - "It violates the RTO target but leaves the RPO completely unaffected in every failure scenario"
    answer: 0
    explanation: |
      A backup in the same region/account shares the failure domain of what it protects. A
      region-wide event or a compromised/misused account can wipe both. DR requires an off-site
      copy in a separate failure domain (the "1" in 3-2-1).
    ref: "concepts.md#backups-the-3-2-1-rule"

  - id: disaster-recovery-rpo-rto-strategies-043
    difficulty: advanced
    tags: [scenario, replication-vs-backup, judgment]
    question: |
      An interviewer asks: "We have cross-region async replicas with 2s lag. Are we covered for
      disaster recovery?" What is the best answer?
    options:
      - "Yes, cross-region replicas fully cover every disaster scenario including bad deploys and mass deletes"
      - "No, asynchronous replication provides no meaningful DR value and should be treated as pure overhead"
      - "Partially — replicas cover region/infrastructure loss with ~2s RPO, but not logical corruption or malicious deletes, which replicate too; you also need point-in-time backups"
      - "Yes, as long as the replication link is asynchronous rather than synchronous across the two regions"
    answer: 2
    explanation: |
      Cross-region replicas handle infrastructure/region loss with a small RPO (the lag). But
      they faithfully copy bad deletes and corruption, so they don't cover logical failures or
      ransomware. A complete DR posture pairs replicas with immutable point-in-time backups.
    ref: "concepts.md#replication-is-not-a-backup-data-corruption--logical-failures"

  - id: disaster-recovery-rpo-rto-strategies-044
    difficulty: intermediate
    tags: [dr-spectrum, cost-model]
    question: |
      What is the unifying mental model for moving down the DR strategy spectrum
      (Backup & Restore → Active/Active)?
    options:
      - "You progressively reduce data durability guarantees at each tier in order to save on storage cost"
      - "You pre-pay more of the recovery work (keeping data, then compute, then live traffic ready) to buy back recovery time, at higher steady-state cost"
      - "You deliberately increase both RPO and RTO together the more money you spend on the strategy"
      - "You shift all recovery responsibility from the engineering team onto the cloud provider's managed services"
    answer: 1
    explanation: |
      Each step down keeps more of the system ready — replicated data, then warm compute, then
      live-serving compute — so less work remains at disaster time. You trade higher ongoing cost
      and complexity for lower RTO/RPO. There's no universally "best" tier; it's per-workload.
    ref: "concepts.md#the-dr-strategy-spectrum-cost-vs-rtorpo"

  - id: disaster-recovery-rpo-rto-strategies-045
    difficulty: advanced
    tags: [failover, split-brain, safety]
    question: |
      Automatic failover that promotes a DR site while the original primary is still alive risks
      what, and what mitigates it?
    options:
      - "Split-brain (two primaries corrupting data), mitigated by quorum and fencing/STONITH"
      - "A cache-miss storm on the DR site's cold caches, mitigated by pre-warming the cache before cutover"
      - "A DNS TTL integer overflow that corrupts the failover record, mitigated by raising the TTL value"
      - "Excess asynchronous replication lag to the promoted site, mitigated by adding more read replicas"
    answer: 0
    explanation: |
      If failover promotes a second primary while the first still accepts writes, you get
      split-brain and divergent/corrupt data. Quorum (majority agreement) and fencing/STONITH
      (isolating the old primary) prevent two active writers — see redundancy-failover-and-health-checks.
    ref: "concepts.md#failover-and-failback"

  - id: disaster-recovery-rpo-rto-strategies-046
    difficulty: intermediate
    tags: [warm-standby, validation]
    question: |
      A benefit of Warm Standby over Pilot Light, beyond faster RTO, is that:
    options:
      - "It requires no continuous data replication to the DR region because the standby stack recreates state on demand"
      - "It eliminates the need for any DNS changes at cutover because clients are already pointed at both regions"
      - "It automatically achieves an RPO of zero because the running standby acknowledges every primary write"
      - "Because the scaled-down stack is always running, you can route a trickle of real traffic or synthetic canaries to it, so it is continuously validated"
    answer: 3
    explanation: |
      Warm Standby runs the full stack (scaled down) continuously, so it can serve traffic now.
      That means you can send synthetic canaries or a slice of real traffic to keep it verified —
      unlike Pilot Light's cold, unexercised compute tier.
    ref: "concepts.md#warm-standby"

  - id: disaster-recovery-rpo-rto-strategies-047
    difficulty: beginner
    tags: [pitr, benefit]
    question: |
      A key DR benefit of continuous PITR log capture (beyond restoring to a chosen time) is:
    options:
      - "It removes the need for any base snapshots, since the transaction-log stream alone can rebuild the full dataset"
      - "It provides a very tight RPO (seconds) for the data layer because writes are captured continuously"
      - "It guarantees a zero RTO because the log stream is replayed instantly at the moment of failover"
      - "It eliminates the need for any cross-region storage by keeping all logs on the primary instance"
    answer: 1
    explanation: |
      Because PITR streams transaction logs continuously, the most recent recoverable point is
      only seconds behind the present — giving a very tight RPO for the data layer, in addition
      to the ability to land on any instant within the retention window.
    ref: "concepts.md#point-in-time-recovery-pitr"

  - id: disaster-recovery-rpo-rto-strategies-048
    difficulty: expert
    tags: [scenario, judgment, tradeoffs]
    question: |
      A write-heavy service demands RPO = 0 and a primary and DR region 80ms RTT apart. The
      product team also insists writes must never block. What should you tell them?
    options:
      - "Use asynchronous replication; it delivers RPO = 0 with no latency cost"
      - "Take more frequent backups until RPO reaches 0"
      - "RPO = 0 requires synchronous replication, which forces every write to wait ~80ms+ and to block when the remote is unreachable — so RPO = 0 and never-blocking writes are mutually exclusive here; they must relax one requirement"
      - "Increase the DNS TTL to compensate for the latency"
    answer: 2
    explanation: |
      RPO = 0 needs synchronous replication, which couples commit latency to the 80ms RTT and
      must block writes if the remote is down (a CAP availability trade). You cannot have zero
      data loss AND never-blocking low-latency writes across regions — the team must relax RPO
      (accept seconds of async lag) or accept the latency/availability cost.
    ref: "concepts.md#multi-site-activeactive"

  - id: disaster-recovery-rpo-rto-strategies-049
    difficulty: intermediate
    tags: [ha-vs-dr, cost-framing]
    question: |
      Which framing best captures how DR cost should be justified to the business?
    options:
      - "DR is insurance priced per workload against the cost of downtime and data loss for that specific system"
      - "DR is a fixed engineering tax applied equally to every service regardless of its business criticality"
      - "DR cost should always be minimized to the cheapest tier since recovery events are too rare to justify spend"
      - "DR cost is identical to the baseline HA architecture cost and can be folded into the same budget line"
    answer: 0
    explanation: |
      DR is essentially insurance: you weigh the ongoing cost of a strategy against the business
      impact (revenue, compliance, reputation) of downtime/data loss for that workload. That's
      why different services justifiably land on different tiers of the spectrum.
    ref: "concepts.md#disaster-recovery-vs-high-availability"

  - id: disaster-recovery-rpo-rto-strategies-050
    difficulty: advanced
    tags: [scenario, testing, silent-failure]
    question: |
      A team's backup dashboard shows green for a year. During a real incident, the restore
      fails because the archives were missing the encryption key. What DR practice would have
      caught this?
    options:
      - "Adding more read replicas so a healthy one can always be promoted in place of the failed restore"
      - "Increasing the backup frequency so the missing encryption key is captured more often per day"
      - "Regular test restores into an isolated environment, verifying integrity end-to-end (including decryption and usability)"
      - "Switching to synchronous replication so the backup archives always carry a current copy of the key"
    answer: 2
    explanation: |
      "Backup succeeded" only proves data was written, not that it can be restored and used.
      Only a full test restore — including decrypting and validating — exercises the whole path
      and catches silent failures like missing keys, corrupt archives, or broken restore scripts.
    ref: "concepts.md#test-your-restores-untested-backup--no-backup"

  - id: disaster-recovery-rpo-rto-strategies-051
    difficulty: beginner
    tags: [dr-spectrum, active-active, rto]
    question: |
      In Multi-Site Active/Active, what is the "failover action" when one region is lost?
    options:
      - "Provision an entirely new region from scratch and restore the latest backups before serving any traffic"
      - "Simply remove the failed site from rotation; surviving sites already serving traffic absorb the load"
      - "Turn on the cold compute tier in the surviving region and scale it up to full production capacity"
      - "Reverse the replication direction and reconcile all write conflicts before the survivors can serve"
    answer: 1
    explanation: |
      Because all sites already serve live traffic, there's no promotion or provisioning to do —
      you just deregister the failed site (e.g. remove it from DNS/health checks) and the
      survivors carry the load, giving near-zero RTO.
    ref: "concepts.md#multi-site-activeactive"

  - id: disaster-recovery-rpo-rto-strategies-052
    difficulty: expert
    tags: [control-plane, data-plane, failover, scenario]
    question: |
      Your Warm Standby DR plan works flawlessly in every drill, but during a real regional
      outage the failover hangs for hours "waiting for capacity" and never completes. What is
      the most likely root cause?
    options:
      - "The DNS TTL was set too low, so client resolver caches never updated to point at the standby region"
      - "The failover depended on a control-plane operation (auto-scaling / CreateStack) that was degraded by the very regional event you were recovering from"
      - "Asynchronous replication lag to the standby exceeded the configured RPO and stalled the promotion"
      - "The backups were stored in an immutable Object Lock bucket, which blocked the restore from writing"
    answer: 1
    explanation: |
      The classic control-plane trap: scaling up the standby fleet or provisioning stacks are
      control-plane operations, and control planes are the first thing overloaded in a broad
      regional event — exactly when you invoke DR. Drills pass because the control plane is
      healthy then. Mitigation is static stability (pre-provisioned capacity) + data-plane
      failover.
    ref: "concepts.md#control-plane-vs-data-plane-in-failover"

  - id: disaster-recovery-rpo-rto-strategies-053
    difficulty: advanced
    tags: [control-plane, data-plane, definitions]
    question: |
      Which of these failover actions is a DATA-plane operation (and therefore the more
      resilient choice during a regional event)?
    options:
      - "Calling CreateStack to provision the recovery environment's infrastructure at failover time"
      - "Increasing an Auto Scaling group's desired capacity to bring the standby fleet up to full size"
      - "Editing Route 53 weighted-routing weights by hand to shift traffic toward the recovery region"
      - "A health check flipping an already-configured Route 53 failover record"
    answer: 3
    explanation: |
      Answering a health-checked DNS query and evaluating the health check are data-plane
      operations, engineered for higher availability. Provisioning stacks, changing ASG size,
      and rewriting routing weights are control-plane mutations that may be unavailable during
      the event you're recovering from.
    ref: "concepts.md#control-plane-vs-data-plane-in-failover"

  - id: disaster-recovery-rpo-rto-strategies-054
    difficulty: expert
    tags: [static-stability, hot-standby, mitigation]
    question: |
      What is the defining property of a "hot standby" recovery region that a scaled-down Warm
      Standby lacks?
    options:
      - "It is provisioned at full production capacity, so failover requires no scale-up and thus no control-plane dependency"
      - "It serves live production traffic at all times like Active/Active"
      - "It keeps no replicated data, only backups"
      - "It relies entirely on auto-scaling at failover time"
    answer: 0
    explanation: |
      Hot standby is the statically-stable active/passive endpoint: full-capacity capacity is
      already running (idle), so failover is just a traffic shift with no scale-up and no
      control-plane call. Warm Standby runs scaled-down and must scale up (a control-plane op).
      Hot standby costs more but removes that disaster-time risk.
    ref: "concepts.md#static-stability-and-hot-standby"

  - id: disaster-recovery-rpo-rto-strategies-055
    difficulty: advanced
    tags: [bcp, bia, definitions]
    question: |
      Who/what properly determines a system's RTO and RPO values?
    options:
      - "They are outputs of a Business Impact Analysis (BIA) — assigned per system by the business based on impact"
      - "The on-call engineer picks them during incidents based on how fast the recovery tooling happens to feel"
      - "The cloud provider sets them for you through the fine print of the service-level agreement in your contract"
      - "They default automatically to the current replication lag measured on the primary database instance"
    answer: 0
    explanation: |
      RTO and RPO are business risk decisions, not engineering conveniences. A Business Impact
      Analysis assigns them per system based on the cost of downtime and data loss. Engineers
      then implement architectures that meet those targets.
    ref: "concepts.md#business-continuity-bcp-bia-and-mtdwrt"

  - id: disaster-recovery-rpo-rto-strategies-056
    difficulty: expert
    tags: [mtd, wrt, rto, subtle-distinction]
    question: |
      A payments system's database is restored and the app is serving at 02:00 (RTO met at
      90 min), but it takes another 45 min to reconcile the in-flight transaction queue before
      the business can safely accept new payments. What captures that extra 45 minutes, and what
      is the MTD relationship?
    options:
      - "It's simply extra RPO; MTD = RPO minus RTO"
      - "It's replication lag; here MTD = RTO only"
      - "It's the DNS TTL; MTD is unrelated to it"
      - "It's Work Recovery Time (WRT); MTD = RTO + WRT"
    answer: 3
    explanation: |
      WRT (Work Recovery Time) is the post-restore time to validate/reconcile data and resume
      the business function. MTD (Maximum Tolerable Downtime) = RTO + WRT, and both together
      must fit under the business's ceiling. Candidates who equate "system up" with "recovered"
      miss WRT.
    ref: "concepts.md#business-continuity-bcp-bia-and-mtdwrt"

  - id: disaster-recovery-rpo-rto-strategies-057
    difficulty: intermediate
    tags: [bcp, dr, scope]
    question: |
      How does Business Continuity Planning (BCP) relate to Disaster Recovery (DR)?
    options:
      - "BCP is org-wide (people, facilities, comms, vendors, processes); DR is the IT/technical subset that recovers systems"
      - "BCP and DR are two interchangeable names for the exact same organization-wide recovery discipline"
      - "DR is the broader org-wide plan covering staff and facilities; BCP is only the narrow IT-systems subset"
      - "BCP only applies to physical on-premises data centers and has no relevance to cloud-hosted workloads"
    answer: 0
    explanation: |
      BCP keeps the whole business running through a disruption — staff, facilities,
      communications, suppliers, legal. DR is the technical subset focused on recovering IT
      systems and data. DR feeds into, but does not replace, BCP.
    ref: "concepts.md#business-continuity-bcp-bia-and-mtdwrt"

  - id: disaster-recovery-rpo-rto-strategies-058
    difficulty: advanced
    tags: [share-tier, history, taxonomy]
    question: |
      In the SHARE/IBM seven-tier DR model, what does Tier 1 (PTAM, "Pickup Truck Access
      Method") describe?
    options:
      - "Fully automated dual-site recovery with continuous mirroring and zero data loss"
      - "Synchronous two-site disk mirroring coordinated with two-phase commit across both data centers"
      - "Backups physically shipped off-site, with recovery measured in days"
      - "No off-site copy of the data at all, relying solely on local on-site backups"
    answer: 2
    explanation: |
      Tier 1 (PTAM) means backups are created and physically transported off-site — recovery
      takes days. Tier 0 is no off-site data; Tier 6–7 are near-zero data loss with automation.
      PTAM roughly corresponds to Backup & Restore in the AWS spectrum.
    ref: "concepts.md#the-shareibm-seven-tier-dr-model"

  - id: disaster-recovery-rpo-rto-strategies-059
    difficulty: expert
    tags: [rco, consistency, scenario]
    question: |
      After a DR failover all microservices report healthy, but customers see orders that
      reference payments which don't exist. Which recovery objective was violated?
    options:
      - "Recovery Consistency Objective (RCO) — interlinked datasets were restored to different points in time, leaving referential-integrity gaps"
      - "Recovery Point Objective — the failover simply lost too much recently committed data across the board"
      - "Recovery Time Objective — the overall recovery took far longer than the agreed downtime target allowed"
      - "Maximum Tolerable Downtime — the outage overran the absolute business ceiling before service resumed"
    answer: 0
    explanation: |
      RCO measures whether interlinked systems are restored to a mutually consistent state.
      Restoring the orders service and payments service to slightly different points in time (or
      out of dependency order) produces exactly this referential-integrity gap. RPO/RTO alone
      don't capture cross-system consistency.
    ref: "concepts.md#recovery-consistency-objective-rco-and-dependency-order-recovery"

  - id: disaster-recovery-rpo-rto-strategies-060
    difficulty: expert
    tags: [dependency-order, recovery-deadlock, scenario]
    question: |
      During a cold-region recovery, service A won't start because it can't reach service B, and
      B won't start because it can't reach A. What is this called and how is it best avoided?
    options:
      - "Split-brain between two primaries; avoid it with quorum voting and fencing of the losing node"
      - "Replication lag between the regions; avoid it by switching to synchronous cross-region replication"
      - "A recovery deadlock from circular startup dependencies; avoid it with lazy init, degraded-mode startup, and a documented bootstrap order"
      - "A cache stampede on cold caches; avoid it by pre-warming every service cache before startup"
    answer: 2
    explanation: |
      Circular startup dependencies can make a cold region un-bootable — a recovery deadlock.
      Recovery must proceed in topological order (identity → data → core → edge); cycles are
      broken with lazy initialization, degraded-mode startup, or a documented bootstrap
      sequence.
    ref: "concepts.md#recovery-consistency-objective-rco-and-dependency-order-recovery"

  - id: disaster-recovery-rpo-rto-strategies-061
    difficulty: advanced
    tags: [dependency-order, whole-system-rto]
    question: |
      Why is whole-system RTO usually larger than the RTO of any single service?
    options:
      - "Because each service's DNS TTL compounds and multiplies across every service in the dependency chain during cutover"
      - "Because RTO is a property of the dependency graph — the system isn't recovered until the critical path through recovery order (identity → data → core → edge) completes"
      - "Because every service must be restored twice — once for data and once for compute — as a mandatory safety measure"
      - "Because asynchronous replication lag accumulates and adds up linearly across every service being recovered"
    answer: 1
    explanation: |
      You must recover services in dependency (topological) order, so the whole-system RTO is at
      least the critical path through that graph. Treating the system as a single monolith
      underestimates real recovery time.
    ref: "concepts.md#recovery-consistency-objective-rco-and-dependency-order-recovery"

  - id: disaster-recovery-rpo-rto-strategies-062
    difficulty: intermediate
    tags: [recovery-to-cloud, drs, definitions]
    question: |
      What distinguishes "recovery TO the cloud" from "recovery IN the cloud"?
    options:
      - "Recovery to the cloud only uses backups; recovery in the cloud only uses replication"
      - "They are simply two interchangeable names for a Multi-Site Active/Active topology"
      - "Recovery to the cloud means an on-prem/other-cloud primary fails over into the cloud (e.g. AWS DRS block-level replication); recovery in the cloud has both sites already in the cloud"
      - "Recovery to the cloud mandates synchronous replication of every write from the on-prem primary"
    answer: 2
    explanation: |
      Recovery *to* the cloud handles an on-premises or other-cloud primary failing over into
      the cloud — AWS Elastic Disaster Recovery (DRS) does this via continuous block-level
      replication, itself a Pilot Light pattern. Recovery *in* the cloud assumes both sites are
      already cloud-hosted.
    ref: "concepts.md#recovery-in-the-cloud-vs-recovery-to-the-cloud"

  - id: disaster-recovery-rpo-rto-strategies-063
    difficulty: advanced
    tags: [delayed-replica, logical-failure, scenario]
    question: |
      A team keeps a replica intentionally lagged one hour behind the primary. What logical-
      failure advantage does this provide, and what is its main limitation?
    options:
      - "It gives an RPO of 0 for logical failures; its main limitation is the high write latency it adds"
      - "A destructive statement hasn't reached it yet, so within the delay window you can extract/promote before the bad change applies; but it only helps if you detect the error inside that window and is useless for infra failover"
      - "It eliminates the need for point-in-time backups entirely by always holding a clean copy"
      - "It protects against ransomware permanently, regardless of how late the attack is detected"
    answer: 1
    explanation: |
      A delayed replica hasn't yet applied a recent DROP/DELETE, so you can catch it in the
      delay window and recover far faster than a full PITR. But it's stale by design (useless for
      infrastructure failover) and only helps if you detect the logical error before the delay
      elapses. It complements PITR/immutable backups, not replaces them.
    ref: "concepts.md#delayed-replicas"

  - id: disaster-recovery-rpo-rto-strategies-064
    difficulty: expert
    tags: [failover-mechanism, arc, data-plane]
    question: |
      You want a manual, deliberate cross-region failover that stays reliable even when other
      AWS control planes are degraded during a regional event. Which mechanism is designed for
      exactly this?
    options:
      - "Rewriting Route 53 weighted-routing weights by hand to shift traffic toward the recovery region"
      - "Calling CreateStack to provision the recovery region's stack at the moment of failover"
      - "Raising the DNS TTL so clients hold the old cached record for longer during the cutover"
      - "Amazon Application Recovery Controller (ARC) routing controls — a highly-available data-plane on/off switch"
    answer: 3
    explanation: |
      ARC routing controls are purpose-built as a data-plane, highly-available toggle for
      deliberate failover, so they work even when other control planes are impaired. Editing
      routing weights or provisioning stacks are control-plane actions that may be unavailable
      during the event.
    ref: "concepts.md#failover-mechanisms-data-plane-safe-routing"

  - id: disaster-recovery-rpo-rto-strategies-065
    difficulty: advanced
    tags: [failover-mechanism, anycast, dns-ttl]
    question: |
      Why can AWS Global Accelerator (anycast) shift traffic faster than health-checked DNS
      failover during a region loss?
    options:
      - "It replicates the underlying data synchronously between the two regions during the shift"
      - "It automatically provisions fresh compute capacity in the surviving region at failover time"
      - "It redirects traffic at the network layer via anycast BGP, avoiding client-side DNS caching/TTL delays"
      - "It raises the DNS TTL to keep clients pinned to a stable endpoint during the region loss"
    answer: 2
    explanation: |
      Anycast advertises the same IPs from multiple regions and shifts at the network layer, so
      there's no dependence on clients expiring cached DNS records. DNS failover is still gated
      by resolver/client TTL caching, which can add minutes.
    ref: "concepts.md#failover-mechanisms-data-plane-safe-routing"

  - id: disaster-recovery-rpo-rto-strategies-066
    difficulty: expert
    tags: [nines, downtime-budget, numbers]
    question: |
      Approximately how much total downtime per year does a 99.99% ("four nines") availability
      target permit?
    options:
      - "About 8.77 hours, the budget that a three-nines target would permit"
      - "About 5.26 minutes, the budget that a five-nines target would permit"
      - "About 4.38 minutes, which is the four-nines budget measured per month"
      - "About 52.6 minutes"
    answer: 3
    explanation: |
      99.99% ≈ 52.6 minutes/year (about 4.38 min/month). 99.9% ≈ 8.77 h/yr; 99.999% ≈ 5.26
      min/yr. This is why a single multi-hour DR event is incompatible with a four-nines annual
      SLO unless such events are rarer than once per several years.
    ref: "concepts.md#availability-nines-and-the-downtime-budget"

  - id: disaster-recovery-rpo-rto-strategies-067
    difficulty: expert
    tags: [nines, rto, slo, scenario]
    question: |
      A team commits to a 99.99% annual availability SLO but also sets a DR RTO of 4 hours for
      the same service. What's the tension an interviewer wants you to surface?
    options:
      - "There is no tension at all; the RTO and the availability SLO are entirely unrelated targets"
      - "An RTO of 4 hours automatically guarantees the 99.99% availability the team committed to"
      - "The availability SLO should be lowered to 90% specifically to leave room for backup windows"
      - "A 99.99% SLO allows only ~52.6 minutes of downtime per year, so a single 4-hour DR event blows the yearly budget several times over — the two targets are inconsistent unless DR events are extremely rare"
    answer: 3
    explanation: |
      Downtime budget for 99.99% is ~52.6 min/year. A 4-hour recovery consumes roughly five
      years' worth of budget in one event. Either the SLO must be relaxed, the RTO tightened
      (warmer strategy), or you must justify that DR events occur far less than annually.
    ref: "concepts.md#availability-nines-and-the-downtime-budget"

  - id: disaster-recovery-rpo-rto-strategies-068
    difficulty: expert
    tags: [sre, defense-in-depth, soft-delete]
    question: |
      In Google SRE's data-integrity defense-in-depth, what is the FIRST line of defense against
      the most common cause of data loss (user/app deletes and bugs), and why is it preferred?
    options:
      - "Offline tape backups, because their air-gapped media is inherently immutable and tamper-proof"
      - "Synchronous replication, because it holds an RPO of 0 and instantly reflects every committed write"
      - "Soft/lazy deletion with a recovery window (e.g. 30-day trash), because it recovers without any restore and is far cheaper and faster than backups"
      - "Out-of-band validators, because they run continuously and scan every dataset in real time"
    answer: 2
    explanation: |
      SRE Ch. 26 makes soft deletion (mark-then-purge with a window like Gmail's 30-day trash)
      the first layer: it recovers from the most common losses — user/app deletes and bugs —
      with no restore at all. Backups are the second layer and out-of-band validators the third.
    ref: "concepts.md#data-integrity-defense-in-depth-google-sre"

  - id: disaster-recovery-rpo-rto-strategies-069
    difficulty: advanced
    tags: [sre, validators, corruption]
    question: |
      What role do out-of-band data validators (the third SRE defense layer) play that backups
      and soft-delete do not?
    options:
      - "They continuously detect corruption / referential-integrity breaks (ideally within ~24h) so you learn about it before the corruption becomes your only surviving copy"
      - "They fully replace the need for replication by keeping a live consistency-checked copy"
      - "They guarantee an RPO of 0 by validating every write the instant it is committed"
      - "They physically air-gap the backups onto offline media as they scan each dataset"
    answer: 0
    explanation: |
      Validators are MapReduce-style jobs that scan for corruption and integrity violations out
      of band. Their value is early detection — catching creeping/gradual corruption before it
      overwrites all good copies and before it's too late to time-travel back to a clean state.
    ref: "concepts.md#data-integrity-defense-in-depth-google-sre"

  - id: disaster-recovery-rpo-rto-strategies-070
    difficulty: expert
    tags: [sre, data-loss-modes, hardest-case]
    question: |
      Google's review of real recoveries found which class of data loss the most common AND the
      hardest to recover from?
    options:
      - "Hardware faults causing a sudden big-bang loss of an entire storage array at once"
      - "Site catastrophes wiping out a whole region and every replica within it simultaneously"
      - "Software bugs causing deletion or referential-integrity loss — often discovered weeks/months later, requiring time-travel (PITR/long retention)"
      - "Operator error deleting a single file, immediately caught and trivially restored from trash"
    answer: 2
    explanation: |
      In SRE Ch. 26's taxonomy (root cause × scope × rate = 24 modes), software bugs causing
      creeping deletion / referential-integrity loss dominated and were hardest — they surface
      long after the fact, so you need long retention and PITR to travel back before the bug.
    ref: "concepts.md#data-integrity-defense-in-depth-google-sre"

  - id: disaster-recovery-rpo-rto-strategies-071
    difficulty: advanced
    tags: [sre, availability, integrity]
    question: |
      What does SRE mean by "availability is the goal, integrity is the means" in a DR context?
    options:
      - "Only raw uptime matters to the user, so the integrity of the underlying data can be ignored"
      - "Data that is preserved but inaccessible is, from the user's perspective, effectively lost — so integrity work exists ultimately to keep data available and usable"
      - "Data integrity should always outweigh availability in every single reliability trade-off you make"
      - "You should maximize replication factor regardless of the consistency guarantees it sacrifices"
    answer: 1
    explanation: |
      SRE reframes DR around the user: intact-but-unreachable data is as useless as lost data.
      Google treated Apps data unavailable > 24h as effectively lost. Integrity practices
      (backups, validators, soft-delete) serve the real objective — usable availability.
    ref: "concepts.md#data-integrity-defense-in-depth-google-sre"

  - id: disaster-recovery-rpo-rto-strategies-072
    difficulty: advanced
    tags: [replication-mechanics, aurora, numbers]
    question: |
      For Aurora Global Database, which set of figures is correct?
    options:
      - "Cross-region replication lag typically < 1 second, in-region replica lag < 100 ms, secondary promotion in < 1 minute"
      - "Cross-region replication is fully synchronous with exactly zero lag and no added write-latency cost"
      - "Cross-region lag is typically several minutes and promoting a secondary to primary takes hours"
      - "It offers only nightly cross-region snapshots, giving a worst-case RPO of roughly 24 hours"
    answer: 0
    explanation: |
      Aurora Global Database replicates cross-region asynchronously with typically sub-second
      lag (in-region replica lag < 100 ms) and can promote a secondary in under a minute even
      during a full regional outage. It is async, so RPO is the (small) lag, not zero.
    ref: "concepts.md#synchronous-vs-asynchronous-replication-mechanics"

  - id: disaster-recovery-rpo-rto-strategies-073
    difficulty: expert
    tags: [active-active, write-strategy, subtle-distinction]
    question: |
      An interviewer asks how you handle writes in an active/active multi-region system where
      each region must accept local writes with lowest latency and you accept last-writer-wins
      conflict handling. Which write strategy is that?
    options:
      - "Write-global (every write routed to a single designated primary region and fanned out from there)"
      - "Write-local (each region accepts local writes and they replicate both ways, e.g. DynamoDB global tables)"
      - "Write-partitioned (a partition key deterministically routes each record to its one owning region)"
      - "Synchronous quorum writes committed across a majority of regions before each write is acknowledged"
    answer: 1
    explanation: |
      Write-local lets each region accept its own writes and replicate bidirectionally (DynamoDB
      global tables, last-writer-wins) — lowest latency but you accept conflict resolution.
      Write-global centralizes writes (no conflicts, higher latency); write-partitioned routes
      by key so writes never collide across regions.
    ref: "concepts.md#synchronous-vs-asynchronous-replication-mechanics"

  - id: disaster-recovery-rpo-rto-strategies-074
    difficulty: expert
    tags: [gitlab-2017, incident, testing]
    question: |
      In the GitLab 2017 outage, backups to S3 were empty because pg_dump silently failed on a
      client/server version mismatch, and the alert emails about it were rejected. What single
      DR lesson does this most directly reinforce?
    options:
      - "Streaming replication to a hot standby alone is a complete DR strategy that makes backups redundant"
      - "Synchronous multi-AZ replication would have blocked and prevented the accidental directory deletion at the source"
      - "Provisioning larger, higher-memory database instances would have kept the outage from ever occurring"
      - "You must regularly test restores and monitor backup SUCCESS (not just runs) end-to-end, because backups fail silently — 'nobody was responsible for testing this procedure'"
    answer: 3
    explanation: |
      Five recovery methods failed and nobody knew because success wasn't verified. The lesson
      is that backups fail silently (version mismatch, empty bucket, unread alerts), so you must
      test restores and actively monitor backup validity — replication and bigger hardware are
      irrelevant to a logical delete + untested backups.
    ref: "concepts.md#canonical-incidents-gitlab-2017-and-google-music-2012"

  - id: disaster-recovery-rpo-rto-strategies-075
    difficulty: advanced
    tags: [google-music-2012, restore-scale, rto]
    question: |
      The 2012 Google Music restore recalled thousands of tapes and restored ~1.5 PB over nearly
      7 days. What general DR lesson does the scale of that restore illustrate?
    options:
      - "Tape backups are always an outdated mistake and should be replaced with object storage in every case"
      - "For very large datasets, restore TIME is the real RTO — processes that work at TB scale don't automatically scale to PB, and parallel sharding plus 'trust points' are needed"
      - "Soft deletion becomes entirely unnecessary once you keep an off-site tape copy of the full dataset"
      - "Live cross-region replication would have restored all 1.5 PB of the data instantly with zero downtime"
    answer: 1
    explanation: |
      Restoring 1.5 PB took ~7 days even with 5,475 parallel jobs — proving that for large data
      the dominant RTO factor is restore throughput, and TB-scale processes must be re-engineered
      (sharding, trust points) for PB scale. Soft deletion would have avoided most of the loss
      up front.
    ref: "concepts.md#canonical-incidents-gitlab-2017-and-google-music-2012"

  - id: disaster-recovery-rpo-rto-strategies-076
    difficulty: expert
    tags: [backup-validation, encryption-key, scenario]
    question: |
      Your backups exist and restored cleanly in last quarter's drill. Which of these could still
      make them useless in a real regional DR — and is the strongest example of decorrelated-
      failure-domain thinking?
    options:
      - "The backup file holds too many table rows for the recovery instance to load in one pass"
      - "The KMS key needed to decrypt the backup lives only in the failed region/account, so the backup cannot be decrypted during the event"
      - "The backup was compressed with gzip, which strips the referential integrity constraints on restore"
      - "The backup was taken during off-peak night hours, so it missed the day's most recent writes"
    answer: 1
    explanation: |
      A backup you cannot decrypt is not a backup. If the encryption key shares the primary's
      failure domain (same region/account) it dies with prod. Validating key availability and
      keeping keys/backups out of the primary's blast radius is core decorrelation thinking —
      alongside restore bandwidth vs RTO and control-plane-free restore paths.
    ref: "concepts.md#backup-validation-beyond-timing-the-restore"

  - id: disaster-recovery-rpo-rto-strategies-077
    difficulty: advanced
    tags: [backup-validation, isolation, safety]
    question: |
      Why should DR test restores be performed into an isolated account/VPC rather than against
      or near production?
    options:
      - "Restores into an isolated account complete faster because they skip the primary's IAM authorization checks"
      - "Because production database engines are locked read-only and physically cannot run any restore tooling"
      - "To cut the cross-region data-transfer bill, since intra-account restores are billed at a lower tier"
      - "To avoid a 'restore test' overwriting or disrupting production data and thereby causing the very incident you were guarding against"
    answer: 3
    explanation: |
      Restoring into or over production risks clobbering live data or destabilizing the system —
      turning a safety exercise into an outage. Isolated accounts/VPCs let you validate integrity,
      decryption, and timing end-to-end without any blast radius on prod.
    ref: "concepts.md#backup-validation-beyond-timing-the-restore"

  - id: disaster-recovery-rpo-rto-strategies-078
    difficulty: expert
    tags: [active-active, logical-failure, scenario]
    question: |
      A team runs three regions active/active with sub-second bidirectional replication and
      claims they no longer need point-in-time backups. A stored-procedure bug issues a bad mass
      UPDATE. What happens, and what's the correct posture?
    options:
      - "The bad UPDATE stays contained in its origin region while the other two stay clean, so backups really are unnecessary"
      - "The bad UPDATE replicates to all three regions in milliseconds; RPO for logical disasters is always > 0 (detection time) and you still need PITR/immutable backups"
      - "The active/active topology detects the anomalous write and automatically rolls it back across all regions"
      - "Expanding the deployment to five active regions would dilute and prevent the corruption from taking hold"
    answer: 1
    explanation: |
      Active/active replicates every write — including the bad UPDATE — everywhere near-instantly.
      No number of live regions protects against logical corruption; only historical/immutable
      backups and PITR can travel back before it. Logical-disaster RPO is bounded below by how
      long detection takes.
    ref: "concepts.md#replication-is-not-a-backup-data-corruption--logical-failures"
