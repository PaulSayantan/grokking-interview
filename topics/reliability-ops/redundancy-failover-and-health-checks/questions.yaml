topic: "Redundancy, Failover & Health Checks"
domain: reliability-ops
topic_slug: redundancy-failover-and-health-checks
version: 1
questions:
  - id: redundancy-failover-and-health-checks-001
    difficulty: beginner
    tags: [spof, fundamentals]
    question: |
      What is a "single point of failure" (SPOF)?
    options:
      - "The first component to be provisioned when bootstrapping a system, before any redundancy is added"
      - "A component whose failure takes down the whole system because there is no redundant path around it"
      - "A component that statistically fails more often than any other because it handles the most requests"
      - "A component that can only handle one request at a time, forcing all other traffic to queue behind it"
    answer: 1
    explanation: |
      A SPOF is any component with no redundant peer, so its failure has no
      fallback path and brings down the whole system. HA design works by asking,
      for each critical component, "what if exactly this dies?" and ensuring the
      answer is "traffic continues on a redundant peer."
    ref: "concepts.md#single-points-of-failure-spof"

  - id: redundancy-failover-and-health-checks-002
    difficulty: intermediate
    tags: [availability-math, series]
    question: |
      Two components each with 99.9% availability are placed in series (both must
      work for the system to work). What is the approximate combined availability?
    options:
      - "99.95%"
      - "99.9%"
      - "99.99%"
      - "99.8%"
    answer: 3
    explanation: |
      Series availabilities multiply: 0.999 × 0.999 ≈ 0.998 = 99.8%. Chaining
      dependencies makes the system LESS available than any single component — a
      key reason to minimize hard (in-series) dependencies.
    ref: "concepts.md#single-points-of-failure-spof"

  - id: redundancy-failover-and-health-checks-003
    difficulty: intermediate
    tags: [availability-math, redundancy, parallel]
    question: |
      Two independent nodes each with 99% availability are placed in parallel
      (either one suffices). What is the combined availability?
    options:
      - "99.9%"
      - "99%"
      - "98.01%"
      - "99.99%"
    answer: 3
    explanation: |
      Parallel/redundant availability = 1 − (1−A1)(1−A2) = 1 − (0.01)(0.01) =
      1 − 0.0001 = 99.99%. Redundancy roughly squares the failure probability
      (q → q²), which is why two cheap nodes beat one gold-plated node — but only
      if the failures are independent.
    ref: "concepts.md#single-points-of-failure-spof"

  - id: redundancy-failover-and-health-checks-004
    difficulty: advanced
    tags: [correlated-failure, failure-domains]
    question: |
      A team says "we have three replicas, so we tolerate two failures." Under what
      condition is this claim dangerously wrong?
    options:
      - "When all three replicas share a failure domain (same AZ, rack, power, or downstream dependency)"
      - "When the replicas run different software versions, since version drift breaks the tolerance math"
      - "When each replica opens its own dedicated database connection instead of sharing a single pool"
      - "When the replicas are spread across three different regions, adding cross-region latency overhead"
    answer: 0
    explanation: |
      The independence assumption behind the q² math breaks if replicas share fate.
      Three replicas in one AZ die together in an AZ outage; three that all call one
      downstream service share that dependency SPOF. Redundancy is only as good as
      the independence of the failure domains — always ask "what is the shared fate?"
    ref: "concepts.md#single-points-of-failure-spof"

  - id: redundancy-failover-and-health-checks-005
    difficulty: beginner
    tags: [redundancy, n-plus-1]
    question: |
      If N is the number of units needed to serve peak load, what does an "N+1"
      redundancy model provide?
    options:
      - "Two entirely separate full duplicate sets of the N capacity, running in parallel"
      - "Exactly enough capacity to meet peak with no spare unit held in reserve"
      - "One spare unit beyond the minimum, tolerating one concurrent failure"
      - "Enough capacity to serve twice the measured peak load at all times"
    answer: 2
    explanation: |
      N+1 provisions one spare beyond the N needed for peak, so it tolerates one
      unit failing — or lets you take one down for maintenance without dropping
      below N. It is the workhorse model for most production services.
    ref: "concepts.md#redundancy-n-n1-n2-2n"

  - id: redundancy-failover-and-health-checks-006
    difficulty: intermediate
    tags: [redundancy, n-plus-2, maintenance]
    question: |
      Why do highly critical systems often provision N+2 rather than N+1?
    options:
      - "N+2 eliminates the need for health checks because the extra spare self-monitors"
      - "So they can survive a failure even while one unit is already out for maintenance"
      - "N+2 works out cheaper than N+1 at scale by amortizing spare capacity across units"
      - "Because N+1 cannot actually serve peak load once overhead is subtracted"
    answer: 1
    explanation: |
      N+1 tolerates one failure, but if a unit is already down for patching, a
      second failure drops you below N. N+2 keeps N units serving even when one is
      out for maintenance AND another fails — critical for infrastructure like
      power and cooling.
    ref: "concepts.md#redundancy-n-n1-n2-2n"

  - id: redundancy-failover-and-health-checks-007
    difficulty: intermediate
    tags: [failure-domains, availability-zone]
    question: |
      In cloud architecture, what does an Availability Zone (AZ) primarily represent?
    options:
      - "A geographically distant region hundreds of miles away used purely for disaster recovery"
      - "A blast-radius boundary with independent power, cooling, and network within a region"
      - "A logical grouping of user accounts and IAM roles for billing and access control"
      - "A single physical server rack containing the primary and its hot standby"
    answer: 1
    explanation: |
      An AZ is the standard fault-isolation boundary in cloud: independent power,
      cooling, and network inside a region. Spreading replicas across 2–3 AZs
      tolerates an AZ event. Regions are the next-larger (geographic) domain, used
      for surviving whole-region outages.
    ref: "concepts.md#redundancy-n-n1-n2-2n"

  - id: redundancy-failover-and-health-checks-008
    difficulty: intermediate
    tags: [active-active, active-passive]
    question: |
      What is the key operational difference between active-active and active-passive
      redundancy?
    options:
      - "Active-active requires no data replication because each replica is fully independent"
      - "Active-active uses only one live replica while active-passive keeps many serving at once"
      - "Active-passive is always faster to fail over than active-active because the standby is pre-warmed"
      - "In active-active all replicas serve traffic simultaneously; in active-passive a standby waits idle and takes over only on failure"
    answer: 3
    explanation: |
      Active-active shares load across all replicas (failover is just
      rebalancing), while active-passive keeps standbys idle until the primary
      fails. Active-active gives near-instant failover but needs headroom and
      write-conflict handling; active-passive is simpler (single writer) but wastes
      idle capacity and takes time to detect and promote.
    ref: "concepts.md#active-active-vs-active-passive-standby-models"

  - id: redundancy-failover-and-health-checks-009
    difficulty: advanced
    tags: [active-active, capacity, headroom]
    question: |
      In a 2-node active-active setup that must survive one node failing, what is the
      maximum utilization each node can run at?
    options:
      - "About 90% — leaving only a thin safety buffer for spikes on each node"
      - "100% — both nodes are always available, so no headroom is ever needed"
      - "About 75% — reserving a quarter of each node for the failover surge"
      - "About 50% — so the survivor can absorb the other's load"
    answer: 3
    explanation: |
      To survive one of two nodes dying, the survivor must carry 100% of load, so
      each node can run at most ~50% (one of the two must be able to take the full
      share). Generally for N-node active-active surviving one failure, keep each
      node below (N−1)/N utilization. Ignoring this is a common way "redundant"
      systems still fall over.
    ref: "concepts.md#active-active-vs-active-passive-standby-models"

  - id: redundancy-failover-and-health-checks-010
    difficulty: intermediate
    tags: [standby, hot-warm-cold]
    question: |
      Ordering standby models from fastest failover (highest cost) to slowest
      failover (lowest cost), which is correct?
    options:
      - "Hot → Warm → Cold"
      - "Warm → Hot → Cold"
      - "Cold → Warm → Hot"
      - "Hot → Cold → Warm"
    answer: 0
    explanation: |
      Hot standby is running with data continuously replicated and ready to serve
      instantly (seconds, highest cost); warm is running but scaled-down / not
      serving (seconds to minutes); cold is not running and must be provisioned or
      restored from backup (minutes to hours, cheapest). The temperature trades cost
      against failover time (RTO).
    ref: "concepts.md#active-active-vs-active-passive-standby-models"

  - id: redundancy-failover-and-health-checks-011
    difficulty: intermediate
    tags: [standby, cold, rto]
    question: |
      A cold standby has the LONGEST failover time of the three temperatures. Why?
    options:
      - "It uses fully synchronous replication on every write, which throttles the primary"
      - "It is not running and must be provisioned or restored from backup before it can serve"
      - "It serves live traffic in parallel with the primary and must first be drained"
      - "It runs at higher CPU utilization than a hot standby, so it needs to cool down first"
    answer: 1
    explanation: |
      A cold standby is powered off / not provisioned; failover requires spinning it
      up and restoring data from backup, taking minutes to hours. That is exactly why
      it is the cheapest option — you pay almost nothing to keep it "ready."
    ref: "concepts.md#active-active-vs-active-passive-standby-models"

  - id: redundancy-failover-and-health-checks-012
    difficulty: beginner
    tags: [failover, definitions]
    question: |
      What is "failover"?
    options:
      - "Splitting one database into multiple shards to spread write load evenly"
      - "Promoting a standby (or shifting traffic to survivors) when the active component fails"
      - "The process of scaling a service down during off-peak hours to save cost"
      - "The act of restarting a crashed process in place on the same failed host"
    answer: 1
    explanation: |
      Failover is taking over the failed component's role — promoting a standby or
      redistributing to surviving active nodes. "Failback" is the later, optional
      return to the original once it recovers.
    ref: "concepts.md#failover-automatic-vs-manual-and-failback"

  - id: redundancy-failover-and-health-checks-013
    difficulty: intermediate
    tags: [failover, automatic, false-positive]
    question: |
      What is the main risk of fully AUTOMATIC failover?
    options:
      - "It always takes longer than manual failover because of the extra consensus round-trips"
      - "It requires no health checks or heartbeats since promotion is triggered on a fixed schedule"
      - "False positives — a transient blip triggers an unnecessary, disruptive failover, and split-brain if the 'failed' primary is actually alive"
      - "It cannot be used for stateless services, only for data-bearing primaries"
    answer: 2
    explanation: |
      Automatic failover is fast but risks acting on false positives (network jitter,
      GC pause) and, worse, split-brain if the old primary is alive but unreachable.
      That is why automatic failover needs quorum + fencing, and why high-blast-radius
      data-bearing failovers often keep a human in the loop.
    ref: "concepts.md#failover-automatic-vs-manual-and-failback"

  - id: redundancy-failover-and-health-checks-014
    difficulty: advanced
    tags: [failover, judgment, database]
    question: |
      For promoting a database primary across regions, which approach is generally
      the most defensible?
    options:
      - "Manual detection and manual promotion with no automation at all, relying entirely on an on-call human"
      - "Fully automatic promotion the instant one heartbeat is missed, with no quorum check first"
      - "Promote whichever replica currently has the most free CPU headroom, regardless of replication lag"
      - "Automatic detection plus quorum and fencing, often with human confirmation before promotion, and never auto-failback"
    answer: 3
    explanation: |
      High-blast-radius, data-bearing failover is where a wrong automatic decision
      can lose committed writes or cause split-brain. Best practice is automatic
      detection, quorum + fencing to be safe, and frequently a human confirmation
      step for promotion; failback should be manual and verified, never automatic.
    ref: "concepts.md#failover-automatic-vs-manual-and-failback"

  - id: redundancy-failover-and-health-checks-015
    difficulty: advanced
    tags: [failover, flapping, hysteresis]
    question: |
      A service's failover keeps oscillating: a latency spike triggers failover, the
      promotion causes a load spike that trips the new primary's health check, and it
      fails back — repeatedly. What is this and how do you fix it?
    options:
      - "Cascading failure; fix by making the health checks deeper so they probe every dependency"
      - "Split-brain; fix by adding more replicas until a stable majority always exists"
      - "A retry storm; fix by removing all client and server timeouts so requests never abort"
      - "Flapping; add hysteresis (require N consecutive failed checks), a cooldown, and never auto-failback"
    answer: 3
    explanation: |
      This is failover flapping caused by an over-sensitive detector plus automatic
      failback. Dampen it with hysteresis (require several consecutive failures before
      failing over), a cooldown period, and by never auto-failing-back — return to the
      original only after manual verification.
    ref: "concepts.md#failover-automatic-vs-manual-and-failback"

  - id: redundancy-failover-and-health-checks-016
    difficulty: intermediate
    tags: [failback, safety]
    question: |
      Why should failback (returning to the original primary after it recovers)
      generally NOT be automatic?
    options:
      - "Automatic failback is always slower than manual because it re-runs the full election protocol"
      - "It is a second failover event with its own risk — the recovered node may be stale or partially broken, and auto-failback can flap"
      - "Failback requires taking the whole system offline for a maintenance window to resync state"
      - "Failback is technically impossible once a failover to the standby has already occurred"
    answer: 1
    explanation: |
      Failback is a second promotion with the same risks as the first. The recovered
      node may still be partially broken or its data stale; auto-failing-back the
      instant it reappears invites flapping. Prefer manual, verified failback after
      confirming the node is fully caught up.
    ref: "concepts.md#failover-automatic-vs-manual-and-failback"

  - id: redundancy-failover-and-health-checks-017
    difficulty: intermediate
    tags: [split-brain, definitions]
    question: |
      What is "split-brain" in the context of automatic failover?
    options:
      - "A load balancer splitting incoming traffic evenly between two equally healthy nodes"
      - "A single node exhausting its memory across two separate worker processes at once"
      - "A network partition makes the standby promote itself while the original primary is still alive, so two primaries accept conflicting writes"
      - "A database sharded across two regions to keep each shard close to its users"
    answer: 2
    explanation: |
      Split-brain is the catastrophic case where a partition causes a standby to
      believe the primary is dead and promote itself, but the original primary is
      still alive and writing. Two primaries then diverge with conflicting writes —
      corruption that is extremely hard to reconcile.
    ref: "concepts.md#split-brain-quorum-and-fencingstonith"

  - id: redundancy-failover-and-health-checks-018
    difficulty: advanced
    tags: [split-brain, detection, partition]
    question: |
      Why can't timeouts alone reliably distinguish a dead primary from a live one,
      making split-brain hard to prevent?
    options:
      - "Because 'the primary is dead' and 'I can't reach the primary (a partition)' look identical to a timeout-based detector"
      - "Because clocks on different nodes are always perfectly synchronized via NTP, masking the failure"
      - "Because timeouts are always set far too short and expire before any real heartbeat can land"
      - "Because heartbeats are sent over TCP, which retransmits reliably and therefore never drops packets"
    answer: 0
    explanation: |
      A silence-based detector cannot tell whether the peer crashed or is merely
      unreachable due to a partition — both produce the same "no heartbeat" signal.
      That ambiguity is the root cause of split-brain and is why quorum and fencing,
      not just timeouts, are required.
    ref: "concepts.md#split-brain-quorum-and-fencingstonith"

  - id: redundancy-failover-and-health-checks-019
    difficulty: intermediate
    tags: [quorum, majority, odd-nodes]
    question: |
      Why do clusters that rely on quorum use an odd number of nodes (3, 5, 7)?
    options:
      - "Because the Raft and Paxos consensus protocols are mathematically unable to count or compare an even number of votes"
      - "So a network partition always has a strict majority on one side; an even count can split evenly with no majority"
      - "So that each node can be assigned ownership of exactly one shard of the keyspace, one shard per node"
      - "Because odd-numbered clusters consume noticeably less memory and CPU per node than even-numbered ones do at scale"
    answer: 1
    explanation: |
      Quorum requires a strict majority (⌊n/2⌋+1) to act. With an odd count, any
      partition leaves a decisive majority on one side; an even count can split
      evenly (e.g. 2–2) leaving neither side able to act. 3 nodes tolerate 1 failure,
      5 tolerate 2.
    ref: "concepts.md#split-brain-quorum-and-fencingstonith"

  - id: redundancy-failover-and-health-checks-020
    difficulty: advanced
    tags: [quorum, fault-tolerance, math]
    question: |
      A quorum-based cluster of 5 nodes can tolerate how many node failures while
      still forming a majority?
    options:
      - "1"
      - "3"
      - "4"
      - "2"
    answer: 3
    explanation: |
      Majority of 5 is 3 (⌊5/2⌋+1). Losing 2 leaves 3 — still a majority, so the
      cluster stays available. Losing 3 leaves only 2, which is a minority. In
      general a quorum cluster tolerates ⌊(n−1)/2⌋ failures.
    ref: "concepts.md#split-brain-quorum-and-fencingstonith"

  - id: redundancy-failover-and-health-checks-021
    difficulty: advanced
    tags: [fencing, stonith]
    question: |
      What does STONITH ("Shoot The Other Node In The Head") accomplish?
    options:
      - "It forcibly powers off / fences the old primary before promoting a new one, so a still-alive-but-unreachable primary cannot keep writing"
      - "It periodically restarts the current node to clear accumulated memory leaks and reset heap fragmentation before they degrade performance"
      - "It gracefully shuts down the load balancer during failover so no new connections arrive while the standby is being promoted"
      - "It synchronously duplicates the primary's entire dataset to every replica so all copies are byte-for-byte identical before a failover"
    answer: 0
    explanation: |
      Fencing/STONITH forcibly cuts off the old primary (power-cycle via IPMI, revoke
      storage access, block its network) BEFORE the new one takes over. This
      guarantees the old node is truly gone — not just unreachable — preventing the
      two-primaries split-brain scenario.
    ref: "concepts.md#split-brain-quorum-and-fencingstonith"

  - id: redundancy-failover-and-health-checks-022
    difficulty: advanced
    tags: [split-brain, witness, two-node]
    question: |
      You have a 2-node primary/standby pair. What is the simplest way to prevent a
      1–1 partition from causing split-brain?
    options:
      - "Let both nodes promote themselves immediately and reconcile the divergent writes afterward using last-writer-wins timestamps"
      - "Add a lightweight third witness (or a shared quorum disk) so a partition always has a majority side"
      - "Disable failover entirely and rely on a human operator to manually promote the standby during every outage"
      - "Shorten both nodes' heartbeat timeouts to 1ms so a partition is detected before either side can promote itself"
    answer: 1
    explanation: |
      With only two nodes a partition splits 1–1 and neither has a majority (or both
      claim it). Adding a third witness / tie-breaker (or a shared quorum disk) means
      one side always holds the majority and can safely act while the other stands
      down.
    ref: "concepts.md#split-brain-quorum-and-fencingstonith"

  - id: redundancy-failover-and-health-checks-023
    difficulty: beginner
    tags: [liveness, readiness, definitions]
    question: |
      In one sentence, what is the difference between a liveness and a readiness
      health check?
    options:
      - "They are two interchangeable names for the exact same check, and Kubernetes runs whichever one you happen to configure"
      - "Liveness probes verify that every downstream dependency is reachable, while readiness probes only sample local CPU utilization"
      - "Liveness failure means 'stop sending me traffic but keep me alive'; readiness failure means 'restart me because I am wedged'"
      - "Liveness failure means 'restart me if I'm broken'; readiness failure means 'stop sending me traffic but leave me running'"
    answer: 3
    explanation: |
      A failed liveness probe triggers a restart/replace of the instance; a failed
      readiness probe removes it from the load-balancer pool without killing it.
      Liveness answers "is the process dead?"; readiness answers "can I serve traffic
      right now?"
    ref: "concepts.md#liveness-vs-readiness-health-checks"

  - id: redundancy-failover-and-health-checks-024
    difficulty: intermediate
    tags: [liveness, anti-pattern, dependencies]
    question: |
      Why is it a bug to make a LIVENESS probe check a downstream dependency (like the
      database)?
    options:
      - "It forces the readiness probe to wait behind the liveness probe's connection pool, roughly doubling every probe's latency"
      - "A dependency blip makes liveness fail, so the orchestrator restarts every pod — but the restart doesn't fix the downstream, causing a fleet-wide crash-loop"
      - "It doubles the total number of health-check requests the pod must answer, exhausting its thread pool under normal load"
      - "Liveness probes are sandboxed by the kubelet and are technically forbidden from opening any outbound network connection"
    answer: 1
    explanation: |
      Liveness failure means "restart me." If liveness depends on a shared downstream,
      a brief downstream blip trips liveness on every pod, the orchestrator restarts
      them all, and since restarting doesn't fix the downstream they crash-loop
      forever. Liveness must check only the process itself.
    ref: "concepts.md#liveness-vs-readiness-health-checks"

  - id: redundancy-failover-and-health-checks-025
    difficulty: intermediate
    tags: [readiness, startup, load-balancer]
    question: |
      During slow startup (warming caches, loading a model), which probe should be
      failing, and why?
    options:
      - "Liveness — so the orchestrator keeps restarting it until the caches finish warming and the model finishes loading"
      - "Both liveness and readiness simultaneously, so the pod is both removed from the pool and restarted during startup"
      - "Neither probe — the instance should join the pool and accept live traffic immediately while caches warm in the background"
      - "Readiness (or a dedicated startup probe) — so the LB doesn't send traffic until the instance is actually ready, without killing it"
    answer: 3
    explanation: |
      During warm-up the instance is alive but not yet able to serve, so readiness
      should fail (keep it out of the LB pool) while liveness passes. Kubernetes adds
      a startup probe specifically so a slow boot doesn't trip liveness and cause a
      crash-loop.
    ref: "concepts.md#liveness-vs-readiness-health-checks"

  - id: redundancy-failover-and-health-checks-026
    difficulty: intermediate
    tags: [liveness, readiness, judgment]
    question: |
      When you are unsure whether a failing condition should trip liveness or
      readiness, which is the safer default and why?
    options:
      - "Readiness, because removing from the pool is reversible while restarting is not"
      - "Liveness, because a full process restart reliably clears any transient fault and always returns the instance to a good state"
      - "Neither — leave both probes passing and let the load balancer's own outlier detection eject the instance instead"
      - "Whichever probe happens to have the shorter configured timeout value, since it will react to the condition first"
    answer: 0
    explanation: |
      Pulling an instance from the LB pool (readiness) is easily reversible — it
      rejoins when healthy. Restarting (liveness) throws away in-flight work and can
      crash-loop. When in doubt, prefer readiness; reserve liveness for genuine
      "I'm wedged and only a restart fixes it" cases.
    ref: "concepts.md#liveness-vs-readiness-health-checks"

  - id: redundancy-failover-and-health-checks-027
    difficulty: beginner
    tags: [shallow, deep, definitions]
    question: |
      What distinguishes a "deep" health check from a "shallow" one?
    options:
      - "A shallow check is served over HTTPS on the main port while a deep check is served over plain HTTP on a debug port"
      - "A deep check simply runs on a much more frequent polling interval than a shallow one, otherwise probing the same thing"
      - "A deep check also probes the instance's dependencies (DB, cache, downstreams), while a shallow check verifies only the local process responds"
      - "A deep check must be exposed on a separate dedicated port so the orchestrator can distinguish it from the shallow check"
    answer: 2
    explanation: |
      A shallow check verifies only that the local process is up (e.g. /healthz
      returns 200). A deep check additionally verifies dependencies are reachable and
      reports unhealthy if any are down. Deep checks sound better but carry the
      cascading-failure anti-pattern.
    ref: "concepts.md#shallow-vs-deep-health-checks-the-deep-check-anti-pattern"

  - id: redundancy-failover-and-health-checks-028
    difficulty: advanced
    tags: [deep-check, anti-pattern, cascading-failure]
    question: |
      Every instance runs a DEEP health check against a shared database, wired
      directly to the load balancer. The DB has a 5-second blip. What happens?
    options:
      - "Nothing — deep checks include built-in blip suppression that ignores any dependency failure shorter than ten seconds"
      - "Every instance reports unhealthy simultaneously, the LB removes all of them, and a minor blip becomes a total correlated outage"
      - "Only one randomly chosen instance is removed from the pool while all the rest keep serving traffic normally"
      - "The load balancer detects the failure and automatically provisions and promotes a replacement database instance"
    answer: 1
    explanation: |
      This is the deep-check anti-pattern. Because all instances share the dependency,
      a single blip fails every instance's check at once; the LB removes the entire
      fleet and a minor hiccup becomes a full outage. Ironically, without the deep
      check the blip would have self-recovered. Keep LB checks shallow.
    ref: "concepts.md#shallow-vs-deep-health-checks-the-deep-check-anti-pattern"

  - id: redundancy-failover-and-health-checks-029
    difficulty: advanced
    tags: [deep-check, best-practice, load-balancer]
    question: |
      What is the recommended way to still monitor deep dependency health WITHOUT
      risking the whole fleet being pulled from the pool?
    options:
      - "Keep the LB health check shallow, and run deep checks out of band (a separate monitor/alarm) that a human or slow controller reacts to"
      - "Wire the deep dependency check directly into the load balancer's remove-from-pool decision so unhealthy nodes leave instantly"
      - "Make every instance restart itself the moment the shared dependency becomes unreachable so it reconnects with a clean state"
      - "Run the same deep dependency check roughly ten times more often so transient failures are caught and cleared faster"
    answer: 0
    explanation: |
      Keep the load-balancer check shallow so a shared-dependency blip can't empty the
      pool. Do deep dependency checks out of band — as monitoring/alarms — decoupled
      from the LB's traffic-routing decision, so humans or a slow controller respond
      rather than instant fleet removal.
    ref: "concepts.md#shallow-vs-deep-health-checks-the-deep-check-anti-pattern"

  - id: redundancy-failover-and-health-checks-030
    difficulty: intermediate
    tags: [health-check, load-balancer, mechanism]
    question: |
      What does a load balancer do with a backend that fails its health check?
    options:
      - "Gradually increases the share of traffic sent to it to probe whether it has recovered under real load"
      - "Issues a restart command to the failing backend process and waits for it to come back before routing again"
      - "Removes it from the pool and routes traffic only to healthy backends"
      - "Permanently deletes and deregisters the backend so it can never rejoin the pool without manual re-registration"
    answer: 2
    explanation: |
      The LB continuously health-checks backends and removes any that fail, routing
      only to healthy ones. This is the core mechanism that makes redundancy
      automatic — but it can create a death spiral if it removes healthy-but-busy
      nodes under load.
    ref: "concepts.md#health-check-driven-lb-removal-and-the-death-spiral"

  - id: redundancy-failover-and-health-checks-031
    difficulty: advanced
    tags: [death-spiral, cascading-failure]
    question: |
      Describe the "health-check death spiral" under high load.
    options:
      - "Nodes restart at random intervals because of a firmware bug in the load balancer's health-check scheduler under high load"
      - "Health checks succeed far too often under load, so the LB never notices and never removes genuinely broken backend nodes"
      - "The database begins rejecting the incoming health-check queries, which acquire conflicting row locks and cause a deadlock"
      - "Overload makes health checks time out, the LB removes those nodes, the same load hits fewer nodes, their checks time out too, and the fleet collapses to zero"
    answer: 3
    explanation: |
      Under load, response times rise and health checks (sharing the same overloaded
      resources) time out. The LB removes those nodes, concentrating the same load on
      fewer survivors, which pushes them over too — a positive-feedback loop that
      empties the pool even though the boxes were still serving real traffic.
    ref: "concepts.md#health-check-driven-lb-removal-and-the-death-spiral"

  - id: redundancy-failover-and-health-checks-032
    difficulty: advanced
    tags: [fail-open, death-spiral, best-practice]
    question: |
      What is "fail-open" behavior at the load balancer, and why does it help against
      the death spiral?
    options:
      - "The LB opens a larger pool of persistent connections to each backend so it can push more concurrent requests through under load"
      - "The LB opens up the security-group firewall rules to allow external internet traffic to reach the backends directly during overload"
      - "If too many backends report unhealthy at once, the LB stops removing them and routes to all — assuming the health check, not the fleet, is wrong"
      - "The LB marks the current deployment as failed and automatically triggers a rollback to the previously healthy version"
    answer: 2
    explanation: |
      Fail-open means: when an implausibly large fraction of backends look unhealthy
      simultaneously, trust the fleet over the health check and keep routing to
      everyone. This breaks the death-spiral feedback loop because mass simultaneous
      "unhealthy" almost always indicates a check/overload problem, not that every box
      truly died. AWS ELB does this.
    ref: "concepts.md#health-check-driven-lb-removal-and-the-death-spiral"

  - id: redundancy-failover-and-health-checks-033
    difficulty: advanced
    tags: [death-spiral, mitigation, resources]
    question: |
      Which mitigation does NOT help prevent a health-check death spiral?
    options:
      - "Making the health check deeper and more aggressive so it detects overload sooner"
      - "Giving health checks a separate, reserved resource path and a generous timeout"
      - "Failing open when too many backends look unhealthy at once"
      - "Requiring multiple consecutive failures before removal (hysteresis)"
    answer: 0
    explanation: |
      Making the check deeper and more aggressive makes the spiral WORSE — it fails
      faster under load and removes nodes sooner, accelerating collapse. The real
      mitigations are reserved resources + generous timeouts, hysteresis, capped
      removal rate, load shedding, and fail-open.
    ref: "concepts.md#health-check-driven-lb-removal-and-the-death-spiral"

  - id: redundancy-failover-and-health-checks-034
    difficulty: beginner
    tags: [heartbeat, failure-detection]
    question: |
      What is a "heartbeat" in failure detection?
    options:
      - "A retry request automatically resent to the same node after an earlier request to it failed or timed out"
      - "The internal CPU clock frequency of a server, measured in gigahertz, that paces how fast it processes work"
      - "A periodic 'I'm alive' signal a node sends; if none arrives within a timeout, the node is suspected dead"
      - "An incremental data backup that the node captures and ships to durable storage once every single second"
    answer: 2
    explanation: |
      A heartbeat is a periodic liveness ping between nodes. If a peer stops receiving
      heartbeats within a configured timeout, it suspects the sender has failed. It is
      the universal building block of failure detection that feeds failover decisions.
    ref: "concepts.md#heartbeats-and-failure-detection"

  - id: redundancy-failover-and-health-checks-035
    difficulty: intermediate
    tags: [failure-detection, timeout, tradeoff]
    question: |
      With a fixed-timeout failure detector, what is the fundamental trade-off in
      choosing the timeout value?
    options:
      - "The timeout value only changes how verbose the failure-detector's log output is and has no effect on detection behavior"
      - "Longer timeouts consume proportionally more heap memory per tracked peer, while shorter timeouts keep the detector lightweight"
      - "A short timeout detects failures fast but produces many false positives; a long timeout is robust but slow to detect real failures"
      - "There is no real trade-off — a shorter timeout is strictly better because it always detects failures sooner with no downside"
    answer: 2
    explanation: |
      A short timeout reacts quickly but misreads GC pauses and jitter as death
      (false positives → needless failover); a long timeout avoids false positives
      but leaves real failures undetected longer (longer outage). A static threshold
      can't optimize both — you pick one point on the curve.
    ref: "concepts.md#heartbeats-and-failure-detection"

  - id: redundancy-failover-and-health-checks-036
    difficulty: advanced
    tags: [phi-accrual, failure-detection, adaptive]
    question: |
      How does a phi-accrual (φ) failure detector differ from a fixed-timeout one?
    options:
      - "It outputs a continuous suspicion level computed from the statistical distribution of recent heartbeat arrival times, adapting to actual network conditions"
      - "It requires every node to carry a GPS-synchronized hardware clock so heartbeat arrival times can be compared on a shared absolute timeline"
      - "It never reports a node as fully dead and instead keeps every peer permanently in a 'suspected' state until an operator intervenes"
      - "It applies a single fixed 30-second timeout uniformly to every node in the cluster regardless of that link's observed latency"
    answer: 0
    explanation: |
      Phi-accrual (used in Cassandra, Akka) outputs a suspicion value φ derived from
      the historical distribution of heartbeat inter-arrival times, rather than a
      boolean. The app picks a φ threshold (e.g. 8 ≈ 10⁻⁸ chance of being wrong). Its
      strength is adapting to real conditions — a jittery link gets more slack
      automatically, cutting false positives without hard-coded timeouts.
    ref: "concepts.md#heartbeats-and-failure-detection"

  - id: redundancy-failover-and-health-checks-037
    difficulty: intermediate
    tags: [failure-detection, false-positive, flapping]
    question: |
      A too-sensitive failure detector most directly causes which downstream problem?
    options:
      - "Larger backups, because each missed heartbeat forces an extra full snapshot to disk"
      - "Failover flapping — needless promotions and oscillation triggered by transient blips"
      - "Reduced network bandwidth from the detector flooding peers with extra probe packets"
      - "Slower CPUs, since the detector pins a core in tight heartbeat-timeout loops"
    answer: 1
    explanation: |
      Failure detection feeds the inputs to failover. If the detector is too twitchy,
      it declares healthy nodes dead on brief blips, triggering unnecessary failovers
      that can oscillate (flap). This is why detection sensitivity and failover
      hysteresis must be tuned together.
    ref: "concepts.md#heartbeats-and-failure-detection"

  - id: redundancy-failover-and-health-checks-038
    difficulty: beginner
    tags: [graceful-shutdown, sigterm]
    question: |
      What signal does an orchestrator like Kubernetes send FIRST to ask a container
      to shut down gracefully?
    options:
      - "SIGTERM"
      - "SIGHUP"
      - "SIGSTOP"
      - "SIGKILL"
    answer: 0
    explanation: |
      SIGTERM is the polite "please stop" signal, giving the app a chance to drain and
      clean up. SIGKILL (which cannot be caught) is sent only if the app fails to exit
      within the grace period, forcing it down and dropping in-flight work.
    ref: "concepts.md#graceful-shutdown-and-connection-draining"

  - id: redundancy-failover-and-health-checks-039
    difficulty: intermediate
    tags: [graceful-shutdown, readiness, draining]
    question: |
      When an app receives SIGTERM, why should it fail its READINESS check (rather
      than liveness) as the first step of graceful shutdown?
    options:
      - "Failing readiness tells the LB to stop sending new requests while the process keeps running to finish in-flight ones"
      - "Because failing liveness drains in-flight connections faster than failing readiness ever could"
      - "Because a failed readiness check automatically extends the pod's termination grace period"
      - "It makes no real difference which probe you fail, since the orchestrator drains connections either way"
    answer: 0
    explanation: |
      Readiness governs pool membership. Failing readiness stops NEW traffic while the
      instance keeps running to complete in-flight requests and clean up. Failing
      liveness would instead ask for a restart — the opposite of a clean drain.
    ref: "concepts.md#graceful-shutdown-and-connection-draining"

  - id: redundancy-failover-and-health-checks-040
    difficulty: advanced
    tags: [graceful-shutdown, race, prestop]
    question: |
      During rolling deploys, clients get connection-refused errors even though the
      app handles SIGTERM. What is the likely cause and fix?
    options:
      - "The backing database is down; adding a deep health check that probes it on every request will surface and clear the refused connections"
      - "The termination grace period is far too long; shortening it to about 1 second forces the pod to exit before the balancer can misroute to it"
      - "A race: the LB still routes to the pod briefly after SIGTERM. Fix by failing readiness and pausing (preStop hook) before closing the listener, so the LB drains it first"
      - "The orchestrator is sending SIGKILL instead of SIGTERM, so the app never runs its handler and cannot begin draining cleanly"
    answer: 2
    explanation: |
      There is a window after SIGTERM where the LB's health-check cache is stale and
      it still routes to the pod. If the app closes its listener immediately, those
      requests get refused. The fix: on SIGTERM, fail readiness and sleep briefly (a
      preStop hook) so the LB observes "unready" and stops routing before you close
      the socket.
    ref: "concepts.md#graceful-shutdown-and-connection-draining"

  - id: redundancy-failover-and-health-checks-041
    difficulty: intermediate
    tags: [graceful-shutdown, grace-period, kubernetes]
    question: |
      What is the default Kubernetes terminationGracePeriodSeconds, after which a pod
      that hasn't exited is sent SIGKILL?
    options:
      - "5 seconds"
      - "30 seconds"
      - "120 seconds"
      - "600 seconds"
    answer: 1
    explanation: |
      Kubernetes defaults terminationGracePeriodSeconds to 30 seconds. If the app
      hasn't exited by then it receives SIGKILL and is forced down, losing anything
      still in flight. Size the grace period to your longest (p99) request duration
      plus drain time.
    ref: "concepts.md#graceful-shutdown-and-connection-draining"

  - id: redundancy-failover-and-health-checks-042
    difficulty: advanced
    tags: [graceful-shutdown, sigterm, pid1]
    question: |
      An app is dropping requests on every deploy and always gets SIGKILLed after the
      grace period. What is a common root cause?
    options:
      - "The termination grace period is set far too high, so the kubelet gives up waiting and force-kills the pod early"
      - "The load balancer is failing open under load and pushing extra traffic onto the pod as it tries to exit"
      - "The app ignores SIGTERM (e.g. a shell wrapper or PID 1 that doesn't forward signals), so it never starts draining"
      - "The deployment runs too many replicas, so the rolling update kills them faster than connections can drain"
    answer: 2
    explanation: |
      If the process never receives/handles SIGTERM — common when a shell script is
      PID 1 and doesn't forward signals to the child — the app can't begin graceful
      shutdown, so it runs until the grace period expires and gets SIGKILLed every
      time, dropping in-flight requests on each deploy.
    ref: "concepts.md#graceful-shutdown-and-connection-draining"

  - id: redundancy-failover-and-health-checks-043
    difficulty: intermediate
    tags: [graceful-shutdown, grace-period, sizing]
    question: |
      Why can setting the termination grace period SHORTER than your longest request
      duration cause problems?
    options:
      - "It forces the load balancer into fail-open mode, routing traffic to the draining pod"
      - "It makes the pod's startup noticeably slower on the next scheduling cycle"
      - "It silently disables the pod's readiness probes for the duration of the shutdown"
      - "Long-running requests get SIGKILLed mid-flight before they can complete during drain"
    answer: 3
    explanation: |
      Draining lets in-flight requests finish, but only until the grace period
      expires. If a request takes longer than the grace period, it gets cut off by
      SIGKILL. Size the grace period to at least your p99 request duration plus drain
      overhead.
    ref: "concepts.md#graceful-shutdown-and-connection-draining"

  - id: redundancy-failover-and-health-checks-044
    difficulty: advanced
    tags: [scenario, redundancy, failure-domains]
    question: |
      You must place 3 database replicas for maximum resilience to an AZ outage in a
      region with 3 AZs. What is the best placement?
    options:
      - "One replica in each of the 3 AZs"
      - "All 3 in one AZ for lowest latency"
      - "All 3 in a single rack in one AZ"
      - "2 in one AZ, 1 in another"
    answer: 0
    explanation: |
      Spreading one replica per AZ means an AZ outage removes at most one replica,
      leaving a 2-of-3 majority (quorum) intact for continued writes. Concentrating
      replicas in one or two AZs creates a correlated failure domain that an AZ event
      can take out entirely.
    ref: "concepts.md#redundancy-n-n1-n2-2n"

  - id: redundancy-failover-and-health-checks-045
    difficulty: advanced
    tags: [scenario, hot-standby, rpo]
    question: |
      A payments system requires the smallest possible data loss and fastest failover,
      cost being secondary. Which standby model fits best?
    options:
      - "Warm standby scaled to zero, spun up and populated from an async replica only once failure is detected"
      - "Hot standby with synchronous replication, ready to serve in seconds"
      - "No standby at all — rely on aggressive client retries and idempotency to paper over the primary's outages"
      - "Cold standby restored from nightly backups, accepting up to a day of data loss to keep costs minimal"
    answer: 1
    explanation: |
      Minimal data loss + fast failover points to a hot standby with synchronous
      replication (near-zero RPO, seconds RTO), accepting the higher cost. Cold/warm
      trade recovery time and data currency for cost, which is wrong when data loss
      must be minimized. See disaster-recovery-rpo-rto-strategies for the RPO/RTO math.
    ref: "concepts.md#active-active-vs-active-passive-standby-models"

  - id: redundancy-failover-and-health-checks-046
    difficulty: advanced
    tags: [scenario, load-shedding, health-check]
    question: |
      Under a traffic surge, a fleet is near capacity and health checks are starting
      to time out. Beyond fail-open, which action best protects availability?
    options:
      - "Shed excess real load (reject/queue low-priority requests) so nodes stay responsive enough to pass health checks and keep serving"
      - "Make the health checks deeper so they probe every downstream and detect the surge sooner across the fleet"
      - "Immediately remove every busy node from the pool so only the least-loaded instances keep receiving requests"
      - "Lower the termination grace period so overloaded nodes restart faster and rejoin with freshly reset connection pools"
    answer: 0
    explanation: |
      Load shedding drops or defers excess/low-priority work so the fleet stays within
      capacity and health checks keep passing — protecting the system rather than
      letting overload trigger a death spiral. See
      reliability-ops/load-shedding-and-backpressure and design-rate-limiter for the
      mechanisms.
    ref: "concepts.md#health-check-driven-lb-removal-and-the-death-spiral"

  - id: redundancy-failover-and-health-checks-047
    difficulty: intermediate
    tags: [hidden-spof, load-balancer]
    question: |
      A design has many redundant app servers, all behind a SINGLE load balancer.
      Where is the SPOF?
    options:
      - "The clients themselves, since they all resolve the same hostname and cache one IP address"
      - "There is no SPOF, because the redundant app-server tier already removes every failure point"
      - "The load balancer itself — it needs a redundant pair or DNS/anycast-level failover"
      - "The app servers, whichever one currently holds the most active keep-alive connections"
    answer: 2
    explanation: |
      Redundant app servers behind one LB just move the SPOF to the LB. If it dies,
      all traffic stops regardless of how many app servers survive. Fix with a
      redundant LB pair, anycast, or DNS-level failover. Interviewers love this
      "hidden SPOF" question.
    ref: "concepts.md#single-points-of-failure-spof"

  - id: redundancy-failover-and-health-checks-048
    difficulty: expert
    tags: [scenario, split-brain, quorum, judgment]
    question: |
      A 2-node primary/standby database has NO fencing and NO witness. A network
      partition isolates the two nodes from each other but both stay up and reachable
      by different clients. With automatic failover enabled, what is the most likely
      outcome?
    options:
      - "The standby correctly detects the partition, waits for the primary to reappear, and no harm occurs at all"
      - "Split-brain: the standby promotes itself while the primary keeps writing, so both accept conflicting writes and data diverges"
      - "The load balancer transparently merges the two divergent datasets back together once the partition heals"
      - "Both nodes recognize the ambiguity and shut themselves down safely until an operator intervenes"
    answer: 1
    explanation: |
      With no quorum majority possible (1–1 split), no witness to break the tie, and
      no fencing to stop the old primary, automatic failover promotes the standby
      while the original primary is still serving its side — two primaries, divergent
      writes, corruption. This is exactly why quorum + fencing + an odd/witness node
      are mandatory for safe automatic failover.
    ref: "concepts.md#split-brain-quorum-and-fencingstonith"

  - id: redundancy-failover-and-health-checks-049
    difficulty: expert
    tags: [scenario, deep-check, judgment, degradation]
    question: |
      Your service can serve mostly-cached responses even when its database is briefly
      unavailable. How should its health check treat the database dependency?
    options:
      - "Make the DB reachability check part of the liveness probe so the pod restarts and reconnects on any blip"
      - "Treat the DB as a non-critical dependency for health purposes and keep serving degraded/cached responses (fail-open) instead of declaring itself dead"
      - "Report unhealthy whenever the DB is unreachable, so the load balancer pulls the instance from the pool immediately"
      - "Restart the pod whenever the DB is unreachable, forcing a fresh connection pool on every dependency hiccup"
    answer: 1
    explanation: |
      Since the service can still do useful work without the DB, a DB blip should not
      make it declare itself dead — that would trigger the deep-check cascading-failure
      pattern and pull the whole fleet. Isolate the non-critical dependency from the
      served-traffic health signal and degrade gracefully. See
      reliability-ops/graceful-degradation-and-fallbacks.
    ref: "concepts.md#shallow-vs-deep-health-checks-the-deep-check-anti-pattern"

  - id: redundancy-failover-and-health-checks-050
    difficulty: intermediate
    tags: [active-active, write-conflicts]
    question: |
      What additional challenge does active-active introduce that active-passive
      (single-writer) largely avoids?
    options:
      - "Longer failover time, since every active replica must be drained before traffic can rebalance"
      - "Higher idle cost, because each active replica must reserve a matching passive standby of its own"
      - "Handling concurrent/conflicting writes across replicas (multi-primary conflict resolution, CRDTs, or partitioned ownership)"
      - "An inability to serve read traffic while all replicas are busy coordinating write ordering"
    answer: 2
    explanation: |
      With all replicas serving (including writes), active-active must resolve
      concurrent conflicting writes — via multi-primary databases, CRDTs, or
      partitioning write ownership. Active-passive sidesteps this because only the
      single active primary writes, giving simpler consistency at the cost of idle
      standby capacity.
    ref: "concepts.md#active-active-vs-active-passive-standby-models"

  - id: redundancy-failover-and-health-checks-051
    difficulty: expert
    tags: [gray-failure, differential-observability, scenario]
    question: |
      A single instance has a failing NIC dropping ~5% of packets. Its /healthz probe
      returns 200 in under a millisecond and fleet-average metrics look normal, yet a
      subset of clients see timeouts and elevated errors. What is this, and why did the
      health check miss it?
    options:
      - "Gray failure: the instance's self-report only tests the cheap local path, so it reports healthy while clients experience the degraded path"
      - "A deep-check cascade: the probe was too shallow to notice the database was slow, so the node kept serving corrupted responses"
      - "Split-brain: two instances have both promoted themselves and are now serving conflicting writes for the same partition"
      - "A death spiral: the load balancer removed too many overloaded nodes at once and concentrated all traffic onto the survivor"
    answer: 0
    explanation: |
      This is gray failure — a partial degradation that the component's own health
      check cannot see because its self-view says "fine." Differential observability
      captures it: the system says healthy, aggregate metrics dilute the pain, but
      clients see errors. Self-report probes are structurally blind to gray failure.
    ref: "concepts.md#gray-failure-and-differential-observability"

  - id: redundancy-failover-and-health-checks-052
    difficulty: expert
    tags: [gray-failure, evacuation, judgment]
    question: |
      One AZ is showing client-observed latency and error degradation, but you can't
      yet pinpoint which component is at fault. What is the best first action?
    options:
      - "Deploy a more aggressive deep health check to every instance in that AZ so the exact fault surfaces sooner"
      - "Wait until monitoring pinpoints the precise failing instance or component before taking any traffic action"
      - "Restart every instance in the AZ in a rolling wave to clear whatever transient condition is degrading them"
      - "Evacuate traffic away from the AZ first (e.g. zonal shift), then root-cause at leisure"
    answer: 3
    explanation: |
      For gray failure the rule is "evacuate, don't diagnose." Shifting traffic away
      from the impaired fault domain is fast and reversible, whereas pinpointing the
      exact degraded component can take far longer than the outage should. Diagnose
      after you've stopped the customer pain — this is exactly what zonal shift is for.
    ref: "concepts.md#gray-failure-and-differential-observability"

  - id: redundancy-failover-and-health-checks-053
    difficulty: advanced
    tags: [gray-failure, observability, sli]
    question: |
      Which monitoring signal is MOST likely to catch a gray failure that per-instance
      self-health-checks miss?
    options:
      - "Client-observed success rate and latency (client-side SLIs / full-path canaries)"
      - "The instance's own /healthz endpoint, polled at a much higher frequency to catch brief dips"
      - "CPU utilization averaged across the whole fleet and alarmed when the mean drifts upward"
      - "The count of running worker processes on each host, checked against the expected baseline"
    answer: 0
    explanation: |
      Gray failure lives in the gap between what a component says about itself and what
      clients actually experience. Instrumenting success/latency from the caller's
      perspective (and comparing each node against its peers for outliers) surfaces the
      degradation that any self-report — however frequent — will keep reporting as healthy.
    ref: "concepts.md#gray-failure-and-differential-observability"

  - id: redundancy-failover-and-health-checks-054
    difficulty: expert
    tags: [fencing-token, distributed-lock, gc-pause]
    question: |
      A client acquires a lock lease, then suffers a GC pause longer than the lease TTL.
      The lock service grants the lock to a second client. The first client wakes up and
      writes, corrupting data despite "holding" the lock. What mechanism prevents this?
    options:
      - "A monotonic fencing token attached to every write, which the storage layer rejects if lower than the highest token already seen"
      - "A shorter lease TTL, set well below any plausible GC pause so the lease can never outlive the client's stall"
      - "STONITH forcibly power-cycling the paused client through IPMI the moment its lease is reassigned"
      - "Retrying the conflicting write with exponential backoff and jitter until the newer lock holder releases it"
    answer: 0
    explanation: |
      A lease is just a timeout guess; a long GC pause violates it, producing two lock
      holders. The fix is a strictly-increasing fencing token issued per grant. The
      storage server remembers the highest token and rejects the woken-up client's stale
      (lower) token — the resource, not the lock, is the final arbiter. Shortening the
      TTL only makes the race MORE likely.
    ref: "concepts.md#fencing-tokens-vs-stonith"

  - id: redundancy-failover-and-health-checks-055
    difficulty: advanced
    tags: [fencing-token, stonith, distinction]
    question: |
      What is the key difference between STONITH and a fencing token?
    options:
      - "They are two names for the same technique, both meaning power-cycling a failed node via IPMI so it can never write again"
      - "STONITH fences at the node level (kill/power-off the box); a fencing token fences at the resource level (the downstream rejects stale writers)"
      - "STONITH is only for stateless services, whereas fencing tokens exist purely to steer load balancers away from unhealthy backend targets"
      - "A fencing token gracefully restarts the old primary in place, while STONITH is what actually promotes and warms up the newly elected primary"
    answer: 1
    explanation: |
      STONITH prevents a NODE from acting by forcibly killing it (needs out-of-band
      power/IPMI). A fencing token prevents a stale WRITE from landing by having the
      resource server reject old tokens — it works even when you can't kill the sender.
      They are complementary: node-level vs resource-level fencing.
    ref: "concepts.md#fencing-tokens-vs-stonith"

  - id: redundancy-failover-and-health-checks-056
    difficulty: expert
    tags: [redlock, fencing-token, distributed-lock]
    question: |
      Kleppmann's central critique of the Redlock algorithm for correctness-critical
      locking is that it:
    options:
      - "Spreads the lock across too many Redis nodes, so the majority-acquire round trips add unacceptable per-lock latency and cost at scale"
      - "Cannot support more than one competing client at a time, so it collapses to a single-writer lock with no real contention handling"
      - "Requires GPS- or atomic-clock-synchronized time on every participating node, which is infeasible to guarantee across commodity hardware"
      - "Provides mutual exclusion but issues no fencing token, so it cannot defend against GC pauses or clock skew that outlast a lease"
    answer: 3
    explanation: |
      Redlock gives mutual exclusion under timing assumptions but hands out no monotonic
      fencing token, so it has no defense against a paused/skewed client that wakes up
      and writes after its lease expired. Correctness that depends on timing guarantees
      isn't safe; you need a token the storage layer enforces.
    ref: "concepts.md#fencing-tokens-vs-stonith"

  - id: redundancy-failover-and-health-checks-057
    difficulty: expert
    tags: [static-stability, control-plane, scenario]
    question: |
      Your AZ-failure runbook says "when an AZ fails, auto-scale the surviving AZs to
      cover the load." Why is this fragile at staff level?
    options:
      - "Auto-scaling is strictly cheaper than pre-provisioning headroom, so a reactive scale-out plan carries no downside worth engineering around"
      - "Auto-scaling groups are hard-partitioned per AZ and cannot launch replacement capacity into a different AZ from the one that failed"
      - "It depends on the control plane (launch APIs, spare capacity, warm-up) exactly when that control plane is most likely impaired and capacity is contended"
      - "Health checks are automatically suspended for the duration of any scaling event, so the new instances silently join the pool while still broken"
    answer: 2
    explanation: |
      Reacting to a failure by scaling puts a control-plane dependency on your critical
      recovery path at the worst moment — during a large correlated event when launch
      APIs may be degraded and everyone is contending for capacity. Static stability
      says pre-provision the survivors so failover is a data-plane action needing no
      changes.
    ref: "concepts.md#static-stability"

  - id: redundancy-failover-and-health-checks-058
    difficulty: advanced
    tags: [static-stability, headroom, capacity]
    question: |
      To be statically stable across 3 AZs (survive losing any one AZ with no scaling),
      at roughly what per-AZ utilization should you run in steady state?
    options:
      - "100% — the surviving AZs will reactively auto-scale to absorb the failed AZ the moment it drops out"
      - "≤ ~33% (1/3), so that any single surviving AZ alone can carry the entire fleet's load by itself"
      - "~90%, leaving only a thin per-AZ buffer since correlated whole-AZ failures are too rare to provision against"
      - "≤ ~67% (2/3), so the two surviving AZs already have capacity for the full load"
    answer: 3
    explanation: |
      Losing 1 of 3 AZs leaves 2 to carry 100%, so each AZ must run at ≤ (N−k)/N = 2/3
      ≈ 67% to absorb the failed AZ using capacity it already has — no control-plane
      scaling required. The deliberate cost is idle headroom in steady state.
    ref: "concepts.md#static-stability"

  - id: redundancy-failover-and-health-checks-059
    difficulty: intermediate
    tags: [static-stability, definition]
    question: |
      What does "static stability" mean for a resilient system?
    options:
      - "The system keeps working during a failure without needing to make any changes — no scaling, no control-plane calls — because the needed capacity is already provisioned"
      - "The system's configuration is frozen in production and can only be altered during a scheduled maintenance window with a full redeploy"
      - "The system uses statically assigned IP addresses everywhere instead of DNS, so hostname resolution can never sit on the critical path"
      - "The system is pinned to a fixed, unchanging request rate and rejects any traffic that would push it above that steady baseline"
    answer: 0
    explanation: |
      Static stability is the property of surviving a failure with no reactive changes:
      everything required to absorb the failure is already running before it happens.
      It matters because the control plane is often impaired during the very event you're
      trying to survive, so depending on it for recovery is risky.
    ref: "concepts.md#static-stability"

  - id: redundancy-failover-and-health-checks-060
    difficulty: expert
    tags: [shuffle-sharding, blast-radius, combinatorics]
    question: |
      With shuffle sharding across 100 nodes and a shard size of 5, roughly how many
      distinct node combinations are possible, and why does that number matter?
    options:
      - "500 combinations (100 × 5) — just enough to hand every customer a distinct pair of nodes to spread their traffic across"
      - "~75 million combinations — so any two customers overlap on very few nodes, near-totally isolating a poison-pill request to one customer's shard"
      - "100 combinations — exactly one per node, since each customer is pinned to a single home node in the ring"
      - "5 combinations — one per shard slot, because the shard size of 5 is the only degree of freedom that matters"
    answer: 1
    explanation: |
      C(100,5) ≈ 75 million. Because each customer gets a random combination, the chance
      two customers share the exact same 5 nodes is ~1 in 75M, and meaningful overlap is
      vanishingly rare. A poison-pill request therefore damages at most a small fraction
      of other customers instead of the whole fleet.
    ref: "concepts.md#cell-based-architecture-and-shuffle-sharding"

  - id: redundancy-failover-and-health-checks-061
    difficulty: expert
    tags: [cell-based, shuffle-sharding, poison-pill, judgment]
    question: |
      Your service is fully replicated across 3 AZs, yet a single malformed "poison-pill"
      request from one customer crashes every instance that handles it. Why doesn't AZ
      redundancy help, and what does?
    options:
      - "AZ redundancy already covers logical faults as well as infrastructure ones; simply adding a 4th AZ raises the tolerance enough to absorb the poison pill"
      - "Consolidate the whole service back into a single AZ so the crash stays contained to one location and can't fan out across zones"
      - "Increase the health-check frequency so the crashed instances are detected and removed from the pool faster than the poison pill can spread"
      - "AZ redundancy only isolates infrastructure failures — the bad request replicates to every AZ. Cell-based architecture and shuffle sharding limit the logical blast radius"
    answer: 3
    explanation: |
      AZ/region redundancy isolates infrastructure faults, but a poison-pill/logical fault
      travels with the request to every redundant copy. Cells (isolated stacks per customer
      subset) cap blast radius to one cell; shuffle sharding gives each customer a random
      node combination so the bad request only hits that combination.
    ref: "concepts.md#cell-based-architecture-and-shuffle-sharding"

  - id: redundancy-failover-and-health-checks-062
    difficulty: advanced
    tags: [cell-based, blast-radius, isolation]
    question: |
      In cell-based architecture, what is a "cell" and what does it bound?
    options:
      - "A single database row held under a lock for the duration of a transaction, bounding contention to that one row"
      - "A cache entry that expires after a fixed TTL, bounding how long stale data can be served to clients"
      - "A fully-isolated complete stack serving a subset of customers, bounding blast radius to that cell's customers instead of the whole fleet"
      - "A network subnet shared by every service in a region, bounding the address space those services are allocated from"
    answer: 2
    explanation: |
      A cell is an independent, complete stack (its own compute/storage/queues) serving a
      partition of customers, with a thin routing layer mapping customers to cells. A cell
      failure — including a poison-pill bug — affects only that cell's customers, so blast
      radius is 1/(number of cells). Cells are size-capped and tested at max size.
    ref: "concepts.md#cell-based-architecture-and-shuffle-sharding"

  - id: redundancy-failover-and-health-checks-063
    difficulty: expert
    tags: [swim, failure-detection, indirect-ping]
    question: |
      In SWIM, when a node's direct ping to a target gets no ack, it does NOT immediately
      declare the target dead. What does it do instead, and why?
    options:
      - "It immediately gossips the target as confirmed dead so the whole cluster evicts it within a single protocol period, skipping the suspect phase"
      - "It asks k random helper nodes to each send an indirect ping-req to the target, so a single bad network path doesn't cause a false positive"
      - "It waits one full second and then retries the exact same direct ping from itself, escalating to dead only if that second attempt also times out"
      - "It floods every other node in the cluster with a simultaneous ping to the target, using the aggregate response count to decide liveness"
    answer: 1
    explanation: |
      A single dropped packet or one bad path between two nodes shouldn't mean "dead."
      SWIM asks k random helpers to each ping-req the target on its behalf; only if the
      direct ping AND all k indirect paths fail does it move the target to "suspect." This
      guards against false positives from a single degraded link.
    ref: "concepts.md#swim-gossip-based-failure-detection"

  - id: redundancy-failover-and-health-checks-064
    difficulty: expert
    tags: [swim, incarnation-number, refutation]
    question: |
      In SWIM, what is the purpose of a node's monotonically increasing "incarnation
      number"?
    options:
      - "It counts how many times the node has restarted so the cluster can bill each process incarnation separately for its uptime"
      - "It acts as a logical sequence number that totally orders application writes for consistency across all the replicas"
      - "It encodes the node's fixed position on the consistent-hash ring so peers know which key ranges it currently owns"
      - "It lets a wrongly-suspected node refute the suspicion by re-broadcasting 'alive' with a higher incarnation, which overrides the stale 'suspect' everywhere"
    answer: 3
    explanation: |
      SWIM ejects nodes via alive → suspect → confirm, gossiping the suspicion first so
      the target can defend itself. A node that hears itself suspected re-broadcasts
      "alive" with a higher incarnation number; since higher incarnations win, this clears
      the stale suspicion cluster-wide and prevents transient blips from flapping nodes out.
    ref: "concepts.md#swim-gossip-based-failure-detection"

  - id: redundancy-failover-and-health-checks-065
    difficulty: advanced
    tags: [swim, scalability, gossip]
    question: |
      Why does SWIM scale failure detection to very large clusters where all-to-all
      heartbeating does not?
    options:
      - "It separates detection from dissemination and uses randomized probing, giving O(1) probes per node instead of O(n²) messages"
      - "It carries every heartbeat over TCP rather than UDP, and since TCP retransmits reliably it never drops a probe even in a huge cluster"
      - "It still requires every node to ping every other node twice per second, but compresses those probes into a single batched packet per peer"
      - "It automatically disables active failure detection once the cluster grows beyond 1,000 nodes and falls back to periodic full snapshots"
    answer: 0
    explanation: |
      All-to-all heartbeating is O(n²) and collapses at scale. SWIM decouples the failure
      detector (decides "is X alive?") from the gossip layer (spreads membership changes),
      and each node probes only one random peer per period (plus k helpers on failure), so
      per-node load stays constant regardless of cluster size.
    ref: "concepts.md#swim-gossip-based-failure-detection"

  - id: redundancy-failover-and-health-checks-066
    difficulty: expert
    tags: [dns-failover, ttl, scenario]
    question: |
      You must fail traffic away from a bad AZ within 60 seconds. Why is DNS-based
      failover (with a 60s TTL) a poor choice, and what fits better?
    options:
      - "DNS is perfectly fine here — a 60s TTL is a hard contract that guarantees every resolver flips to the new record within 60 seconds"
      - "ISP/corporate resolvers and client-side caches routinely ignore low TTLs, so real failover lags to minutes/hours; use LB-level rerouting, anycast, or zonal shift instead"
      - "DNS records can only ever return a single IP address, so you cannot express the surviving AZ's endpoints alongside the failed one"
      - "DNS-based failover requires a full application redeploy to take effect, which cannot realistically complete inside a 60-second window"
    answer: 1
    explanation: |
      DNS TTL is a hint, not a contract: resolvers and client/JVM caches frequently honor
      it poorly (some pin IPs for a connection's life), so a long tail of clients takes
      minutes to hours to fail over — nowhere near 60s. LB VIP rerouting, anycast (BGP
      reconvergence), and zonal shift don't depend on DNS caches expiring.
    ref: "concepts.md#dns-based-failover-and-ttl-traps"

  - id: redundancy-failover-and-health-checks-067
    difficulty: advanced
    tags: [dns-failover, negative-caching, recovery]
    question: |
      Beyond delaying failover, how does DNS caching also hurt RECOVERY after you've
      fixed an endpoint?
    options:
      - "It permanently deletes the old record from every resolver, so clients get no answer at all until you manually recreate the entry"
      - "It automatically doubles the configured TTL on recovery as a penalty, so cached answers now linger for twice as long as before"
      - "Negative caching (and cached old answers) means resolvers keep serving the stale/absent answer after the fix, so recovery also lags"
      - "It forces every client to re-resolve the name instantly the moment the endpoint is fixed, so recovery is actually faster than failover"
    answer: 2
    explanation: |
      Caching cuts both ways: a resolver that cached "no answer" or the old IP keeps
      serving it even after you've restored the endpoint, so clients see the fixed state
      later than you'd expect. The same TTL/resolver behavior that slows failover also
      slows failback/recovery.
    ref: "concepts.md#dns-based-failover-and-ttl-traps"

  - id: redundancy-failover-and-health-checks-068
    difficulty: advanced
    tags: [anycast, dns-failover, alternatives]
    question: |
      Why can anycast reroute traffic away from a failed site WITHOUT depending on DNS
      TTLs or client caches?
    options:
      - "Anycast simply pushes the updated DNS record to resolvers faster than unicast does, so the TTL effectively shrinks to near zero"
      - "Anycast sends a global signal that disables client-side and resolver DNS caching everywhere, forcing an immediate fresh lookup"
      - "Anycast attaches a separate, protocol-mandated TTL that is always fixed at exactly 1 second, so cached answers expire almost instantly"
      - "The same IP is announced from many locations, so BGP reconverges to a healthy site with no DNS change and no client re-resolution needed"
    answer: 3
    explanation: |
      With anycast the destination IP is constant and advertised from multiple sites; when
      one site fails, BGP routing simply reconverges to another site announcing that IP.
      Clients never need to re-resolve the name, so no DNS cache or TTL sits on the
      critical path — a key advantage over DNS-based failover.
    ref: "concepts.md#dns-based-failover-and-ttl-traps"

  - id: redundancy-failover-and-health-checks-069
    difficulty: advanced
    tags: [zonal-shift, evacuation, static-stability]
    question: |
      Zonal shift lets you evacuate an impaired AZ with one action. What precondition
      must hold for it to be SAFE?
    options:
      - "You must be statically stable across AZs — the surviving AZs already have the capacity to absorb the evacuated AZ's load"
      - "The impaired AZ must first be fully root-caused and the failing component identified before any traffic can be safely shifted away"
      - "The service must be deployed in exactly two AZs, since zonal shift can only redistribute load between a single pair of zones"
      - "All DNS TTLs on the service's records must be lowered to zero so that the traffic move can propagate to clients instantly"
    answer: 0
    explanation: |
      Zonal shift is only the action of moving traffic off an AZ; it's safe only if the
      remaining AZs can already carry the full load without scaling (the (N−k)/N headroom
      rule). Static stability is the precondition; zonal shift is the mechanism that acts
      on it — and it doesn't require diagnosing the impaired AZ first.
    ref: "concepts.md#zonal-shift-and-evacuating-an-impaired-az"

  - id: redundancy-failover-and-health-checks-070
    difficulty: intermediate
    tags: [zonal-shift, autoshift, mechanism]
    question: |
      What does "zonal autoshift" add beyond a manually triggered zonal shift?
    options:
      - "It continuously scales up capacity in every AZ during normal operation, keeping each zone permanently over-provisioned against a future shift"
      - "It lets the cloud provider trigger the AZ evacuation automatically based on its internal telemetry, without waiting for you to notice"
      - "It permanently removes the impaired AZ from your account so that it can never be scheduled into or send traffic to your service again"
      - "It replaces the need to run in multiple AZs entirely, letting a single-AZ deployment survive that zone failing by shifting to a hidden spare"
    answer: 1
    explanation: |
      A manual zonal shift is you issuing start-zonal-shift --away-from. Autoshift delegates
      the trigger to the provider, which shifts traffic off an AZ automatically when its
      internal signals indicate impairment — catching gray-failing zones faster than a human
      reacting to client complaints.
    ref: "concepts.md#zonal-shift-and-evacuating-an-impaired-az"

  - id: redundancy-failover-and-health-checks-071
    difficulty: expert
    tags: [alb, health-check-defaults, detection-time]
    question: |
      Using ALB target-group defaults (interval 30s, timeout 5s, unhealthy threshold 2),
      roughly how long until a dead target is marked unhealthy, and how do you make a
      busy-but-alive node LESS likely to be wrongly evicted?
    options:
      - "~5s (just the single timeout value); lower the interval to 1s so the target is probed far more aggressively and evicted sooner"
      - "~2s (the timeout minus overhead); disable the health check entirely so a busy node can never be marked unhealthy at all"
      - "~65s (30×2 + 5); raise the timeout and/or unhealthy threshold above your worst-case GC pause / p99 stall"
      - "~300s (the default registration timeout); shorten the deregistration delay so draining targets leave the pool faster"
    answer: 2
    explanation: |
      Detection ≈ interval × unhealthy-threshold + timeout ≈ 30×2 + 5 ≈ 65s. To avoid
      evicting a slow-but-alive node, widen the timeout and/or require more consecutive
      failures so a transient stall (GC pause, p99 spike) doesn't trip removal. This is the
      concrete knob behind the false-positive vs detection-latency trade-off.
    ref: "concepts.md#load-balancer-health-check-defaults-and-the-detection-time-budget"

  - id: redundancy-failover-and-health-checks-072
    difficulty: advanced
    tags: [alb, fail-open, death-spiral]
    question: |
      The ALB documentation states what specific fail-open behavior?
    options:
      - "If a target fails a single health check, the LB immediately launches and registers a fresh replacement target to keep the pool size constant"
      - "When the pool is overloaded the LB opens additional listener ports and spreads the incoming connections across them to raise throughput"
      - "On any target failure the LB opens the target group's security group to all inbound internet traffic so recovery probes can reach it"
      - "If all targets fail health checks at the same time in all enabled AZs, the load balancer fails open and routes to all targets"
    answer: 3
    explanation: |
      ALB's documented fail-open: when every target across all enabled AZs is failing
      simultaneously, the LB treats that as "the check is wrong, not the whole fleet
      dead" and routes to all targets. This is the death-spiral defense built into the
      product — mass simultaneous unhealthy almost always means overload/check error.
    ref: "concepts.md#load-balancer-health-check-defaults-and-the-detection-time-budget"

  - id: redundancy-failover-and-health-checks-073
    difficulty: expert
    tags: [prestop, graceful-shutdown, sizing]
    question: |
      To eliminate connection-refused errors during rolling deploys, how should you size
      the preStop sleep relative to the LB's health-check timing?
    options:
      - "preStop sleep should be as short as possible, ideally 0s, since any added delay just holds the pod open longer and slows the rolling deploy without changing routing"
      - "preStop sleep should equal the terminationGracePeriodSeconds exactly, so the pod spends its entire grace window sleeping before the process is ever signaled to stop"
      - "preStop sleep should be ≥ LB health-check-interval × unhealthy-threshold, so the LB observes the target as unready before the process closes its listener"
      - "preStop timing is irrelevant as long as the app handles SIGTERM, because the SIGTERM handler alone fully drains the load balancer before the listening socket is closed"
    answer: 2
    explanation: |
      The LB needs interval × unhealthy-threshold to notice the target went unready. If the
      process closes its socket before that, in-flight/new requests get refused. Sizing the
      preStop sleep to at least that window (e.g. ~15–20s for a 5s×3 check) lets the LB drain
      the target first — the highest-value fix for deploy-time 5xx spikes.
    ref: "concepts.md#load-balancer-health-check-defaults-and-the-detection-time-budget"

  - id: redundancy-failover-and-health-checks-074
    difficulty: expert
    tags: [lease, leader-election, clock-skew]
    question: |
      A lease-based leader believes its lease is still valid and keeps writing, but the
      lease service already expired it and elected a successor. What is the usual root
      cause, and what defends the data?
    options:
      - "The lease duration was simply configured too long; shortening it to a few hundred milliseconds is the only fix, since a brief lease can never outlive a stalled leader"
      - "The successor was wrongly elected by a minority-side partition that lacked quorum; adding more replicas to grow the voting set is what closes the two-leader window"
      - "The load balancer mistakenly routed client writes to both leaders at once; reconfiguring the LB to pin every write to a single target endpoint eliminates the divergence"
      - "Clock skew (or a GC pause) between leader and lease service creates a two-leader window; a monotonic fencing token enforced at the resource rejects the stale ex-leader's writes"
    answer: 3
    explanation: |
      If the leader's clock runs behind the lease service (or a GC pause delays its renewal
      check), it can think it still holds a lease that has actually expired — a two-leader
      window. A lease bounds normal handoff but can't cover this; a fencing token that the
      storage layer enforces rejects the stale leader's writes. Use monotonic clocks for
      elapsed-time and pair lease + token.
    ref: "concepts.md#lease-based-leader-election-and-clock-skew"

  - id: redundancy-failover-and-health-checks-075
    difficulty: advanced
    tags: [lease, leader-election, renewal]
    question: |
      Why must a lease-holding leader renew its lease well BEFORE the lease's expiry time
      rather than right at it?
    options:
      - "To account for network round-trip time, so there's no window where the lease expires and a successor is elected while the old leader still thinks it leads"
      - "To reduce CPU usage on the leader, since renewing early lets the renewal be batched with other periodic housekeeping work and avoids a separate wakeup near expiry"
      - "Because the lease protocol strictly forbids any renewal once more than 50% of the lease duration has already elapsed, forcing the leader to renew within the first half"
      - "To keep the exact lease duration secret from other nodes, so a would-be usurper cannot predict the precise moment the current leader's lease is scheduled to lapse"
    answer: 0
    explanation: |
      Renewal isn't instant — the request takes a round trip. If the leader waits until the
      exact expiry to renew, network delay can push the lease past expiry before the renewal
      lands, letting the service elect a successor while the old leader still believes it
      leads. Renewing early leaves margin so leadership never gaps.
    ref: "concepts.md#lease-based-leader-election-and-clock-skew"

  - id: redundancy-failover-and-health-checks-076
    difficulty: expert
    tags: [two-node, quorum, split-brain, judgment]
    question: |
      A colleague proposes a 2-node cluster for a stateful primary "because two is
      redundant." What is the precise problem?
    options:
      - "Two nodes actually cost more to run than three, because each must be over-provisioned to carry the full load alone if its peer dies, so the pair is simply wasteful"
      - "Two nodes have no possible majority, so a partition forces a choice between split-brain (both act) and losing availability (neither can act) — you get neither safety nor HA"
      - "Two nodes physically cannot replicate data to each other, because replication protocols require a minimum of three participants to establish and commit a durable log quorum"
      - "Two nodes are forced to use fully synchronous replication on every write, which adds so much per-write latency that the pair is far too slow to serve a stateful primary"
    answer: 1
    explanation: |
      With 2 nodes, a 1–1 partition leaves no side with ⌊2/2⌋+1 = 2 votes. Either you let a
      lone node act (split-brain risk) or require both to agree (the survivor can't act when
      its peer dies). Minimum safe voting membership is 3; a 2-node pair needs a witness in a
      third failure domain.
    ref: "concepts.md#two-node-clusters-and-quorum-edge-cases"

  - id: redundancy-failover-and-health-checks-077
    difficulty: expert
    tags: [quorum, even-cluster, fault-tolerance]
    question: |
      Compared to a 3-node quorum cluster, how many MORE node failures can a 4-node quorum
      cluster tolerate?
    options:
      - "One more — a 4-node cluster needs only 2 votes for a majority, so it tolerates 2 failures instead of the single failure a 3-node cluster can survive"
      - "Zero more — a 4-node cluster needs 3 for majority, so it still tolerates only 1 failure, just like 3 nodes"
      - "Two more — with 4 voting members a single surviving node still counts as a quorum, so the cluster keeps working even after 3 of its 4 nodes have failed"
      - "It actually tolerates fewer failures than a 3-node cluster, because the extra even node introduces tie votes that can deadlock the majority calculation entirely"
    answer: 1
    explanation: |
      Majority of 4 is 3, so a 4-node cluster tolerates only ⌊(4−1)/2⌋ = 1 failure — the
      same as 3 nodes, at higher cost. Even counts waste a node; you must go to 5 to tolerate
      2. This is why quorum clusters always use odd counts.
    ref: "concepts.md#two-node-clusters-and-quorum-edge-cases"

  - id: redundancy-failover-and-health-checks-078
    difficulty: expert
    tags: [quorum, witness, failure-domain, scenario]
    question: |
      You run a database across two data AZs and add a witness for quorum. Where should
      the witness go so that losing EITHER data AZ keeps the cluster available?
    options:
      - "In the same AZ as the primary, so the witness has the lowest possible latency to the leader and can confirm lease renewals fastest during normal steady-state operation"
      - "On the same host as one of the data nodes, sharing its hardware to save the cost of a separate instance while still contributing a third distinct vote to the quorum"
      - "It doesn't matter where the witness runs, since a witness only stores a copy of the tie-breaking vote and any location provides exactly the same quorum guarantee"
      - "In a third AZ (or region), so a surviving data AZ plus the witness always forms a majority regardless of which data AZ fails"
    answer: 3
    explanation: |
      A witness co-located with one data AZ provides no majority if that AZ is the one that
      fails. Placing it in a third failure domain means the surviving data AZ + witness = 2
      of 3 votes no matter which data AZ dies, preserving availability. Tiebreakers must live
      in an independent failure domain.
    ref: "concepts.md#two-node-clusters-and-quorum-edge-cases"

  - id: redundancy-failover-and-health-checks-079
    difficulty: expert
    tags: [failure-detection, gc-pause, false-positive, numbers]
    question: |
      A detector heartbeats every 2s and marks a node dead after 3 missed beats (~6s
      budget). In production, stop-the-world GC pauses reach 4s. What is the risk and the
      fix?
    options:
      - "No risk here, because the 6s detection budget comfortably exceeds a 4s pause, leaving a full 2s of margin that absorbs any additional scheduling jitter on the link"
      - "The node will never be detected as dead under this budget; the fix is to shorten the heartbeat interval to 0.5s so any failure is always caught within a single second"
      - "A 4s pause eats most of the 6s budget, risking false-positive failover during GC — raise the threshold above worst-case pause or use phi-accrual/adaptive detection"
      - "GC pauses cannot affect heartbeats at all, because the heartbeat sender runs on a separate dedicated thread that the stop-the-world collector leaves scheduled and running"
    answer: 2
    explanation: |
      A stop-the-world GC pause freezes heartbeat sending too, so a 4s pause consumes most
      of the 6s budget and a slightly longer one triggers a needless failover of a healthy
      node. Set the detection threshold safely above worst-case pause/p99 stall, or use an
      adaptive detector (phi-accrual) that widens slack when the link is jittery.
    ref: "concepts.md#heartbeats-and-failure-detection"

  - id: redundancy-failover-and-health-checks-080
    difficulty: advanced
    tags: [failback, reconciliation, split-brain]
    question: |
      After a split-brain incident where two primaries accepted divergent writes, what is
      the realistic cost of reconciliation, and what does it imply for design?
    options:
      - "Reconciliation is fully automatic and essentially free once the partition heals, so split-brain is harmless and needs no special prevention baked into the design"
      - "You simply restart both nodes after the partition clears and the replication log replays automatically, so the divergent writes self-heal with no committed data lost"
      - "Divergent writes need last-writer-wins, vector clocks, or manual merge — and some systems can't reconcile at all — so it's better to FENCE to prevent split-brain than to repair it"
      - "The load balancer transparently merges the two divergent datasets during failback, diffing the write logs and resolving any conflicts before it restores client traffic"
    answer: 2
    explanation: |
      Reconciling conflicting writes is expensive and lossy: last-writer-wins drops data,
      vector clocks need app-level merge logic, and some data can't be merged at all. Because
      repair is so costly (or impossible), the design principle is to PREVENT split-brain via
      quorum + fencing rather than plan to reconcile after it — and never auto-failback into
      a possibly-diverged node.
    ref: "concepts.md#split-brain-quorum-and-fencingstonith"

  - id: redundancy-failover-and-health-checks-081
    difficulty: advanced
    tags: [health-check-taxonomy, dependency-check, best-practice]
    question: |
      Using the AWS Builders' Library health-check layers (liveness, local, dependency,
      deep/anomaly), which layer should drive the LOAD BALANCER's remove-from-pool
      decision, and which belongs to out-of-band monitoring?
    options:
      - "Dependency checks should drive the LB's remove-from-pool decision so an unhealthy downstream ejects a node instantly, while the liveness layer is relegated to monitoring"
      - "Deep/anomaly checks should drive the LB because they catch the widest range of failure modes, and the shallower liveness and local layers can safely be ignored entirely"
      - "Local/liveness checks drive the LB (so a shared-dependency blip can't empty the pool); dependency and deep/anomaly checks belong to out-of-band monitoring"
      - "All four layers should be wired directly into the LB's routing decision, so that every category of failure immediately removes the affected target from the serving pool"
    answer: 2
    explanation: |
      The LB check should test only local health (things a restart or pool-removal actually
      fixes), so a shared-dependency blip doesn't fail every node at once. Dependency and
      deep/anomaly checks — which compare against downstreams or the fleet — belong to
      out-of-band monitoring and slow controllers, not the instant traffic-routing decision.
    ref: "concepts.md#shallow-vs-deep-health-checks-the-deep-check-anti-pattern"
